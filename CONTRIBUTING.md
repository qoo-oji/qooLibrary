# 貢献するにあたって

## 最初に: このプロジェクトは実在の蔵書を扱う

qooLibrary はマンガ・同人誌のライブラリ管理アプリです。パーサの正しさは
**実際のファイル名の形**に依存するため、開発では実在の蔵書を参照します。

**あなたの蔵書はあなたのものです。** 作品名・サークル名・作者名・フォルダ名は
個人の趣味嗜好と購入履歴を明かす情報であり、**このリポジトリは公開されています。**

そして実際に事故が起きています。フェーズ 1 で実在の固有名詞がソースと文書へ
混入し、公開リポジトリに 8 日間置かれました。除去のために履歴を書き換えて
force-push しましたが、**GitHub は到達不能になったオブジェクトを配信し続ける**
ため露出は続き、最終的にリポジトリを削除して作り直しました。

以下の仕組みは、その再発を防ぐためにあります。

## クローンしたら最初に

```sh
git config core.hooksPath Scripts/git-hooks
```

私的なデータがコミットへ入るのを止める pre-commit hook が有効になります。

**この設定をしないと保護はありません。** 内容の照合は、照合する相手
（あなたの蔵書、あなたの登録フォルダ名）が手元にしか無いため、**CI では
原理的に走れません。**

## 守ってほしいこと

### 実データからは「形」だけを取り出す。「中身」は借りない

テストの標本を実データの形に似せるのは正しい方針です——きれいな例だけで
固めると、その分野で最も普通の入力を取りこぼします。**しかし中身を借りては
いけません。**

| してよいこと | してはいけないこと |
|---|---|
| 「実ファイル名の 75.5% が NFD だった」と集計を書く | 実際のファイル名を例として書く |
| 括弧の対応が崩れた名前が実在すると書く | その名前を引用する |
| 合成名（`98765架空社` `サンプルプレビュー`）で標本を作る | 実在名を少し変えて使う |

**書いてはいけない先には、コード・文書・テストだけでなく、コミットログと
AI との会話も含まれます。** 一度書いたものは、あなたが消しても消えないことが
あります。

### 保護の範囲

| 対象 | 扱い |
|---|---|
| ファイル名・フォルダ名（作品名・サークル名・作者名・登録フォルダ名）| **不可** |
| ボリューム名・機材構成 | 許容 |
| ユーザー名・ホームディレクトリのパス | **不可** |

### 実データは `Tests/GoldenDataset/private/` にだけ置く

`.gitignore` 済みです。ここ以外に実データを書かないでください。
コーパスは `swift Scripts/extract-golden-corpus.swift` で作れます
（アプリが保存した登録フォルダの一覧を読み、あなたの手元にだけ書き出します）。

## 検査

| 検査 | 何を見るか | 走る場所 |
|---|---|---|
| `check-personal-identifiers.swift` | **あなたの環境**に由来する名前（登録フォルダ名・ユーザー名・ホームパス）。コーパス不要 | 手元（pre-commit）|
| `check-private-data-leak.swift` | ゴールデンコーパスの固有名詞との全文照合 | 手元（pre-commit）|
| `check-published-history.swift` | **公開されている面**。force-push で消したつもりの履歴が今も配信されていないか | 手元 |
| その他の静的検査 6 本 | アーキテクチャ制約 | CI |

### 秘密を公開してしまったら

**書き換えでは消えません。** `git filter-repo` ＋ force-push を行っても、
GitHub はハッシュ直指定でのアクセスに応え続けます（実測済み）。

対処は次のいずれかです。**どちらも外部に影響するので、実行前に必ず
リポジトリの持ち主の判断を仰いでください。**

1. ホスト側サポートへ到達不能オブジェクトの削除を依頼する
2. リポジトリを削除して作り直す（fork・star・Issue が少ないうちは安価）

## 開発の作法

実装上の制約・設計判断・過去の失敗から得た教訓は `CLAUDE.md` に、
仕様は `docs/Specifications/` にあります。**着手前に、触る領域の章を
読んでください。**

とくに次の 3 つは、このプロジェクトが繰り返し失敗して身につけたものです。

- **直す前に測る。** 推測で設計を決めない。
- **テストを書いたら変異を当てる。** 「検査を外したら本当に落ちるか」を確かめる。
- **検査を足したら「これは何を見ていないか」を問う。** このプロジェクトの
  漏洩事故は 4 回とも、**検査の適用範囲が脅威の範囲より狭い**ことが原因でした。
