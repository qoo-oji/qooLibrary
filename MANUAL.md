# qooLibrary マニュアル（ライブラリ設定）

<a id="top"></a>

このマニュアルは、qooLibrary の**ライブラリ設定**で使う記法と規則をまとめたものです。
アプリの設定画面にある「マニュアル」リンクは、このファイルの該当する節を開きます。
開発者向けの情報（ビルド手順・設計資料）は [`README.md`](README.md) にあります。

## 目次

| 節 | 内容 |
|---|---|
| [設定の全体像](#overview) | どこで何を設定するか、設定を変えたら何が起きるか |
| [基本設定](#basics) | 重複の表示、埋め込みメタデータ、サムネイル |
| [フィールド](#fields) | ラベルを分類する軸。既定の 6 種と追加フィールド |
| [フォルダ名の解析](#folder-levels) | フォルダの階層からラベルを付ける |
| [ファイル名の解析（ファイル名フォーマット）](#filename-formats) | ファイル名の読み方。予約語と区切り、優先順 |
| [予約語の一覧](#reserved-words) | 全 19 語。コミック向けの 10 語と、映像向けの `@actor` `@season` `@episode` `@subtitle` `@date` |
| [シリーズ名と巻数の決まり方](#series-and-volume) | `@series` `@volume` を書いたとき・書かないときの違い |
| [対象拡張子と画像拡張子](#extensions) | 取り込むファイルの種類 |
| [巻数フォーマット](#volume-formats) | 巻数を読み取る正規表現 |
| [シリーズ名の組み立て](#series-title) | タイトルを持たないファイルの表示名 |
| [区切り文字](#delimiters) | ペア型とセパレータ型 |
| [保護文字列](#protected-tokens) | 区切りを含んでいても 1 つの値として読む範囲 |
| [ブックフォルダ](#book-folders) | 画像だけが入ったフォルダを 1 件として扱う |
| [ライブラリフォルダの中の特別なフォルダ](#special-folders) | `covers` と `.qooarchive` |
| [表記の揺れの扱い](#normalization) | 全角と半角、大文字と小文字、空白 |
| [正規表現の書き方](#regex) | 巻数フォーマットと保護文字列に共通の注意 |
| [未整理のファイル](#unresolved) | どのフォーマットにも一致しなかったファイルの片付け方 |
| [テンプレート](#templates) | プリセット 8 種と自分のテンプレート |

---

<a id="overview"></a>
## 設定の全体像

ライブラリは「フォルダを 1 つ登録し、その中のファイルをファイル名とフォルダ名から
読み取って一覧にする」ものです。読み取りの規則がライブラリ設定です。

| 開き方 | 内容 |
|---|---|
| 「ライブラリ」メニュー →「ライブラリフォルダを登録…」 | 登録ウィザード。テンプレートを選び、実際のファイル名で読み取り結果を確かめてから登録します。登録を押すまで何も変更されません |
| 「ライブラリ」メニュー →「ライブラリの設定…」 | 登録済みのライブラリの設定。通常は 4 つの項目（基本設定・フィールド・フォルダ名の解析・ファイル名の解析）が並び、残りは「高度な設定…」の中にあります |

**設定を保存すると、そのライブラリは自動的にスキャンし直されます。** 手で編集した
タイトルやラベル、評価は、保護されている限りスキャンで上書きされません
（インスペクタの「メタデータを保護」）。

テンプレートは登録のときに初期値をコピーするだけです。登録した後の設定は
そのライブラリのもので、テンプレートを変えても登録済みのライブラリは変わりません
（プリセットが更新されたときだけ、差分を選んで取り込めます。[テンプレート](#templates)）。

---

<a id="basics"></a>
## 基本設定

| 項目 | 内容 |
|---|---|
| サムネイルを常に非表示 | このライブラリの中では、表示メニューの「サムネイルを表示」に関わらずサムネイルを出しません。項目数の多いライブラリや、ネットワーク上のライブラリで役立ちます |
| 重複ファイルをまとめて表示 | 同じタイトル（と巻数）のファイルを、ライブラリ表示で 1 行にまとめます。既定はまとめません。まとめた組には件数のバッジが付き、比較ウインドウでどれを残すかを選べます。ファイルは削除されません |
| 埋め込みメタデータを読む | `ComicInfo.xml`（zip / cbz などの中）・EPUB・PDF が持つ著者・タイトル・シリーズ・巻数を読み、**ファイル名やフォルダ名から読んだ値より優先**します。各ファイルは一度だけ開き、変わっていなければ次のスキャンで開き直しません |

**重複の判定は「タイトルと巻数が同じ」か「タイトルが同じ」かを選べます。**
タイトルが読み取れていないファイル（未整理のファイル）は、どちらの設定でもまとめません。

**埋め込みメタデータの巻数について**: `ComicInfo.xml` には巻数を表す要素が
`Number` と `Volume` の 2 つあり、書き出したアプリによって使い方が逆です。両方の値が
食い違うファイルがあると、スキャンのあとにどちらを使うか尋ねられます。毎回同じ答えで
よければ、「高度な設定…」→「巻数フォーマット」の末尾で「常に Number」「常に Volume」を
選べます。

---

<a id="fields"></a>
## フィールド

**フィールド**は、ラベルを分類する軸です。新しいライブラリには次の 6 種が
最初から用意されています。

| フィールド | 対応する予約語 | 値の出どころ（プリセットの場合） |
|---|---|---|
| 著者 | `@author` | ファイル名の `[著者]` の部分 |
| サークル | `@studio` | ファイル名の `[サークル (著者)]` の部分 |
| ジャンル | `@genre` | ファイル名の `(ジャンル)` の部分 |
| イベント | `@event` | ファイル名の先頭の `(イベント)` |
| キーワード | `@keyword` | ファイル名の末尾の `[キーワード]` |
| 本の種別 | `@mediatype` | ファイル名の先頭の `(同人誌)` `(一般コミック)` など |

- **名前は変えられます。** 「著者」を「作者」に変えても、フォーマットの `@author` は
  そのまま使えます。予約語がフィールドの身元で、名前は表示のためのものです。
- **既定の 6 種は削除できません。** 削除すると、その予約語で取り出した値の行き先が
  なくなります。使わないフィールドはそのまま置いておいても、ラベルが 1 件も無ければ
  ラベルフィルタには出ません。
- **フィールドは追加できます**（上限 10）。追加したフィールドには、
  「参照名」の欄で予約語を結び付けられます——`@keyword2`〜`@keyword5`（用途は
  自由）、`@actor`（出演）、`@season`（シーズン）、`@series`（シリーズ名）から
  選びます。結び付けると、その予約語をファイル名フォーマットに書いて自動で
  ラベルを付けられます。何も結び付けなければ、そのフィールドのラベルは
  インスペクタから手で付けます（プリセットの「プレイ」がその例です）。
- **既定の 6 種の参照名は変えられません。** 「著者」フィールドに `@genre` を
  結び付けられると、後から辿れない設定が作れてしまうためです。
- フィールドごとに「自動付与」のオン／オフがあります。オフにすると、スキャンは
  そのフィールドのラベルを付けたり消したりしません。
- 各フィールドは色を持ち、ラベルの帯の色になります。フィールドの編集ウインドウ
  （「ライブラリ」メニュー →「ラベルを編集…」）でラベルの改名・統合・削除もできます。

シリーズ名は、フィールドではなく各ファイルの属性として保存されます。プリセットの
一般コミックには「シリーズ」というフィールドがあり、シリーズ名がラベルとしても
付きます（`@series` をそのフィールドに結び付けているため）。

---

<a id="folder-levels"></a>
## フォルダ名の解析

ライブラリフォルダの直下を**階層 1**として、フォルダの階層ごとにフォルダ名の
読み方を決めます。割り当てが 1 つも無ければ、フォルダ名は使いません。

```
ライブラリフォルダ
└── 著者値A                      ← 階層 1
    └── 作品名A                  ← 階層 2
        └── 作品名A 第01巻.cbz
```

| 割り当て | 意味 | 例 |
|---|---|---|
| 使わない | その階層のフォルダ名からはラベルを付けません | |
| 単一のフィールド | フォルダ名全体を、選んだフィールドのラベルにします | 階層 1 →「著者」なら `著者値A` が著者ラベルになります |
| フォーマット | フォルダ名をファイル名フォーマットと同じ記法で読み、複数のラベルを取り出します | `[@author] @series` なら `[著者値A] 作品名A` から著者とシリーズを取り出します |

### 規則

- 記法はファイル名フォーマットと同じです（[ファイル名の解析](#filename-formats)）。
  `@title` だけはフォルダ名からは読みません。タイトルは常にファイル名から決まります。
- **フォルダ名とファイル名の両方から同じフィールドの値が取れたときは、ファイル名を
  優先します。** フォルダ名は、ファイル名が持っていないフィールドだけを補います。
  優先はフィールドごとです——フォルダ名からサークルが取れ、ファイル名からは著者と
  ジャンルが取れたなら、3 つとも付きます。
  フォルダ名とファイル名は違う情報を持ち合うのがふつうで、組み合わせて初めて
  全体が揃うという考え方です。同じフィールドで食い違うのは例外なので、
  そのときはファイル名を信じます。
- 同じフィールドを複数の階層に割り当てることができます。その場合、1 つのファイルに
  そのフィールドのラベルが複数付きます。
- 設定した階層にフォルダが無い配置（著者フォルダを介さずライブラリ直下にファイルを
  置いた場合など）は、その階層の割り当てを飛ばしてファイル名だけから読みます。
  エラーにはなりません。
- 階層のフォーマットが一致しなかったフォルダも、その階層を飛ばして先へ進みます。
- [ブックフォルダ](#book-folders) は 1 件のファイルとして扱われるので、階層として
  数えません。

---

<a id="filename-formats"></a>
## ファイル名の解析（ファイル名フォーマット）

ファイル名（拡張子を除いた部分）をどう読むかを、**フォーマット**という 1 行の
記法で書きます。フォーマットは複数登録でき、**上から順に試して最初に一致したもの**が
使われます。並びはドラッグで変えられ、1 本ずつ有効／無効を切り替えられます。

```
(@mediatype) [@studio (@author)] @title (@genre) [@keyword]
```

この 1 本は次のファイル名を読みます。

```
(同人誌) [サークル値A (著者値1)] 作品タイトル1 (ジャンル値1) [キーワード値1].cbz
  └本の種別  └サークル └著者    └タイトル     └ジャンル      └キーワード
```

### 書き方

| 要素 | 書き方 | 意味 |
|---|---|---|
| 予約語 | `@title` `@author` など | ファイル名のその部分を取り出します。一覧は[予約語の一覧](#reserved-words) |
| 区切り文字 | `[` `]` `(` `)` など | フィールドの境目です。有効な区切り文字は[区切り文字](#delimiters)で選びます。入れ子にできます |
| リテラル | それ以外の文字 | ファイル名にその文字がそのまま現れることを要求します。`第@volume話` の `第` `話` など |
| 空白 | 半角スペース | 「空白があってもなくてもよい」という意味です。半角・全角の空白いくつにも、空白なしにも一致します。保存すると半角 1 個に整えられます |

- `@` で始まる語は予約語として読まれます。予約語でない綴りは保存できません。
- 有効にしていない括弧（既定では `【】` など）は、区切りではなくリテラルとして扱われます。
- 区切り文字で切り出した値は、前後の空白を取り除いて保存します。

### 一致の仕組み

フォーマットとファイル名は**全体が完全に一致**しなければなりません。
先頭や末尾に余分な文字が残ると、そのフォーマットは一致しなかったことになり、
次のフォーマットが試されます。

予約語には 2 種類あります。

| 種類 | 予約語 | 一致する範囲 |
|---|---|---|
| 自由文字列 | `@title` `@series` `@author` `@studio` `@event` `@genre` `@keyword` `@actor` `@subtitle` `@keyword2`〜`@keyword5` `@ignore` | 次の区切り文字やリテラルまで。区切り文字が値の中に入っていても、フォーマット全体が一致する読み方があればそれを選びます |
| 型付き | `@volume` `@season` `@episode` `@date` `@mediatype` | 決まった形の文字列にだけ一致します。`@volume` `@season` `@episode` `@date` は[巻数フォーマット](#volume-formats)の各区画に一致するもの、`@mediatype` は本の種別の語彙にあるものだけです。条件を満たさなければ、そのフォーマット全体が不一致になります |

型付きの予約語があるので、`@series (@volume) - @author` に
`作品名A (仮) (01) - 著者値A` を当てると、`(仮)` は巻数ではないためシリーズ名の一部と
読まれ、`(01)` が巻数になります。

### 保存できないフォーマット

| 形 | 理由 |
|---|---|
| 括弧の対応が取れていない | `[@author @title` など |
| `@title` が 2 回ある、同じ予約語が 2 回ある | どちらの値を使うか決められません。`@ignore` だけは何回でも書けます |
| 自由文字列の予約語が区切りなしに並んでいる | `@author @title` のように空白しか挟まないと、どこで切ればよいか決められません。間に区切り文字かリテラルを置くか、`[@author] @title` のように括弧で囲みます。`@volume` `@season` `@episode` `@date` `@mediatype` は境界になるので、隣に置けます |
| 何も取り出さない | `@ignore` だけのフォーマットなど |
| `@title` も `@series` も `@volume` も無い | ただし `(@mediatype)` のように本の種別だけを取り出すフォーマットは保存できます |

### 優先順の決め方

一致する条件が広いフォーマットを上に置くと、狭いフォーマットが一度も試されません。
**細かい形を上に、大まかな形を下に**並べます。プリセットの同人誌は
`(@mediatype) [@studio (@author)] @title (@genre) [@keyword]` から始まり、要素を 1 つずつ
減らした形が下に続き、最後に `(@mediatype) [@studio] @title` のような短い形が来ます。

同じ形で先頭だけが `(@mediatype)` と `(@event)` のフォーマットが並んでいるときは、
`(@mediatype)` の側を上に置きます。`@mediatype` は語彙にある語にしか一致しないので、
`(C99)` のようなイベント名は `(@mediatype)` を素通りして `(@event)` に届きます。

### 編集画面

フォーマットの編集ダイアログには、予約語のパレットとサンプルの欄があります。
パレットの語はカーソルの位置にそのまま挿されます（前後に空白は足しません）。
サンプルにファイル名を入れると、保存する前に読み取り結果を確かめられます。

---

<a id="reserved-words"></a>
## 予約語の一覧

| 予約語 | 取り出すもの | 保存先 |
|---|---|---|
| `@title` | タイトル | 各ファイルのタイトル。手で編集して保護すると、スキャンで上書きされません |
| `@series` | シリーズ名 | 各ファイルのシリーズ名。「シリーズ」フィールドに結び付けてあれば、ラベルにもなります |
| `@volume` | 巻数 | 各ファイルの巻数。ライブラリ表示の並び替えと、重複の判定に使います。ラベルにはなりません |
| `@author` | 著者名 | 各ファイルの著者名と、「著者」フィールドのラベル |
| `@studio` | サークル名 | 「サークル」フィールドのラベル |
| `@event` | イベント名 | 「イベント」フィールドのラベル |
| `@genre` | ジャンル | 「ジャンル」フィールドのラベル |
| `@keyword` | キーワード | 「キーワード」フィールドのラベル |
| `@mediatype` | 本の種別 | 「本の種別」フィールドのラベル。**語彙にある語にだけ一致します**（後述） |
| `@ignore` | 何も取り出さない | 照合のためだけに使い、値は捨てます。1 本の中で何回でも書けます |

映像向けの 5 語です。コミックのプリセットは使いませんが、どのライブラリでも
書けます。

| 予約語 | 取り出すもの | 保存先 |
|---|---|---|
| `@season` | シーズン | 各ファイルのシーズン。フィールドに結び付ければラベルにもなります |
| `@episode` | 話数 | 各ファイルの話数。並び替えとシリーズスタックの順序に使います。ラベルにはなりません |
| `@subtitle` | サブタイトル（1 話ごとの題）| 各ファイルのサブタイトル。検索の対象になります。ラベルにはなりません |
| `@date` | 公開日 | 各ファイルの公開日。`2024` `2024-01` `2024-01-15` の形で保存します |
| `@actor` | 出演 | 結び付けたフィールドのラベル |

用途を自分で決める軸です。フィールドを作って名前を付け、「参照名」の欄で
結び付けてから使います。

| 予約語 | 取り出すもの |
|---|---|
| `@keyword2` `@keyword3` `@keyword4` `@keyword5` | 結び付けたフィールドのラベル |

- `@title` は必須ではありません。`@series` か `@volume` を書いていれば省略できます。
  両方書いた場合は独立した値として扱います。
- `@subtitle` は `@title` と一緒に書けます（`@title` が作品名、`@subtitle` が
  その回の題、という形）。
- `@studio` `@event` `@genre` `@keyword` `@actor` `@keyword2`〜`@keyword5` は、
  対応するフィールドが無いと値が捨てられます（既定の 6 種を削除できないのは
  そのためです）。
- `@series` `@author` `@season` は、フィールドが無くても各ファイルの属性として
  残ります。

### `@mediatype` の語彙

`@mediatype` が一致するのは、次の語だけです。

1. プリセットが持つ本の種別（`一般コミック` `成年コミック` `同人誌` `同人CG集`）
2. そのライブラリの「本の種別」フィールドに**既にある**ラベル

自分の種別（`自炊` など）を使いたいときは、どれか 1 件のファイルに「本の種別」の
ラベルとして手で付けます。次のスキャンからその語にも一致するようになります。

語彙で絞っているのは、`(@mediatype) …` と `(@event) …` のように先頭だけが違う
フォーマットを区別するためです。`@mediatype` を自由文字列にすると、上にあるほうが
イベント名まで吸ってしまいます。

---

<a id="series-and-volume"></a>
## シリーズ名と巻数の決まり方

`@series` と `@volume` は、フォーマットに書いても書かなくても構いません。
書かない場合は `@title` から導きます。

| フォーマット | シリーズ名 | 巻数 |
|---|---|---|
| どちらも書かない | `@title` の末尾から巻数の表記を取り除いたもの。末尾に巻数が無ければシリーズ名は無し | `@title` の末尾から読んだもの |
| `@volume` だけ書く | `@title` から `@volume` に相当する表記を取り除いたもの | `@volume` の値 |
| `@series` を書く | `@series` の値そのまま。`@title` からは何も取り除きません | `@volume` があればその値、無ければ無し |

```
作品名A 第01巻.cbz      フォーマット [@author] @title
  → タイトル: 作品名A 第01巻 ／ シリーズ名: 作品名A ／ 巻数: 1
```

「末尾の巻数」をどう見つけるかは[巻数フォーマット](#volume-formats)で決めます。
`上巻` `前編` のような、シリーズ名を切るだけで番号を持たない表記は、巻数なしとして
扱われます（シリーズ名は `上巻` の手前で切れます）。

ライブラリ表示では、同じシリーズ名のファイルが 1 行にまとまり（シリーズスタック）、
巻数順に並びます。巻数の無いファイルは番号のあるファイルの後ろに来ます。

---

<a id="extensions"></a>
## 対象拡張子と画像拡張子

| 項目 | 内容 | 既定 |
|---|---|---|
| 対象拡張子 | ライブラリに取り込むファイルの拡張子。カンマで区切ります | `zip, cbz, rar, cbr, 7z, cb7, pdf, epub` |
| 画像拡張子 | [ブックフォルダ](#book-folders)の判定に使う画像の拡張子 | `jpg, jpeg, png, gif, webp, avif, heic, bmp, tiff`（空欄のときはこの既定を使います） |

- 一覧に無い拡張子のファイルはスキャンで無視され、ライブラリ表示にも出ません。
  フォルダ表示ではそのまま見えます。
- **対象拡張子を空にしないでください。** 空にすると、すべてのファイルを取り込みます
  （`.DS_Store` やメモの `.txt` も含みます）。
- 動画などをライブラリとして管理したいときは、ここに拡張子を足します。ダブルクリックで
  開くアプリは、環境設定の「ビューア」で拡張子ごとに選べます。

---

<a id="volume-formats"></a>
## 巻数フォーマット

巻数フォーマットは、決まった形の値を読み取るための**正規表現**です。
「高度な設定…」→「巻数フォーマット」で編集します。

画面は**役割ごとに 4 つの区画**に分かれています。どの区画に入れるかで、どの
予約語がそのパターンを使うかが決まります。

| 区画 | 使う予約語 | 何を読むか |
|---|---|---|
| 巻数 | `@volume` | タイトルの末尾の巻数 |
| シーズン | `@season` | `S01` `シーズン2` など |
| 話数 | `@episode` | `E05` `第05話` など |
| 公開日 | `@date` | `2024-01-15` `(2024)` など |

**同じパターンでも区画が違えば別のものとして扱います**——「話数」の区画に
入れたパターンは `@volume` には使われません。以下はまず「巻数」の区画の話で、
他の 3 区画も規則は同じです。

| 既定のフォーマット | 一致する例 | 種別 |
|---|---|---|
| `第([0-9]+(?:\.[0-9]+)?)巻` | `第01巻` `第3.5巻` | 巻数 |
| `([0-9]+(?:\.[0-9]+)?)巻` | `01巻` | 巻数 |
| `(?i:vol(?:ume)?)\.?\s*([0-9]+(?:\.[0-9]+)?)` | `vol.01` `Vol 3` `VOLUME12` | 巻数 |
| `[\s_-]+[vV]([0-9]+(?:\.[0-9]+)?)` | `作品名 v01` `作品名_v01` | 巻数 |
| `[vV]([0-9]+(?:\.[0-9]+)?)` | `v01` | 巻数 |
| `_([0-9]+(?:\.[0-9]+)?)` | `作品名_01` | 巻数 |
| `(上巻\|中巻\|下巻\|最終巻\|前編\|中編\|後編\|完結編\|総集編)` | `上巻` `前編` | 区切り |
| `総集編([0-9]+)` | `総集編2` | 区切り |

同人誌のプリセットには `v` 系と `_` 系が入っていません。成年コミックのプリセットは
巻数フォーマットを持たず、シリーズ名を導きません。必要なら足せます。

### 規則

- **巻数の値**は `(?<volume>…)` という名前付きグループがあればそこから、無ければ
  **唯一のキャプチャグループ**から取ります。キャプチャグループが 0 個、または名前なしで
  2 個以上あると保存できません。数値として読めない値は一致しなかったことにします。
- **種別**は「巻数」と「区切り」の 2 つです。「区切り」はシリーズ名を切るだけで
  巻数を持ちません。`総集編([0-9]+)` のようにキャプチャがあっても区切りにできます。
- 照合は**全角を半角に畳んだ文字列**に対して行います。`[0-9]` は `０-９` にも
  一致します。逆に `[０-９]` のような全角の文字クラスは決して一致しないので、
  設定画面が警告します。
- 空白を必ず 1 つ以上要求したいときは `\s+`、あってもなくてもよいときは `\s*` と
  書きます。全角スペースも `\s` に含まれます。
- タイトルの末尾に届く一致のうち**最も長いもの**を採ります。同じ長さなら一覧で
  上にあるものです。並び順だけで決める仕組みではないので、`第([0-9]+)巻` と
  `([0-9]+)巻` の順序を気にする必要はありません。
- `\s*([0-9]+)`（空白と数字だけ）は既定に入っていません。タイトル末尾の数字を
  巻数と読んでしまうためです。必要なら自分で足してください。
- `@volume` を書いたフォーマットでは、生の数字（`01` `3` `3.5`）と「巻数」種別の
  フォーマットに一致するものが `@volume` として読まれます。「区切り」種別は
  `@volume` の対象になりません。

### シーズン・話数・公開日の区画

- **生の数字には一致しません。** `@volume` だけが `作品名 01` の `01` を拾います。
  `@episode` も同じにすると、`作品名 1920x1080` の `1080` を話数として読んでしまう
  ためです（実際の蔵書で起きることを確かめました）。通し番号を数字だけで書いて
  いる場合は、`#([0-9]+)` のようなパターンを自分で足してください。
- **1 つのパターンで 2 つの値を取れます。** `S(?<season>[0-9]+)E(?<episode>[0-9]+)` の
  ように、区画の名前（`season` `episode`）を付けた名前付きグループを書くと、
  それぞれの値として読まれます。このパターンは「話数」の区画に入れます。
- **公開日**は `(?<year>…)` が必須で、`(?<month>…)` `(?<day>…)` は任意です。
  保存される形は `2024` `2024-01` `2024-01-15` のいずれかです。

### ComicInfo.xml の巻数

一覧の末尾にある設定です。詳細は[基本設定](#basics)を参照してください。

正規表現の書き方と安全のための仕組みは[正規表現の書き方](#regex)にあります。

---

<a id="series-title"></a>
## シリーズ名の組み立て

`@title` を持たないフォーマット（`@series` と `@volume` だけのフォーマットなど）で
読み取ったファイルは、タイトルがありません。そのファイルを一覧にどう出すかを決める
書式で、既定は `@series @volume` です。

書けるのは `@series` `@volume` `@season` `@episode` `@subtitle` の 5 つと、
その間に置くリテラルです。映像プリセットは `@series @episode「@subtitle」` を
使います。

**値の無い部分を囲む括弧は、括弧ごと消えます。** `@series @episode「@subtitle」`
でサブタイトルの無いファイルを読むと `作品名 5` になり、`「」` は残りません。
括弧で囲んでいない部分（`S@seasonE@episode` のような書き方）は消えないので、
括弧で囲むか、値が必ずある予約語だけを使ってください。

組み立てた結果が空になるファイルは、ファイル名で表示されます。

---

<a id="delimiters"></a>
## 区切り文字

区切り文字は、ファイル名の中でフィールドの境目を示す文字です。2 種類あります。

| 種類 | 例 | 意味 |
|---|---|---|
| ペア型 | `[]` `()` `【】` `〔〕` `《》` `〈〉` `「」` `『』` `（）` `｛｝` `{}` | 開きと閉じの組。入れ子にできます |
| セパレータ型 | `-` `_` `/` `~` など | 1 文字（または文字列）でフィールドを区切ります。囲む範囲は持ちません |

- **既定で有効なのは `[]` と `()` だけ**です。`【】` などの全角の括弧はタイトルの
  一部として使われることが多いので、既定では区切りではなくリテラルとして扱います。
- セパレータ型は**既定ですべて無効**です。`-` はタイトルの中にも普通に現れるため、
  有効にするとタイトルが途中で切れます。`シリーズ名 (巻数) - 著者名` のような
  ファイル名を扱うときにだけ有効にしてください。
- セパレータ型は前後の空白ごと 1 つの区切りとして読みます。`A - B` `A-B` `A　-　B` は
  同じです。全角・半角の異体字（`-` `－` `‐` `–` `—` `―`）は 1 つの定義にまとめてあります。
- フォーマットの中に、有効にしていない括弧を書くと、それはリテラルとして照合されます。

---

<a id="protected-tokens"></a>
## 保護文字列

保護文字列は、区切り文字を含んでいても**1 つの値として読みたい範囲**を表す
正規表現です。`作品名A (2019) 第01巻` の `(2019)` を年号として読み、`(` `)` を
フィールドの境目と見なさないために使います。

| 既定の保護文字列 | 守るもの |
|---|---|
| `\((19[0-9]{2})\)` | 括弧に入った 1900 年代の年号 |
| `\((20[0-9]{2})\)` | 括弧に入った 2000 年代の年号 |
| `\((結\|終\|完\|完結\|完全版)\)` | 完結の印 |

### 規則

- 一致した部分は**取り除かれません**。値の一部としてそのまま残ります。
  `[著者値A] 作品名A (完全版)` なら、タイトルは `作品名A (完全版)` になります。
- 位置の条件を「位置を問わない」「先頭」「末尾」から選べます。
- 保護文字列が重なったときは、左にあるもの、同じ位置なら長いもの、それも同じなら
  一覧で上にあるものを採ります。
- **保護文字列は巻数フォーマットより優先**します。`\((下巻)\)` を保護文字列にすると、
  `(下巻)` はシリーズ名の区切りとして読まれません。
- 保護は区切り文字だけでなく型付きの予約語にも効きます。保護した範囲は `@volume` や
  `@mediatype` の候補になりません。
- 長さ 0 に一致する正規表現（`x*` など）は無視されます。
- 照合は全角を半角に畳んだ文字列に対して行い、大文字と小文字を区別しません。

`(仮)` のような印を保護したいときは `\(仮\)` と書きます（括弧は正規表現の特別な文字
なので `\` を付けます）。書き方は[正規表現の書き方](#regex)を参照してください。

---

<a id="book-folders"></a>
## ブックフォルダ

**ブックフォルダ**は、画像だけが入っているフォルダです。qooLibrary はこれを
1 件のファイル（1 冊）として扱います。

次の 3 つをすべて満たすフォルダがブックフォルダになります。

1. サブフォルダを持たない
2. 直下に対象拡張子のファイルが 1 つも無い
3. 直下に画像拡張子のファイルが 1 つ以上ある

`covers` と `.` で始まるフォルダは対象外です。

- フォルダ名は、ファイル名フォーマットで読みます（拡張子が無いだけで、ファイルと
  同じ扱いです）。フォルダ名の解析の階層としては数えません。
- カバー画像は、中の画像をファイル名の自然順（`2` が `10` の前）に並べた先頭の
  1 枚です。
- 中の画像が個々にライブラリへ載ることはありません。
- サブフォルダや対象拡張子のファイルが後から増えると、ブックフォルダではなくなり
  通常のフォルダに戻ります。そのときは通知が出ます。ラベルは残ります。
- 「高度な設定…」→「ブックフォルダの開き方」で、ダブルクリックしたときに中の画像を
  一覧するか、環境設定の「ビューア」で選んだアプリで開くかを決められます。

---

<a id="special-folders"></a>
## ライブラリフォルダの中の特別なフォルダ

| フォルダ | 役割 |
|---|---|
| `covers` | **カバー画像の差し替え用**です。ファイルと同じ名前（拡張子を除く）の画像をここに置くと、中身から作ったサムネイルの代わりに使われます。各フォルダに 1 つずつ置けます。ライブラリには取り込まれません |
| `.qooarchive` | **保管庫**です。「保管庫に移動」したファイルはライブラリフォルダ直下のこのフォルダへ移り、一覧から外れます。ラベルと評価はそのまま残ります。「ライブラリ」メニュー →「メンテナンス…」→「保管庫」で戻せます |

qooLibrary がライブラリフォルダの中に作るのは `.qooarchive` だけです。ラベルや評価は
ファイルに書き込まず、アプリのデータベースに保存します。

---

<a id="normalization"></a>
## 表記の揺れの扱い

ファイル名は手で付けることが多く、全角と半角、大文字と小文字、空白の数が
まちまちです。qooLibrary は次の違いを無視して読みます。

| 違い | 扱い |
|---|---|
| 全角と半角（英数字・記号・空白） | 同じものとして照合します。ファイル名の `（同人誌）` に `(@mediatype)` が一致します。半角カナと長音記号は変換しません |
| 大文字と小文字 | 同じものとして照合します。ラベルも `abc` と `ABC` は同じラベルになります |
| 空白の数 | フォーマットの空白は、空白なしにも複数の空白にも一致します。ラベルの一意性判定では、連続する空白を 1 つに畳みます |
| Unicode の正規化（濁点の合成の仕方など） | 同じものとして扱います。macOS が付けるファイル名の形（NFD）と、他の環境から来た形（NFC）を区別しません |

取り出した値は**原文の綴りで**保存されます。全角のまま付いた名前は全角のまま
ラベルになり、同じ名前の半角のラベルがあればそちらに統合されます（最初に登録された
綴りが表示されます）。

---

<a id="regex"></a>
## 正規表現の書き方

巻数フォーマットと保護文字列は正規表現です。ICU の正規表現（macOS 標準）で、
次の点を知っていれば十分です。

| 書きたいこと | 書き方 | 例 |
|---|---|---|
| 数字 1 桁以上 | `[0-9]+` | `第([0-9]+)巻` |
| 小数も許す | `[0-9]+(?:\.[0-9]+)?` | `3.5` にも一致 |
| どれか 1 つ | `(?:A\|B\|C)` | `(?:上巻\|下巻)` |
| あってもなくてもよい | `?` | `vol\.?` は `vol` と `vol.` に一致 |
| 大文字小文字を区別しない | `(?i:…)` | `(?i:vol)` は `VOL` `Vol` にも一致 |
| 空白 | `\s+`（1 つ以上）／ `\s*`（0 個以上） | 全角スペースも含みます |
| 括弧そのもの | `\(` `\)` `\[` `\]` | `\(仮\)` |
| 値として取り出す範囲 | `(…)` または `(?<volume>…)` | 巻数フォーマットでだけ意味を持ちます |
| 取り出さないグループ | `(?:…)` | `(?:上巻\|下巻)` |

### 安全のための仕組み

正規表現の中には、入力によって照合に非常に長い時間がかかるものがあります
（`(a+)+$` のように、繰り返しの中に繰り返しがある形）。qooLibrary は次の 3 つで
備えています。

1. **照合には時間の上限があります**（1 回 20 ミリ秒）。超えたフォーマットはそのスキャンの
   間だけ無効になり、通知でお知らせします。アプリが固まることはありません。
2. 保存するとき、危険な形（繰り返しの入れ子、後方参照、先読み、全角の文字クラス）を
   **警告**します。警告であって拒否ではありません。
3. 設定画面でサンプルに当てて、実際に一致するかを確かめられます。

---

<a id="unresolved"></a>
## 未整理のファイル

どのファイル名フォーマットにも一致しなかったファイルは**未整理**として扱われます。
ラベルが付かず、タイトルもシリーズ名も無いので、ラベルフィルタから辿り着けません。

- 左ペインの最下部に「未整理のファイル N 件」が常に出ています。押すと、そのライブラリの
  未整理のファイルだけが一覧になります。
- 一覧の上部にある「フォーマットを追加…」で、その場でフォーマットを 1 本足せます。
  足すと、未整理のファイル全件に対して読み直しが走ります。
- 各ファイルのインスペクタには、**近いフォーマット**（先頭からいちばん長く一致した
  フォーマット）が出ます。どこまで読めたかの手がかりになります。
- ラベルを手で付けると、そのファイルは未整理の一覧から消えます。
- 一致させるつもりの無いファイルは、右クリックの「以後出さない」で一覧から外せます。
  名前を変えると、この設定は解除されます。
- 未整理が 500 件を超えるときは、1 件ずつ片付けるより先にフォーマットを足すことを
  勧める案内が出ます。

スキャンの結果に未整理のファイルがあったときは、スキャンのあとにその件数が通知されます。

---

<a id="templates"></a>
## テンプレート

テンプレートは、登録のときに設定の初期値として使う雛形です。

### プリセット

| プリセット | ファイル名の形 | フォルダ名の解析（階層 1）| 巻数フォーマット |
|---|---|---|---|
| 一般コミック | `(@mediatype) [@author] @title` ／ `[@author] @title` | `[@author] @series` として読む | あり |
| 成年コミック | 上と同じ | フォルダ名を「著者」にする | なし |
| 同人誌 | `(@mediatype) [@studio (@author)] @title (@genre) [@keyword]` と、要素を減らした形。先頭が `(@event)` の形も | フォルダ名を「サークル」にする | あり（`v` 系と `_` 系を除く）|
| 同人CG | 同人誌と同じ形（本の種別は `同人CG集`）| フォルダ名を「サークル」にする | あり（同上）|
| 映像作品 | `@episode「@subtitle」` `@series @episode` など。`S01E01` の形にも一致 | フォルダ名を `@series` として読む | 話数・シーズン（巻数は無し）|
| 映像（単発）| `[@actor] @title` ／ `@title` | 使わない | 無し |

映像の 2 つは対象拡張子が違います（`mp4, mkv, mov, m4v, avi, wmv, ts`）。
フィールドも 6 種で、コミックの「サークル」「イベント」の代わりに「出演」
「シーズン」「シリーズ」を持ちます。

映像のファイル名は書き方が人によって大きく違うので、**プリセットのままでは
一致しないファイルが多く残ります**。残ったものは[未整理](#unresolved)の一覧に
出るので、そこから自分の形に合わせたフォーマットを足してください。

登録ウィザードでは、選んだフォルダの実際のファイル名を各プリセットに当てて、
一致した件数の多いものを推奨します。

**フォルダ名の解析を使うかどうかは、登録ウィザードの「基本的なカスタマイズ」で
選べます。** 既定は、あなたのフォルダ名が実際にラベルとして使える形かどうかを
実測して決めます——たとえばサークル名でフォルダ分けしていれば「使う」、
作品名やジャンルで分けていれば「使わない」が既定になります。判断の根拠は
「フォルダ名の N% が、ファイル名から取れる情報と一致します」という形で示されます。

プリセットが新しいバージョンになると、そのプリセットから登録したライブラリに
通知が届き、ライブラリ設定の「基本設定」に案内が出ます。差分ビューで項目ごとに
取り込むかどうかを選べます。自分で編集した項目には印が付き、既定では選ばれません。
適用は取り消せます。

### 自分のテンプレート

「ライブラリ」メニュー →「テンプレートを管理…」で、テンプレートを作成・複製・編集・
削除できます。ライブラリ設定の「テンプレートとして保存…」や、登録ウィザードの
カスタマイズ画面からも保存できます。

- プリセットは編集できますが、保存は「別名で保存…」だけです。プリセット自体は変わりません。
- テンプレートは JSON として書き出し・読み込みができます。
- テンプレートを削除しても、そこから登録したライブラリには影響しません。
