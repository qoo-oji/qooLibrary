# 14. UI — メインウインドウ

対応要件: MW-01〜MW-04 / LP-01〜LP-08 / LF-01〜LF-14 / PN-01〜PN-06 / RT-01〜RT-03 / TB-01〜TB-12 / VM-01〜VM-23 / LV-01〜LV-04 / IV-01〜IV-10 / SR-01〜SR-06 / RP-01〜RP-12 / CV-01〜CV-08 / RL-01〜RL-07 / RA-01〜RA-08 / DT-01〜DT-11

---

## 14.1 全体構成

```
┌──────────────┬─────────────────────────────────┬──────────────┐
│ 左ペイン       │ 中央ペイン                          │ 右ペイン       │
│              │ ┌ ツールバー ────────────────┐ │              │
│ ┌ フォルダ ─┐ │ │ 表示切替/戻進/アドレス/検索…  │ │ 詳細情報       │
│ │ ツリー    │ │ └────────────────────────┘ │ カバー画像     │
│ │          │ │                                 │ ラベル設定     │
│ ├──────────┤ │ 一覧（アイコン / リスト）           │ 評価          │
│ │ ラベル    │ │                                 │              │
│ │ フィルタ  │ │                                 │              │
│ └──────────┘ │                                 │              │
├──────────────┴─────────────────────────────────┴──────────────┤
│ ステータスバー                                                    │
└────────────────────────────────────────────────────────────────┘
```

**[CP-01 の例外]** 他の画面と違い `ThreePaneWindow` は使わず、`NavigationSplitView` + `.inspector()` を使う [設計判断]。戻る/進む/上の階層へのツールバー項目がサイドバーとの境界線を追従してセンター（中央）ペインの左端に揃う（`NSTrackingSeparatorToolbarItem` 相当）挙動が `NavigationSplitView` でしか得られないため。タブ・複数ウインドウ対応 [MW-01][MW-02][MW-03]。実装の詳細な試行錯誤（`ToolbarItemGroup(placement: .navigation)`・`.inspector()` 使用時にツールバー項目がインスペクタ側へ移動してしまう既知の制限・`ToolbarSpacer` 等）は `CLAUDE.md` 参照。

```swift
public struct MainWindow: View {
    @Environment(WindowState.self) private var windowState
    @Environment(SessionState.self) private var session
    var body: some View {
        NavigationSplitView {
            VSplitView { FolderTreePane(); LabelFilterPane() }   // [14.2]
        } detail: {
            FileListPane()                                       // [14.3]
                .inspector(isPresented: $isInspectorPresented) {
                    InspectorPane()                               // [14.4]
                }
                .toolbar { /* 戻る/進む/上の階層へ・表示切替・新規フォルダ・右ペイン折りたたみ */ }
        }
        .safeAreaInset(edge: .bottom) { StatusBar() }
    }
}
```

## 14.2 左ペイン

### 上半分: フォルダツリー

4 グループに分けて表示する。

| グループ | 内容 |
|---|---|
| ボリューム | Macintosh HD、外部ボリューム。ツリー展開で配下フォルダを表示 |
| **よく使う項目** | 環境設定で表示を選べる標準の場所 [ユーザー要望、下記「よく使う項目グループ」] |
| テンポラリフォルダ | 登録フォルダを最上位としてツリー表示。複数登録可 |
| ライブラリフォルダ | 同上 |

```swift
public struct FolderTreeNode: Identifiable, Sendable {
    public let id: String            // 正規化パス
    public let url: URL
    public let displayName: String   // 登録フォルダは displayName [LP-03]
    public let kind: Kind            // .volume / .home / .temporary / .library / .plainFolder
    public var isOnline: Bool        // オフラインはグレーアウト [LP-04]
    public var isLocked: Bool        // ロック中はグレーアウト [LK-22]
    public var hasAccess: Bool       // FDA 未付与領域 [SB-04]
    public var isSymlink: Bool       // バッジ表示 [SL-06]、展開不可 [SL-05]
    public var children: [FolderTreeNode]?
}
```

| # | 要件 |
|---|---|
| LP2-01 | 「テンポラリフォルダ」見出し右端に `＋` と歯車 [LP-01] |
| LP2-02 | 「ライブラリフォルダ」見出し右端に `＋` と歯車 [LP-02] |
| LP2-03 | 登録フォルダは `displayName` で表示 [LP-03][RG-05] |
| LP2-04 | オフラインはグレーアウト + ツールチップで理由 [LP-04][VD-11] |
| LP2-05 | 展開状態を保存・復元 [LP-05] |
| LP2-06 | 中央ペインの表示フォルダと選択位置を同期 [LP-06] |
| LP2-07 | 各グループは折りたたみ可 [LP-07] |
| LP2-08 | ライブラリ配下の `.qooarchive` をツリーに表示しない [LP-08][FA-11] |
| LP2-09 | アクセス権のない領域は「アクセス権がありません」と表示し、付与導線を出す [SB-04] |
| LP2-10 | 見出しへのフォルダドロップで登録 [DD-04]。FS 検証で拒否する場合は理由を表示 [FS-05] |
| LP2-11 | 中央ペインの選択項目のドロップで移動 [DD-05]。ライブラリへのドロップ後は自動ラベル付与 [DD-06] |

#### よく使う項目グループ [HG-01〜HG-10、ユーザー要望]

Finder のサイドバー「よく使う項目」にあたるグループを、ボリュームとテンポラリの
間に置く。並べられる場所と既定は **`FavoriteLocations` が唯一の出どころ**
（`Sources/qooLibraryApp/MainWindow/FavoriteLocations.swift`）。

| # | 要件 |
|---|---|
| HG-01 | 並べられるのは アプリケーション（`/Applications`）／ホーム／デスクトップ／書類／ダウンロード／ムービー／ミュージック／ピクチャ の 8 つ。**順序はこの並びで固定**（並べ替えは提供しない——位置が変わらなければ覚えた場所で押せる） |
| HG-02 | **既定で表示するのは アプリケーション・ホーム・デスクトップ・ダウンロード の 4 つ**［ユーザー指定］。残りは環境設定「表示」タブの「「よく使う項目」に表示」で切り替える |
| HG-03 | 表示・非表示は**項目ごとに 1 つのキー**（`qoo.favorites.visible.<rawValue>`）で持つ。配列で持つと「まだ設定していない」と「全部消した」を区別できず、後から候補を増やしたときに既存の利用者へ出すかどうかも決められない |
| HG-04 | 存在しないフォルダは出さない（`~/Sites` のように既定では作られないものがある）。**アクセス権の有無では落とさない**——落とすと許可を与える入口が消える |
| HG-05 | 表示名は文字列カタログから引く（`menu.go.*`）。訳語は Apple の一次情報（`/System/Library/CoreServices/SystemFolderLocalizations`）から写す。**`localizedName` は使わない**——`Bundle.main.preferredLocalizations` に固定され、アプリ内の表示言語切替に追従しない [実測] |
| HG-06 | アイコンは `FileIconProvider`（Finder と同一）。未許可でも正しく取れる [実測] |
| HG-07 | 行を押したとき、未許可なら**その場で許可パネル**を出してから移動する（`StandardLocationOpener`）。展開して「アクセス権がありません」になった場合も、そのボタンからその場で求める——環境設定へ誘導する他のグループとはここだけ扱いが違う［ユーザー判断: 明示的な要求の直後だから］ |
| HG-08 | 入口は `NavigationRoot.favorites`。⌘↑ は**実ホームで止まる**（ダウンロードからホームへは上がれる）[ユーザー判断]。`/Applications` のように実ホーム配下でない項目には天井を設けない |
| HG-09 | これらの行では、フォルダ自体を作り替える操作（名前を変更・複製・ゴミ箱・圧縮・エイリアス）を出さない [ユーザー判断、`FolderTreeRowContext.allowsStructuralOperations`]。配下の通常フォルダには従来どおり全項目が出る |
| HG-10 | 行への D&D は許可する（書き込むのは「そのフォルダの中へ」なので、標準の場所自体は壊れない）[ユーザー判断] |

**サンドボックス下の到達性**（実アプリと同一 entitlement のプローブで実測。
根拠の表は `qooLibrary.entitlements` のコメント）:

| 項目 | 素の到達性 | 対応 |
|---|---|---|
| アプリケーション（`/Applications`） | ○ | 素のサンドボックスで読める |
| ダウンロード／ムービー／ミュージック／ピクチャ | ○ | `com.apple.security.files.downloads.*` と `assets.*` で素通り |
| デスクトップ／書類 | ✗（TCC） | そのパスへの許可が要る。`/` を許可していても毎回 TCC が出る |
| ホーム | ✗ | `/` への許可で足りる |

#### 起動時に開くフォルダは「グループ直下に見えているもの」から選ぶ [ユーザー判断]

| グループ | 選べる範囲 |
|---|---|
| ボリューム | 任意のフォルダ（`NSOpenPanel`＋専用のブックマーク） |
| よく使う項目 | **いま表示している項目**のみ |
| テンポラリ／ライブラリ | 登録済みのフォルダ |

選んだ場所へ届かない（環境設定で隠した・フォルダが消えた・読めない）ときは
**ボリューム経由のホームへ落とす**［ユーザー判断］。判定は `access(2)` 1 回で、
よく使う項目はすべて起動ボリューム上なので待ちが青天井になる I/O ではない。

`StartupFolderKind.favorite` の `rawValue` が `"virtualHome"` なのは歴史的な事情
（以前この値は「ホーム」固定を意味した）。既存の設定はそのまま
「よく使う項目 ＞ ホーム」として引き継がれる。

#### パッケージは 1 つの項目として扱う [PK-01〜PK-06、ユーザー要望]

`.app` をダブルクリックすると中へ入ってしまっていたのを、Finder と同じく
**起動する**ようにした。判定は `URLResourceKey.isPackageKey`——`.app` だけでなく
`.photoslibrary`／`.rtfd`／`.bundle` なども対象［ユーザー判断: パッケージ全般］。

| # | 要件 |
|---|---|
| PK-01 | ダブルクリック／Enter で**開く（起動する）**。中へは入らない |
| PK-02 | 中を見るのはコンテキストメニュー「パッケージの内容を表示」から（訳語は Finder の実文言 `LocalizableMerged.strings` の `N158` から写した） |
| PK-03 | **フォルダツリーに出さない。** `/Applications` を展開すると素のフォルダだけが並ぶ（[実測] 45 件中 41 件がパッケージ） |
| PK-04 | 種類は実際の種別（`.app` なら「アプリケーション」）。「フォルダ」とは答えない |
| PK-05 | 「フォルダを上にまとめる」[LV-03] ではファイル扱い。ドロップ先にもしない |
| PK-06 | 再帰検索は**中へ降りない**（`.app` 1 つで数千件あり、降りると結果が中身で埋まる）。パッケージ自身は結果に含める |

**実体がディレクトリであること自体は隠さない。** `FolderEntry.isDirectory` は
真のままで、UI の分岐は `isNavigableFolder`（`isDirectory && !isPackage`）を見る。
サイズ欄のように「実体がディレクトリか」を知りたい箇所は `isDirectory` を使う
——ここを潰すと、ディレクトリの `fileSize` が `nil` であるために
**アプリのサイズが「0 KB」と表示される**（実機検証で発見）。

副次的に、写真・ミュージックライブラリ（`.photoslibrary` 等）の中へ降りなくなる
ので、そこで出ていた TCC の許可ダイアログも止まる。

**三角マークの判定もこれに揃える** [`DirectoryProbe.hasSubdirectory(countingPackages:)`]
——ツリーに出さないものを理由に三角を出すと「開いても何も無い」嘘になる。
同じ理由で **`UF_HIDDEN`（`chflags hidden`）が立った項目も数えない**
[ユーザー報告で発見]: 名前が `.` で始まらないので `readdir` の名前判定では
弾けないが、Finder も `.skipsHiddenFiles` も隠すため、数えると
「一覧には何も出ないのに三角だけがある」状態になる。

#### 行の識別子は「正規化パス＋枝」[実機検証で発見・修正]

**同じ実フォルダが 2 つの枝に同時に現れる。** `~/Downloads` は「よく使う項目」の
行であると同時に、Macintosh HD → Users → … と辿った先にもある。以前はパス文字列
だけを鍵にしていたため、片方を展開するともう片方も開き、選択のハイライトが 2 か所に
出て、`scrollTo` がどちらへ飛ぶか決まらなかった——最後のものは「起動時に
『よく使う項目』の行をフォーカスする」を直接壊す。

`FolderTreeSelection`（url ＋ branch、同一性は**正規化パスと
`FolderTreeBranch.identityKey`**）を選択・展開・可視判定・スクロールのすべての
鍵にする。`URL` の等価判定は末尾スラッシュや `isDirectory` フラグまで見るので
同一性には使わない。枝の同一性に `rootURL` を含めないのも同じ理由
（登録 ID だけで一意に決まる）。

**ライブラリとテンポラリは同一性の上では区別しない**（`registered:<id>` に畳む）。
`NavigationRoot` は種別を持たないので、選択を組み立てる側は登録一覧を引いて
種別を決めるしかない——その一覧は非同期に読み込まれるため、**起動直後は空で、
テンポラリをライブラリと取り違える**。取り違えても登録 ID は同じなので、
畳んでおけば行と選択が一致する（登録 ID は種別をまたいで一意）。
**一覧が揃ったあとに選択を組み直す、という対処を採ってはいけない**——
そのメソッドは `await` を挟むので、あとで読む `selectedURL` は 1 世代古い値に
なり得て、`.onChange(of: listSelection)` がユーザー操作と区別できずに発火し、
**起動時フォルダを開いた直後にそれを捨てて古い場所へ戻す**（実機検証で踏んだ）。

**登録フォルダでも同じ形が元からあった**（登録したフォルダはボリュームを辿っても
到達できる）——「よく使う項目」の追加で確実に起こるようになったので直した。

#### 起動直後の自動展開は「材料」と「行き先」が揃ってから

`FolderTreePane` は次の 2 つが揃った時点で**1 回だけ**自動展開する:

- `treeDataLoaded`: ボリューム・ホーム・登録フォルダの一覧を読み終えた
- `startupFolderResolved`: 「起動時に開くフォルダ」の解決が終わった
  （`MainWindowView`）

**待たないと、解決前の暫定の行き先に対して展開が走り、`expandedNodeIDs` は減らない
のでその展開が残る**——ユーザー報告「テンポラリフォルダを指定しているのに、起動すると
Macintosh HD がホームフォルダまで展開される」がこの形だった。

以前は「上書きの見込みがあるか」を `UserDefaults` から事前に推測していたが、
`.home` を除外していたため **起動時フォルダを「ホーム」にすると仮想ホーム（`Data`）が
開いていた**［実機検証で発見］。`TabTarget.home` は `WindowState.init` で評価され、
その時点では `VolumeAccessStore.loadAndActivateAll()` がまだ終わっていないため
`defaultHome` が「実ホームを読めない」と判断してしまう。**解決を必ず通す**ように
して直した。

#### グループの開閉は永続化する [ユーザー判断]

4 グループそれぞれの開閉を `UserDefaults`（`qoo.folderTree.expanded.*`、既定はすべて
開）に覚える。**`@AppStorage` は使わない**——これらは「行を描くかどうか」の `if` 条件に
使うため、その形で読むと Observation が無限に再評価してハングする [1-9 で実際に踏んだ]。
初期値だけ素の値として読み、変更時に明示的に書き戻す一方向の同期にする。

**覚えるのは見出しのクリックだけ。** 自動展開（`revealSelectionIfNeeded`）は
`@State` を直接動かす——自動で開いたものまで覚えると、利用者が畳んだ意図を
アプリ側が黙って上書きすることになる。

### 下半分: ラベルフィルタ

```swift
// 実装は `QooApplication/LabelFilterModel.swift`（**アプリ層に置く**——UI に
// 置くと自動テストから触れない。SwiftUI には依存しない）。
@MainActor @Observable public final class LabelFilterModel {
    public private(set) var library: LibrarySummary?          // nil なら出さない [LF-01]
    public private(set) var groups: [LabelGroupSummary]       // ラベル 0 件は除く [LF-02]
    public private(set) var labels: [LabelGroupID: [LabelSummary]]
    public private(set) var selection: [LabelGroupID: Set<LabelID>]  // ウインドウ固有 [ST-21][LF-06]
    public var ratingFilter: FileQuery.RatingFilter?          // [RT-01〜03]
    public private(set) var matchedCount: Int?                // ライブラリ全体の該当数 [LF-11]
    public private(set) var totalCount: Int?                  // 分母 [LF-11]
    public private(set) var allowedChildNames: Set<String>?   // 中央ペインが残す名前 [VM-02]
    public private(set) var revision: Int                     // 再計算の合図

    public func toggle(_ label: LabelSummary)
    public func clearAll()                                    // [LF-07] ⇧⌘K。評価も落とす
    public func visibleLabels(in: LabelGroupSummary) -> [LabelSummary]   // [PN-02][PN-03][PN-06]
    public func load(registrationUUID: UUID?, services: LibraryServices) async
    public func refreshResults(folderRelativePath: String?, services: LibraryServices) async
    public func deepMatches(_ urls: [URL], libraryRootPath: String,
                            services: LibraryServices) async -> Set<URL>?  // [LF-14]
}
```

**中央ペインへは「DB の一覧を描く」のではなく「実体の一覧を絞る」形で効かせる。**
フォルダ表示モードではすべてのファイル操作が可能でなければならず [VM-03]、フィルタが
全 OFF なら DB に載っていないもの（対象拡張子以外）も従来どおり見える必要がある
[VM-01] ため。絞り込みの入口は 2 つ:

| 場面 | 使うもの | なぜ分かれるか |
|---|---|---|
| 通常の一覧 | `allowedChildNames`（直下の名前の集合）| 1 度の問い合わせで「該当ファイル」と「該当を配下に持つフォルダ」が両方得られる [VM-02] |
| 再帰検索の結果 | `deepMatches`（URL ごとに照合）| 深い階層の該当は**直下フォルダの名前へ畳まれて**集合に入るので、名前で突き合わせると同じフォルダの非該当ファイルまで通る [LF-14] |

**`nil` は「絞らない」**。空集合（＝何も残さない）と取り違えると一覧が黙って空になる
ので、問い合わせに失敗したときも `nil` に倒す——件数だけ伏せる。

### 実装状況（2-8、2026-08）

| 項目 | 状態 |
|---|---|
| LF-01〜02 / LF-04〜11 / LF-13〜14 | 済 |
| PN-02〜PN-06 | 済（PN-05 の検索欄は下記の未確認あり）|
| RT-01〜RT-03 | 済。**評価を付ける手段（RA-01〜08）も実装済み**（§14.4「評価」、2026-08）——以前は実データで常に 0 件だった |
| LF-03（グループの D&D 並べ替え）| `List` の `.onMove` で実装済み。実機未確認 |
| LF-12（アーカイブ済みラベル）| 済（`labels(includeArchived: false)`）|
| VM-04（`.qooarchive`）| 未（ファイルアーカイブ自体がフェーズ 3）|
| 未確認 | ラベル内の検索欄 [PN-05] と再帰検索との併用 [LF-14] の**実機操作**。合成入力が TextField に届かないため（既存のツールバー検索でも同じ）、手動確認へ回した |

| # | 要件 |
|---|---|
| LF2-01 | ライブラリまたはその配下を表示中に、有効なラベルグループの一覧を表示 [LF-01] |
| LF2-02 | ラベル 0 件のグループは表示しない [LF-02][LG-05] |
| LF2-03 | グループの並べ替えは D&D。順序はライブラリ単位の永続設定で全ウインドウ共有 [LF-03][LG-07][ST-23] |
| LF2-04 | グループをクリックで所属ラベルを展開 [LF-04] |
| LF2-05 | ラベルはチェックボックスで複数選択 [LF-05] |
| LF2-06 | 初期状態は全 OFF。選択状態はウインドウごとに独立 [LF-06][ST-21] |
| LF2-07 | 一括 OFF ボタン [LF-07] |
| LF2-08 | 同一グループ内は OR、グループ間は AND。`(1-A OR 1-B) AND (2-A OR 2-B)` [LF-08][LF-09][LF-10] |
| LF2-09 | 現在の条件と該当件数を常時表示 [LF-11] |
| LF2-10 | アーカイブ済みラベルは表示しない [LF-12][LA-02] |
| LF2-11 | アーカイブ済みファイルは結果に含めない [LF-13][FA-05] |
| LF2-12 | フォルダ表示モード／ライブラリ表示モードのいずれでも有効 [LF-14] |
| LF2-13 | ライブラリを切り替えたら選択をリセット [ST-26] |

### ピン留め [PN-01〜PN-06]

```pseudo
visibleLabels(group, selection, expanded):
    pinned = group.labels.filter(isPinned && !isArchived)
    checked = group.labels.filter { selection[group.id]?.contains($0.id) == true }
    if expanded: return すべて（スクロール + インクリメンタル検索）      # [PN-05]
    if pinned.isNotEmpty: base = pinned                               # [PN-02]
    else:                 base = group.labels.sortedByName().prefix(10)  # [PN-03]
    return (base ∪ checked).sortedByName()                            # [PN-06]
```

| # | 要件 |
|---|---|
| PN2-01 | ピン留めは必須機能。100 超のラベルを前提とする [PN-01] |
| PN2-02 | ピン留めがなければ名前順で上位 10 件、残りは「もっと見る」[PN-03] |
| PN2-03 | 各ラベル右にピンボタン。ライブラリ単位の永続設定で全ウインドウ共有 [PN-04][ST-23] |
| PN2-04 | 「もっと見る」展開時はスクロール可能 + インクリメンタル検索 [PN-05] |
| PN2-05 | チェック中のラベルはピン対象外でも常に表示 [PN-06] |

### お気に入り評価フィルタ [RT-01〜RT-03]

```swift
// 実体は `FileQuery.RatingFilter`（`QooKit`）——UI とクエリで別の型を持つと
// 変換の往復で意味がずれるので 1 つにしてある。
public struct RatingFilter: Sendable, Hashable {
    public let stars: Int                    // 0〜5。`init` で丸める [RT-01]
    public let mode: Mode                    // [RT-03]
    public enum Mode: String, Sendable, Hashable, CaseIterable { case atLeast, exact }
    public static let unrated = RatingFilter(stars: 0, mode: .exact)
}
```

ラベルフィルタと AND で結合する [RT-02]。UI が出すのは星 1〜5 [RT-01] で、
**星 0 は `.exact` と組んで「未評価だけ」を表す**（`rating` 列は未評価を 0 で持つ）
——以前は `minimum` と `unratedOnly` の 2 つで表していたが、それでは
「星 3 ちょうど」を書けなかった。範囲外の星は `init` で丸める（決して一致しない
条件を作らせない）。

## 14.3 中央ペイン

### ツールバー [TB-01〜TB-12]

| ID | 要素 | 有効条件 |
|---|---|---|
| TB-01 | 表示モード切替（シーソー） | ライブラリフォルダを開いているときのみ。ボリューム／テンポラリでは無効 |
| TB-02 | 戻る・進む | 常時 |
| TB-03 | アドレスバー | 常時。パスコンポーネントのクリックで階層移動 |
| TB-04 | アイコン／リスト切替 | 常時 |
| TB-05 | 保管庫に移す | ライブラリのみ |
| TB-06 | 自動リネーム設定 | テンポラリのみ |
| TB-07 | 一括リネーム実行 | テンポラリのみ |
| TB-08 | 検索バー | 常時 |
| TB-09 | ライブラリへ移動 | テンポラリのみ |
| TB-10 | 再スキャン | ライブラリ／テンポラリ |
| TB-11 | ラベル再適用 | ライブラリのみ |
| TB-12 | 変換リネーム | テンポラリのみ |

無効時はツールチップで理由を示す [MX-04]。ロック中は変更系を無効化し、理由を示す [LK-22][LK-23]。

### フォルダ表示モード [VM-01〜VM-04]

| # | 要件 |
|---|---|
| VM2-01 | フィルタ全 OFF なら Finder 同等にすべて表示 [VM-01] |
| VM2-02 | フィルタ ON なら、条件に該当するファイルと**該当ファイルを配下に持つフォルダ**だけを表示 [VM-02] |
| VM2-03 | すべてのファイル操作が可能 [VM-03] |
| VM2-04 | フィルタ全 OFF のときのみ `.qooarchive` の中にアクセス可 [VM-04][FA-11] |

VM-02 の実装: `LabelIndex.query` の結果から相対パスの祖先集合を作り、現在フォルダ直下の子のうち「自身が該当」または「祖先集合に含まれる」ものだけを残す。

### ライブラリ表示モード [VM-10〜VM-16]

| # | 要件 |
|---|---|
| VM2-10 | フィルタ全 OFF なら、現在フォルダ配下の対象拡張子ファイルとブックフォルダをフラット表示 [VM-10][IF-16] |
| VM2-11 | 対象は**現在のフォルダ配下のみ**。ライブラリ全体ではない [VM-11] |
| VM2-12 | フィルタ ON なら対象拡張子 ∧ フィルタ条件でフラット表示 [VM-12] |
| VM2-13 | リネーム・削除は可能。移動・コピー・圧縮／展開・新規フォルダ作成は無効化 [VM-13] |
| VM2-14 | `.qooarchive` 配下は表示しない [VM-14][FA-12] |
| VM2-15 | ソートはタイトル名・シリーズ名・巻数・更新日・サイズ・評価 [VM-15] |
| VM2-16 | 重複グループ表示に対応 [VM-16][14.3.1 節] |
| VM2-17 | リネーム時はラベル再計算の表示を行う [RC-14] |

### モード切替時の挙動 [VM-20〜VM-23]

| # | 要件 |
|---|---|
| VM2-20 | ファイル選択中にライブラリ→フォルダへ切替 → そのファイルのあるフォルダへ移動 [VM-20] |
| VM2-21 | 同時に左ペインをその階層まで展開しフォーカス移動 [VM-21] |
| VM2-22 | 複数選択なら先頭ファイルのフォルダを対象 [VM-22] |
| VM2-23 | 選択なしなら現在のフォルダに留まる [VM-23] |

### 実装状況（2-9、2026-08）

| 項目 | 状態 |
|---|---|
| VM2-10〜12（フラット表示・現在フォルダ配下・フィルタ）| 済（`LibraryContentModel` ＋ `LibraryServices.files(_:)`）|
| VM2-13（構造を変える操作の無効化）| 済。判定は `FolderContentView.allowsStructuralOperations` **1 箇所**——コンテキストメニュー・メニューバー・キーバインド・D&D がそこを見る |
| VM2-15（並べ替え）| 済。**SQL の `ORDER BY` に委ねる**（下記）|
| VM2-16（重複グループ）| **済**（2026-08、2-18）。`FileQuery.grouping` を渡すと SQL の窓関数が代表 1 行に畳み、`FilePage.duplicateCounts` が件数を返す。バッジは `duplicateCountBadge`。**既定は無効** [DU-01] |
| VM2-17（リネーム時のラベル再計算表示 [RC-14]）| 未（`RC-01〜16` は 2-7 の残り）|
| VM2-20〜23（モード切替）| 済（`WindowState.setDisplayMode`）|
| LV-04（タイトル・シリーズ・巻数・評価の列）| 済。**「種類」「追加日」はライブラリ表示モードで出さない**（下記）|
| IV2-05/07（アイコン表示のタイトル）| 済（`FolderEntry.displayName`）|
| IV2-02（カバー画像を優先）| 済。解決順序は `CoverResolution`（`QooApplication`）**1 箇所**で、右ペインと一覧のセルが同じ関数を通る（下記）|
| SR2-03（タイトル・シリーズも検索対象）| 済。`searchKey` を「ファイル名 stem ＋ タイトル ＋ シリーズ名」の連結に変えた（下記）|
| IF-17/IF-18（ブックフォルダの印と開き方）| 済。印は `BookFolderIndex`、開き方は**ライブラリ単位の設定**［ユーザー判断］（下記）|
| VM2-14（`.qooarchive` を表示しない）| 走査が `.` 始まりを列挙しない [IF-03] ため自動的に満たされる。ファイルアーカイブ自体はフェーズ 3 |

#### 一覧の源が 2 つある（この節で最も大事なところ）

| モード | 一覧の源 | 絞り込み |
|---|---|---|
| フォルダ表示 | **実体**（`contentsOfDirectory`）| `allowedChildNames` で残す名前を絞る [VM-02] |
| ライブラリ表示 | **DB の行**（`FileQuery` → `FilePage`）| SQL の `WHERE` に入っている [VM-12] |

`displayedEntries` がこの 2 つを切り替える。ライブラリ表示モードでは
**メモリ上の絞り込みも並べ替えも行わない**——ページングする一覧 [FI-05] を
あとから並べ替えると「読み込んだ 200 件の中だけが正しい」順序になる。

#### 決めたこと（仕様が答えていなかった 3 点）

| 論点 | 決定 |
|---|---|
| **階層移動**［ユーザー判断］| **しない。** フォルダ行が出ない [VM-10] のに ⌘↑ だけ効くと「降りられないのに上がれる」非対称になるので、`canGoToParent` はライブラリ表示モード中は偽を返す。ブックフォルダのダブルクリックは**関連付けアプリで開く**（中へ降りない）。左ペインのツリー・移動メニューからの移動は従来どおり効き、行き先が同じライブラリの配下ならモードを保つ。**ライブラリの外へ出たら自動的にフォルダ表示モードへ戻す**——シーソーが無効になる場所でライブラリ表示のまま留まると、抜け出す手段が無くなる |
| **件数が多いときの読み込み**［ユーザー判断］| **スクロール末尾で追加読み込み。** 200 件ずつ [FI-05]。合図は行の `.onAppear`（`Table` はセルごとに呼ばれるが、受け側が `isLoadingMore` で弾く）。ステータスバーは読み込んだ件数ではなく **`totalCount`** を出す——でないとスクロールのたびに件数が増えて見える |
| **スコープ**［ユーザー判断］| 重複グループ [VM2-16] は外す（2-18 の前半を前倒しすることになるため）。[IF-17][IF-18] は 2-9 に含める |

#### 「種類」と「追加日」の列を出さない理由 [LV-04]

どちらも `managedFile` が値を持たず、**SQL で並べ替えられない**。
ページングする一覧でメモリ上だけ並べ替えると、読み込んだぶんの中でだけ
正しい順序になる。列そのものを出さないほうが、並べ替えられない列を
見せるより誤解が少ない。`FolderColumn` / `FolderSortComparator.Key` の
`isAvailableInLibraryMode` がこの判定を持つ。

#### 一覧のカバー画像 [IV2-02][IV-02][IV-03]

**解決順序（①ユーザー指定 → ②サイドカー → ③先頭画像）は `CoverResolution`
（`QooApplication`）に 1 つだけ置く。** 読み手は右ペイン（`CoverEditorModel`）と
ライブラリ表示モードのセル（`ThumbnailImage`）の 2 つで、**同じに見えるものに
独立した実装を 2 つ作らない**——このリポジトリが 4 度踏んでいる「片方だけ直して
取り残す」を避ける。

| 決めたこと | 理由 |
|---|---|
| **ライブラリ表示モードのアイコン表示だけ**［ユーザー判断］| 要件が「ライブラリ表示モードでは」と限定している。リスト表示はそもそもサムネイルを出さない（Finder と同じく `FileIconProvider` のアイコン）ので対象外。フォルダ表示モードで②のサイドカー探索を走らせずに済む利点もある |
| ①の材料は `FileRow` が既に持っている | `coverImageRef`/`coverImageSource` が行に入っているので、追加の DB 問い合わせが要らない。複製の**場所**の組み立て（`UserCoverStore.url(forRef:)`）は純粋な計算なので、`LibraryContentModel.Row.userCoverURL` として 200 行ぶん作っても費用が無い |
| **存在確認は描くときにまとめて** | 参照はあるが実体が無い（複製が消えた）とき、黙って②③へ落ちるのが正しい。①の存在確認（ローカル）と②の探索（ネットワークかもしれない）を 1 回の `FileIO.perform` で行う [NV6-02] |
| **サイドカー探索を親フォルダ単位でまとめない** | 解決するのは可視セルのぶんだけで、直後に走るサムネイル生成（アーカイブを開く）のほうが桁で高い。ネットワークでも SMB クライアント自身がディレクトリをキャッシュする [NV-100 の実測] ——**アプリ側にキャッシュを足さない**という既存の判断に揃える |
| `.task(id:)` の鍵に `userCoverURL` を含める | ①の割り当てが変わったら解決し直す。**`nil` になる**（自動へ戻した・⌘Z）ことも変化なので、URL そのものを鍵にするだけで両方向を拾える |

**既知の限界**: 外部で `covers/` に画像を置いた・消したときは、DB の行が変わらない
ので一覧に反映されない（右ペインも同じ性質）。次に一覧を読み直せば出る。

#### タイトル・シリーズ名での検索 [SR2-03][SR-03]

`searchKey` を**ファイル名 stem ＋ タイトル ＋ シリーズ名**の連結に変えた。
組み立ては `ManagedFileSearchKey`（`QooPersistence`）1 箇所。

| 論点 | 決定 |
|---|---|
| **部品の区切り** | **U+001F**（`TextNormalizer.searchKeyPartSeparator`）。空白で繋ぐと `normalize` の空白畳み込みで 1 本の文字列になり、**部品をまたいだ語が当たる**（`作品A` ＋ `巻` で `"A 巻"` に当たる）。U+001F は幅畳み込みの写像域外・`lowercased()` で不変・空白でもないので `normalize` を素通りし、**利用者が検索欄へ打つ手段が無い**ので問い合わせ側の鍵には決して現れない |
| 重複の除去 | 同じ値は 1 つだけ持つ。ファイル名の stem がタイトルと一致するのは自動抽出ではごく普通で、落とさないと索引 `mf_search` が理由もなく倍になる |
| 書き込み口 | 4 つ（insert / `updateInPlace` / `applyParsedFields` / `setFields`）。**`updateInPlace` は stem だけ**を書き、最終形は直後に走る `applyParsedFields` が書く——走査は `ScanEngine` の ②→④ でこの 2 つを必ず対にして呼ぶ。ここで DB のタイトルを読み直すと、走査の内側の輪で 1 ファイルにつき SELECT が 1 本増える。**この対を崩す経路を作らないこと。** |
| **書いた値ではなく行を読み直す** | `applyParsedFields` の `title` は `CASE WHEN titleOrigin = 'manual'` で据え置かれることがある。渡された値から組み立てると、手動編集した行だけ鍵が実態とずれる |
| 既存 DB | **再スキャン任せ**［ユーザー判断］。`searchKey` は再生成可能列なので次の走査で作り直る |
| 著者名 | 含めない（SR-03 が挙げていない）|

#### ブックフォルダの印と開き方 [IF-17][IF-18][BF-08][BF-09]

**フォルダ表示モードだけの話**——ライブラリ表示モードでは既に 1 冊として 1 行に
出ており、印の出番が無い。

| 論点 | 決定 |
|---|---|
| 判定の出どころ | **DB の `isBookFolder`**（`bookFolderChildNames`）。その場で規則 [IF-01] を計算するとフォルダの数だけ列挙が要り、費用が釣り合わない |
| 入口 | **ボリューム経由では引かない** [LF-01 と同じ判断]。評価・ラベル・カバーもそう扱っており、ここだけ例外にすると一貫しない |
| 印を出す対象 | **フォルダにだけ。** 同名のファイルとフォルダは共存できるので、名前だけで照合するとファイルの側にも印が付く（`BookFolderIndex.indicatesBookFolder`）|
| 印を付ける場所 | `displayedEntries`（`reload()` ではない）。印の出どころ（DB）は実体の列挙とは別に更新されるので、一覧を作った時点で焼き付けると印だけが古いまま残る |
| **検索結果には付けない** | `searchResults` は配下から再帰的に集めたものだが、`bookFolderNames` は直下のぶんしか無い。名前だけで突き合わせると深い階層の同名フォルダに誤って印が付く |
| 見た目 | `bookFolderBadge`（`DesignSystem`）**1 実装**をリストとアイコンで共有する。寸法はアイコンの一辺から導く（スライダーで変わるため）|
| IF-18 の既定 | **フォルダを開く**（配下の画像一覧を表示）。これは 2-9 以前からの挙動そのもの |
| IF-18 の設定の粒度 | **ライブラリ単位**［ユーザー判断］。画像フォルダ中心のライブラリとアーカイブ中心のライブラリで期待が違う。`LibrarySettingsPayload.opensBookFolderWithApp`（既定 `false`）|
| 判定の位置 | 「開く」ではリンクを解決する**前に**元の項目で判定する——印は直下の一覧から来ており、解決先が別の場所なら引けない |

#### 一覧を読み直す合図は 3 つ

`.task(id: LibraryLoadKey)` の鍵に混ぜてある。

| 合図 | 何を拾うか |
|---|---|
| モード・場所・並び順・フィルタ・検索 | 利用者の操作 |
| `CommandStack.operationHistory.count` | 評価・ラベル・タイトル・カバーの編集と ⌘Z [RA-01][RL-01][RP-10] |
| **`LibraryServices.contentRevision`** | **走査が DB を書き換えたこと**。`DirectoryObservation`（実体の変更）だけでは足りない——描いているのは DB の行なので、走査が書き終わるまで出す材料が無い。`LibrarySyncCoordinator.onScanFinished` の**最初の利用者** |

#### `FileQuery.mode` は SQL では使われていない

`.folder` / `.libraryFlat` の別は `whereClause` に一度も現れない。フラット
表示は `scope: .folder(path:, recursive: true)` で表現でき、`mode` は
**呼び出し側が scope を決める材料**でしかない。呼び出しの意図を残すため
フィールドは残してあるが、**ここに分岐を足すときは `scope` との二重管理に
ならないか確かめること。**

### リスト表示 [LV-01〜LV-04]

| # | 要件 |
|---|---|
| LV2-01 | ファイル名・更新日・サイズ・種類でソート（Finder に倣う）[LV-01] |
| LV2-02 | 列幅調整、列の表示／非表示 [LV-02] |
| LV2-03 | フォルダを上にまとめるかを環境設定で切替 [LV-03] |
| LV2-04 | ライブラリ表示モードではタイトル・シリーズ・巻数・評価の列も選択可 [LV-04] |

### アイコン表示 [IV-01〜IV-10]

| # | 要件 |
|---|---|
| IV2-01 | フォルダ表示モードでも圧縮ファイル・フォルダの先頭画像サムネイルを表示 [IV-01] |
| IV2-02 | ライブラリ表示モードではカバー画像を優先。順位はユーザー指定 > サイドカー > 先頭画像 [IV-02][IV-03] |
| IV2-03 | アイコンサイズはスライダー。**ウインドウごとに独立** [IV-04][ST-22]（ネイティブタブ移行後は 1 ウインドウ＝1 タブなので実質タブごと） |
| IV2-04 | 「このライブラリの既定にする」で `Library.iconSize` へ書き戻す [IV-04a] |
| IV2-05 | ライブラリ表示モードではファイル名でなくタイトル名を表示 [IV-05] |
| IV2-06 | タイトル名は解析時に DB へ登録。右ペインで編集可 [IV-06] |
| IV2-07 | タイトルがなければファイル名にフォールバック [IV-07] |
| IV2-08 | タイトル編集はファイル名に連動しない [IV-10][RP-10] |
| IV2-09 | サムネイル生成は非同期。生成中はプレースホルダ [IV-08] |
| IV2-10 | キャッシュは App Support 配下。上限と手動クリアを環境設定に [IV-09][CL-05] |
| IV2-11 | サムネイル一括非表示トグルに従う [DS-01][DS-06] |

### 検索 [SR-01〜SR-06]

| # | 要件 |
|---|---|
| SR2-01 | フィルタ全 OFF なら通常の検索 [SR-01] |
| SR2-02 | フィルタ ON ならその結果の中から検索 [SR-02] |
| SR2-03 | 対象はファイル名。ライブラリ表示モードではタイトル名・シリーズ名も含む [SR-03] |
| SR2-04 | インクリメンタル。200ms デバウンス [SR-04] |
| SR2-05 | 検索範囲（現在フォルダのみ／配下すべて）を切替可 [SR-05] |
| SR2-06 | 正規化済み文字列で比較し全角半角・大小の差を吸収 [SR-06][DB-03] |
| SR2-07 | 応答 300ms 以内 [PF-04] |

### 重複グループ表示 [14.3.1 節]

| # | 要件 |
|---|---|
| DG-01 | 代表ファイルのアイコン右上に `×N` バッジ [DU-06] |
| DG-02 | バッジまたはコンテキストメニューで展開 [DU-07] |
| DG-03 | 展開時は比較ビューへ遷移できる [DU-20][DU-29] |
| ~~DG-04~~ | **撤回 [§19.8]**（代表の手動固定 [DU-08]。代表は自動選定に任せ、残す 1 冊は比較ビューで選ぶ）|
| DG-05 | 「重複のみを表示」フィルタ [DU-11] |

> **実装済み（2026-08、2-18）。** バッジ [DG-01] は代表行のアイコン**右上**
> （ブックフォルダの印 [IF-17] は右下なので、1 冊が両方でも重ならない）。
> 展開 [DG-02] とビューへの遷移 [DG-03] は行のコンテキストメニュー
> 「重複を比較…」に統合した——**バッジ自身はクリックを受け取らない**
> （行の選択・ダブルクリックを妨げないため、`bookFolderBadge` と同じ）。
> 「重複のみを表示」[DG-05] は
> 空きスペースのコンテキストメニューのトグルで、**畳んでいるときだけ出す**。
> ウインドウ側は 15章 §15.14。
| DG-06 | グループ選択時の操作は代表のみ。「グループ全体に適用」を明示的に選ぶ [DU-12] |

## 14.4 右ペイン（インスペクタ）

```swift
public struct InspectorPane: View {
    // 単一選択: すべての情報
    // 複数選択: 共通情報のみ + ラベルの一括付与／削除 [RP-02]
}
```

### 詳細情報 [DT-01〜DT-11]

| ID | 項目 | 備考 |
|---|---|---|
| DT-01 | 作成日 | |
| DT-02 | 変更日 | |
| DT-03 | サイズ | |
| DT-04 | 種類 | |
| DT-05 | 含まれるファイル数 | 遅延読み込み、DB キャッシュ [9 章 §9.8] |
| DT-06 | 含まれるサブフォルダ数 | 同上 |
| DT-07 | アプリの関連付け | [AS-01] |
| DT-08 | タイトル | EPUB メタ優先。編集可 [DT-08][IV-06] |
| DT-09 | シリーズ名・巻数 | |
| DT-10 | フルパス | |
| DT-11 | アーカイブ状態 | 保管庫にある場合は元パスも [FA-04]。**済**（2-11、下記）|

### 保管庫 [FA-01][FA-07][DT-11]（2-11、2026-08）

| 何 | どこ |
|---|---|
| 判定（出す向き・オフラインでの見せ方・DB に行が無いとき） | `VaultEditorModel`（`QooApplication`）|
| 描画 | `InspectorVaultSection` |

**`.notInLibrary` では枠ごと出さない。** 評価 [RA-01] は「星を付けられない理由」を
出す価値があるが、保管庫は蔵書だけの機能で、**ここから取り込ませる操作が無い**
——理由を書いても次の手が示せない。

**オフラインでは押せない。** 実ファイルを `.qooarchive` へ動かす操作なので
ボリュームが要る（ラベルの保管庫 [LA-01] が DB だけを触るのとは事情が違う）。
理由も添える——黙って無効にすると「壊れている」のと区別が付かない。

保管庫にあるときは**元の場所** [FA-04][DT-11] を出す。記録が無い（外部で
`.qooarchive` へ入れられた）ものは現在のパスから導く [FA-03]。

### タイトル編集 [RP-10〜RP-12]

| # | 要件 |
|---|---|
| RP2-01 | 編集は DB の `title` のみ。ファイル名は変更しない [RP-10][IV-10] |
| RP2-02 | 手動編集時に `titleOrigin = .manual` を記録し、以後の自動上書きから保護 [RP-11] |
| RP2-03 | 「ファイル名から再取得」で手動編集を破棄し自動抽出値に戻す [RP-12] |

#### 実装状況（2-10 の第 3 段、2026-08）

| 項目 | 状態 |
|---|---|
| RP2-01 タイトルのみ書き換え | 済（`InspectorTitleSection` ＋ `TitleEditorModel`）。ファイル名は触らない [RP-10][IV-10] |
| RP2-02 `titleOrigin = .manual` | 済。**保護そのものは v1 の SQL が既に持っていた**（`applyParsedFields` の `CASE WHEN`）——2-10 で書き手が現れただけ |
| RP2-03 ファイル名から再取得 | 済。**タイトル＋シリーズ名・巻数・著者**まで戻す［ユーザー判断］。ラベルは触らない（下記）|
| DT-08 / DT-09 の表示 | 済。シリーズ名・巻数は**値があるときだけ**出す |
| 実機検証 | 済（2026-08）。使い捨てボリュームで、編集・⌘Z・再取得・ボリューム経由での非表示まで確認。**実機でしか出ない欠陥が 2 件出た**（下記）|

#### 入力欄とモデルの突き合わせ［実機検証で 2 度直した］

`@State` の草案（入力欄）と `@Observable` のモデルを並べて持つので、
**どちらが新しいかを判定する材料**（`lastKnownText`）が要る。

| 事象 | 正しい振る舞い | 間違えた形 |
|---|---|---|
| 打鍵している | 草案を残す | — |
| ⌘Z・再取得・再スキャンで**外から**変わった | **草案を捨ててモデルを写す** | 「編集中は触らない」だけの規則にすると、欄に古い草案が残り**フォーカスを外した瞬間に書き戻されて取り消しが取り消される** |
| 欄に触れて外しただけ | **何も書かない** | 素直に確定させると、値は同じでも `titleOrigin` が `.manual` になり、以後の自動抽出が黙って効かなくなる——**画面上は何も変わらないので気づけない** |

後者の判定は `TitleEditorModel.shouldCommit(draft:lastKnown:)` に切り出してある
（`starsAfterTapping` [RA-02] と同じ理由: この 1 行が「利用者が決めた」の定義）。

#### 決めたこと

| 論点 | 決定 |
|---|---|
| 再取得の範囲 [RP-12] | **タイトル＋シリーズ名・巻数・著者**［ユーザー判断］。**ラベルは触らない**——手で付けたラベルが巻き添えで消えると、取り消すまで気づけない。走査と同じ手順（`FolderLabelResolver.resolve` → `EmbeddedMetadataMerge.apply`）を通すので、「再取得したのに次の再スキャンで違う値になる」が起きない |
| 埋め込みメタデータ | **DB のキャッシュから読む** [EM-07]。実体を開くとネットワーク越しでは数秒待たされるうえ、印が変わっていなければ走査も開かない [SE3-25] ので同じ値しか得られない |
| 未設定のときの入力欄 | **ファイル名を入れない**。入れると、何も打たずに確定しただけで「ファイル名と同じタイトルを手で付けた」ことになり、以後の自動抽出が効かなくなる [RP-11]。ファイル名はプレースホルダとして見せる [IV-07] |
| 空欄にしたとき | **未設定へ戻すが `manual` の印は残す。** 自動抽出値を使わないという意思表示そのものが RP-11 の対象 |
| 「再取得」を出す条件 | **手動編集のときだけ**（`canRederive`）。自動のままで押しても何も変わらない導線を常駐させない（評価の `canApplyToSeries` と同じ判断）|
| 選択の数 | **単一選択のみ。** RP-02 が一括を名指しするのはラベルだけ |

#### Undo

`SetFileFieldsCommand`（**DB 操作の Undo の 3 つ目**）。手動編集と再取得を 1 つの
コマンドで扱い、前後を同じ型（`FileFieldEdit`）で持つ。

**`titleOrigin` まで戻すのが要点。** 再取得は origin を `.manual` から `.auto` へ
落とすので、値だけ戻して印を戻し忘れると**次の再スキャンで手動編集が消える**
——しかも取り消した直後は正しく見えるので気づけない。

### カバー画像 [CV-01〜CV-08]

| # | 要件 |
|---|---|
| CV2-01 | コミックファイルではカバー画像を表示 [CV-01]。サムネイル非表示トグルに従う [DS-06] |
| CV2-02 | 好きな画像に差し替え可 [CV-02]。D&D [CV-03] とダイアログ [CV-04] の両方 |
| CV2-03 | アーカイブ内の別ファイル、アーカイブ外のファイルのいずれも選択可 [CV-05] |
| CV2-04 | 差し替え画像は App Support のキャッシュへ複製。DB は参照のみ [CV-06][CL-05] |
| CV2-05 | 「既定に戻す」で自動抽出へ [CV-07] |
| CV2-06 | 元画像が消えても複製が残るため表示は維持 [CV-08] |

#### 実装状況（2-10 の第 3 段、2026-08）

| 項目 | 状態 |
|---|---|
| CV2-01 | 済（`InspectorCoverSection`）。解決順序 ①②③ を `CoverEditorModel` が持つ |
| CV2-02 | 済。D&D [CV-03]・ダイアログ [CV-04] の両方 |
| CV2-03 | 済。アーカイブ内は `ArchiveCoverPickerDialog`（サムネイル一覧）［ユーザー判断］ |
| CV2-04 | 済。`UserCoverStore`（`usercovers/<libraryUUID>/<uuid>.<ext>`）|
| CV2-05 | 済。**ユーザー指定のときだけ出す** |
| CV2-06 | 済。回帰テストで固定 |
| ②サイドカー [IV-02②] | 済（`SidecarCoverLocator`）［ユーザー判断で今回に含めた］。**`.qooarchive` 配下の探索 [FA-17] も 2-11 で満たした**——`SidecarCoverLocator` は親の `covers` を見るだけなので、保管庫がサイドカーを連れて行けば [FA-14] 自動的に効く（判定を足していない）|
| 実機検証 | 済（2026-08）。**画面に出ている画素の色**で解決順序・差し替え・既定に戻す・⌘Z・起動時の掃除まで確認。**未確認は D&D [CV-03] のみ** |

#### 決めたこと

| 論点 | 決定 |
|---|---|
| サイドカーを DB に書くか | **書かない。** 仕様の疑似コード [9章 §9.6] も②で DB を見ずに実体を探している。保存すると、あとから `covers/` に画像を置いた・消したときに DB が古いまま残り、画面と実体が食い違う。費用は `covers` フォルダ 1 つの列挙なので釣り合う |
| 複製の置き場所 | **自動サムネイルのキャッシュとは別ディレクトリ。** `CoverImageCache` は上限 [IV-09] で削られ「キャッシュを空にする」で全部捨てられる——同じ場所に置くと巻き添えで消え、元画像が既に無ければ CV-08 に反して二度と戻らない |
| 複製の名前 | **保存のたびに新しい UUID。** `FileID` から導出すると、rowid が再利用されたとき他人のカバーを拾う（`FileContentStamp` を導入した理由 [TH-08] と同じ形の事故）。DB の `coverImageRef` に書かれたものだけを引く |
| 差し替え・既定に戻すで複製を消すか | **消さない。** どちらも ⌘Z で戻せるので、その場で消すと**取り消した先に実体が無い**状態を作る |
| いつ捨てるか | **起動時に一度だけ**（`purgeUnreferencedUserCovers`）。`CommandStack` はメモリのみで再起動をまたがないため、そのとき参照されていない複製はもう誰も戻せない（`SecureExtractor.cleanupResidualStaging()` と同じ位置づけ）。ライブラリを消したときはその場で丸ごと捨てる |
| 書く順序 | **先に複製、あとで DB。** 逆にすると、複製の書き込みに失敗したときに「参照はあるが実体が無い」行が残る——表示は既定へ落ちるので、何が起きたのか画面から読み取れない |
| 画像でないものを渡されたら | **書く前に断る** [ER-03]。ダイアログは画像に絞り [CV-04]、D&D は種別を見てから読む |
| 複製が失われていたら | 表示は②③へ落とすが、**「既定に戻す」は出したまま**にする——出さないと迷子の参照を片付けられない |
| 選択の数 | **単一選択のみ。** 複数選択にカバーの節を出さない |

#### Undo

`SetCoverCommand`（**DB 操作の Undo の 4 つ目**）。差し替えと「既定に戻す」を
1 つのコマンドで扱い、前後を同じ型（`CoverAssignment`）で持つ。
`AssignLabelCommand` と同じく `.partial` を返さない（書き戻しが 1 回で済む）。

### ラベル設定 [RL-01〜RL-07]

| # | 要件 |
|---|---|
| RL2-01 | 付与ラベルの追加・削除 [RL-01] |
| RL2-02 | 追加時はまずラベルグループを選び、次に既存ラベルか新規作成かを選ぶ [RL-02] |
| RL2-03 | 1 グループから複数付与可 [RL-03][LB-02] |
| RL2-04 | ピン留めのある一覧からチェックボックスで付与・削除 [RL-04] |
| RL2-05 | 付与中のラベルはピン対象外でも表示 [RL-05] |
| RL2-06 | 自動付与と手動付与を視覚的に区別（自動には小さなインジケータ）[RL-06] |
| RL2-07 | 即座に DB 反映、Undo 可 [RL-07] |
| RL2-08 | アーカイブ済みラベルは追加候補に出さない [LA-03] |
| RL2-09 | ラベルを手動で外したら `.manuallyRemoved` を記録［ユーザー判断で RC-04 を拡張。下記］|

#### 実装状況（2-10 の第 2 段、2026-08）

| 項目 | 状態 |
|---|---|
| RL-01 / RL-03 / RL-04 | 済（`InspectorLabelSection` ＋ `LabelEditorModel`）|
| RL-02 | 済（`AddLabelDialog`。グループ → 既存／新規 の 2 段）|
| RL-05 | 済。付与済みはピン対象外でも必ず出す（`PinnedLabelListing` の `mustInclude`）|
| RL-06 | 済。`LabelChip.isAutomatic`（`sparkles`）。**混在なら出さない**（下記）|
| RL-07 | 済。`AssignLabelCommand`（**DB 操作の Undo の 2 つ目**）|
| 実機検証 | 済（2026-08）。使い捨てボリュームで、付け外し・⌘Z・複数選択の三状態・追加ダイアログ・再スキャン耐性・ボリューム経由での非表示まで確認。詳細は CLAUDE.md「右ペインのラベル設定」節 |
| RP-02 複数選択の一括付与／削除 | 済［ユーザー判断］。三状態のチェックボックス。評価では対象外にしたが、**RP-02 は名指しで要求している** |
| LA-03 | 済。アーカイブ済みは追加候補に出さない。ただし**付与済みなら一覧には出す** [RL-05]（下記）|
| ラベルの改名・統合・保管庫へ送る | 未（2-6 のラベル編集ウインドウ [LE-01〜12] の担当）|
| ラベル固有色の編集 | 未（同上 [LE-10][CO-06]）|
| 中央ペイン・コンテキストメニューからの付与 | 未（経路はインスペクタのみ。評価と同じ）|

#### 決めたこと

| 論点 | 決定 |
|---|---|
| **外したときの印** | **auto でも manual でも `manuallyRemoved` を立てる**［ユーザー判断］。`RC-04` は自動ラベルについて定めているが、**利用者から見れば origin の違いは画面上の小さな印だけ**で、「外したのに再スキャンで戻ってくる」の驚きはどちらでも変わらない。外す操作を「このファイルにこのラベルは不要」という意思表示として扱う。付け直せば印は消える（`assign` が origin を上書きする）|
| 三状態を押したときの向き | **`.some`（一部に付いている）→ 全部に付ける。** 一般的な三状態チェックボックスの慣習であり、しかも安全側——外す側へ倒すと、まだ付けていないファイルではなく**付いていたファイルの側**が変わる。`NSButton.allowsMixedState` の巡回（中間 → オン）とも一致する |
| 自動の印の出し方 [RL-06] | **付いているものが全部 auto のときだけ出す。** 混在しているのに出すと、手で付けたものまで再スキャンで消えるように読めてしまう |
| アーカイブ済み [LA-03][RL-05] | **追加候補には出さないが、付与済みなら一覧には出す。** 出さないと「画面に無いのに付いている」ラベルができ、外す手段が消える——`LA-03` が禁じているのは*追加候補*に出すことであって、既に付いているものを隠すことではない |
| DB に行が無いものが混ざったとき | **件数の差を画面に出す** [RP-02]。「10 件選んだのに 8 件にしか付かなかった」を黙って起こさない |
| 常設の一覧に出すグループ | 候補が 1 件でもあるグループだけ。**追加ダイアログには全グループを出す**——ラベル 0 件のグループも「新しいラベルを作る先」として要る |

#### 一覧の並べ方はラベルフィルタと共有する [RL-04]

`RL-04` が「**ラベルフィルタと同様に**、ピン留めのある一覧表示から…チェック
ボックスで設定する」と名指ししているので、並べ方（ピン留め優先・上位 N 件・
「もっと見る」・検索）は `PinnedLabelListing`（`QooKit`）の純粋関数に置き、
`LabelFilterModel` と `LabelEditorModel` の両方がそれを呼ぶ。両者で違うのは
「必ず含める」の述語だけ——フィルタは*チェック中* [PN-06]、こちらは*付与済み*
[RL-05]。同じに見える一覧に独立した実装を 2 つ作らない [CP-02 と同じ考え方]。

#### 三状態は AppKit のチェックボックスで描く

**SwiftUI の `Toggle` は三状態を表せない**（`isOn: Bool` しか受け取らない）。
`MixedStateCheckbox`（`NSButton` + `allowsMixedState`）を使う——`FixedWidthPopUp`
と同じ、「SwiftUI に無い挙動だけ AppKit の薄いラッパで補う」形。押された後の
見た目は**モデルから描き直す**（ボタン自身の巡回結果は当てにしない）。

#### Undo [RL-07]

`SetRatingCommand`（`RA-06`）に倣った `AssignLabelCommand`。

- 単一選択の付け外しと複数選択の一括 [RP-02] は**同じコマンド**を通す。件数が
  違うだけの同じ操作に独立した経路を 2 つ作らない。
- **変更前の `origin` を 1 件ずつ持つ。** 同じラベルでも、あるファイルでは
  `auto`、別のファイルでは `manual`、また別では `manuallyRemoved`、さらに
  「行が無い」——という 4 通りが混在しうる。一律に消すと ⌘Z が元と違う状態を
  作る（しかも「戻した」ように見える）。
- 書き戻しは **1 トランザクション**なので `.partial` を返さない（`SetRatingCommand`
  が返しうるのは、星の値ごとに分けて書くため）。

### 評価 [RA-01〜RA-08]

星 1〜5、同じ星の再クリックで解除 [RA-01][RA-02]。「このシリーズ全巻に適用」を提供し、対象件数を事前表示する [RA-04][RA-05]。`seriesName` 未設定時は無効 [RA-07]。

#### 実装状況（2-10 の第 1 段、2026-08）

| 項目 | 状態 |
|---|---|
| RA-01 / RA-02 | 済（`RatingStars` ＋ `RatingEditorModel.starsAfterTapping`）|
| RA-03 | 済（実体は `managedFile.rating` のみ。シリーズ用の列を持たない）|
| RA-04 / RA-05 / RA-06 | 済。件数は**ボタンの文言**に出す（下記）|
| RA-07 | 済。押せないボタンではなく「シリーズ名がありません」と書く |
| RA-08 | 済。ブックフォルダも `managedFile` の 1 行なので自動的に対象 |
| 複数選択の一括評価 | **対象外**［ユーザー判断］。仕様が要求しているのは単一選択と RA-04 だけで、誤操作で大量に書き換わる形を先に作らない。足すときは同じ `SetRatingCommand` に複数 ID を渡せばよい |
| 中央ペイン・コンテキストメニューからの評価 | 未（経路はインスペクタのみ）|
| 「このシリーズ全巻に適用」の出し方 | **効果があるときだけ出す**（`Subject.canApplyToSeries`）。星が付いていれば「適用」、付いていなくてもシリーズのどこかに星があれば「解除」に意味がある。どちらでもなければ出さない——未評価のまま「全巻の評価を解除」を常駐させると、何もしていない状態の一番目立つ位置に、押すと他の巻の星が消える導線が座る［実機検証で発見］ |
| 未選択（現在のフォルダを描いているとき）| 評価欄を**出さない**［設計判断］。そこで描いているのは「いま居る場所」であって選んだ項目ではない——立っている場所に星を付ける操作は意味を成さないし、ライブラリを歩くあいだずっと「評価できません」が常駐することになる。ブックフォルダ [RA-08] は 1 冊として親の一覧に並ぶのでそちらで選べる |

#### 構成

```
InspectorRatingSection（描くだけ）
   └ RatingEditorModel（QooApplication・判定と読み込み）
        └ LibraryServices.fileRow(at:in:) / setRating / filesInSameSeries
             └ ManagedFileRepository
```

`LabelFilterModel` と同じ分け方で、判定を `QooApplication` に置く——アプリターゲットのコードは `swift test` から触れないため、RA-02・RA-07・「DB に行が無いとき」を自動テストで固定できなくなる。

#### 決めたこと

| 論点 | 決定 |
|---|---|
| DB 行の引き当て | **`FileIdentity`（`library.volumeUUID` ＋ inode）で引く** [ID-02]。`relativePath` の照合にすると、綴り（NFD/NFC・大小文字）が 1 文字食い違った瞬間に黙って空振りする経路を新たに作ることになる（差分スキャンで実際に踏んだ形、10章 §10.6）|
| ライブラリの外 | 評価欄ごと出さない [LF-01 と同じ判断]。ボリューム経由で見ているだけのフォルダに評価の枠が出ると、何に対する評価なのか読み取れない |
| ライブラリの中だが DB に行が無い | **無効の星 ＋ 理由**［ユーザー判断］。黙って消すと「星を付けられない」のか「壊れている」のか区別が付かない |
| オフラインのライブラリ | **評価できる。** DB しか触らないので、実体の有無に関わらない（「ライブラリ機能を無効にする」で同じ判断をしている）|
| RA-05 の見せ方 | **確認ダイアログではなくボタンの文言**［設計判断］。RA-05 が求めているのは「対象件数を実行前に表示する」ことで、ダイアログとは書いていない。⌘Z で戻せる操作に毎回 1 枚挟むと、本当に見てほしいときの 1 枚まで読み飛ばされる（走査結果の提示で同じ判断をしている）|
| RA-04 の対象 | `.trashed` **だけ**を除く。孤立・オフラインは含める——外付けを抜いているあいだに全巻へ適用したら挿し直したときだけ 1 冊違う、という形のほうが驚きが大きい [ID-08] |
| 照合の鍵 | `seriesName` ではなく正規化済みの `seriesKey`（索引 `mf_lib_series`）。呼び出し側にシリーズ名を渡させると正規化の責務が移り、表記ゆれのある巻が**黙って対象から漏れる**——漏れたことは件数を数えても分からない |

#### Undo [RA-06]

**このコードベースで最初の「DB 操作の Undo」**（ロードマップ上は 2-15）。要件が「1 つの Undo 単位」と名指ししている以上、後から被せる形にはできないため評価だけ先に作った。

- 単発の星付けと全巻適用は**同じ `SetRatingCommand`** を通す。件数が違うだけの同じ操作に独立した経路を 2 つ作らない。
- **変更前の値は 1 件ずつ持つ。** 全巻の元の評価はばらばらでありうるので、一律に 0 へ戻すと ⌘Z が元の評価を破壊する——しかも「戻した」ように見えるので気づきにくい。
- 書き戻しは変更前の値でまとめる（星は 6 通りなので最大 6 回の書き込み）。1 つのトランザクションではないため、途中で失敗したら `.partial` でどこまで戻せたかを伝える [ER-13]。
- ⌘Z / ⇧⌘Z はこの View を通らずに DB を書き換えるので、インスペクタの `.task(id:)` は **`CommandStack.operationHistory.count`** も鍵に含める（`record` が run / undo / redo のいずれでも 1 件増やす）。

#### 消えないことの担保

| 経路 | 担保 |
|---|---|
| 再スキャン | `updateInPlace` の SQL に `rating` 列が無い（回帰テスト `survivesRescan`、変異検証済み）|
| JSON バックアップ | `rating` は `regenerableColumns` に**無い**＝再生成不可能データとして書き出される [MG-22] |

