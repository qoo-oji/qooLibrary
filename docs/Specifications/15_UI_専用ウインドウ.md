# 15. UI — 専用ウインドウ

対応要件: LS-01〜LS-03 / LE-01〜LE-12 / LAW-01〜LAW-03 / FAW-01〜FAW-05 / UR-01〜UR-06 / OR-01〜OR-05 / PW-01〜PW-20 / CW-01〜CW-45 / NW-01〜NW-08 / OB-01〜OB-10 / HP-01〜HP-09

すべて UI 共通要件（13 章）に従う。ペイン構成は `ThreePaneWindow` / `TwoPaneWindow` を使う [UI-02][UI-03]。

```swift
public enum WindowRoute: Hashable, Sendable {
    case librarySettings(libraryID: UUID?)          // 15.1
    case labelGroupEditor(libraryID: UUID?)         // 15.2
    case labelVault(libraryID: UUID?)               // 15.3
    case fileVault(libraryID: UUID?)                // 15.4
    case temporarySettings(folderID: UUID?)         // 15.5
    case unresolvedFiles(libraryID: UUID?)          // 15.6
    case orphanedFiles(libraryID: UUID?)            // 15.7
    case moveResultAndPending(sessionID: UUID)      // 15.8
    case convertRename(scope: ConvertScope)         // 15.9
    case preferences(tab: PreferencesTab)           // 15.10
    case notificationHistory                        // 15.11
    case setupWizard(step: SetupStep?)              // 15.12
    case operationHistory                           // 18.1
}
```

---

## 15.1 ライブラリの設定ウインドウ

3 ペイン。左＝ライブラリ一覧（登録・削除も）／中央＝設定項目グループ／右＝詳細。

| 中央ペインの項目 | 右ペインの内容 |
|---|---|
| 基本設定 | 表示名、ライブラリタイプ、大文字小文字の区別 [N-04]、既定アイコンサイズ、サムネイル既定 [DS-04] |
| 対象拡張子 | 対象拡張子、画像拡張子 [IF-02] |
| 区切り文字 | ペア型・セパレータ型の追加削除 [DL-02][DL-10][DL-11]。追加時の影響プレビュー [DL-06]。セパレータ型には注意書き [DL-13] |
| 保護文字列 | 一覧（**正規表現を行内で編集** [PT-11 改訂]）、位置条件、サンプルへの効果プレビュー [PT-01][PT-05][PT-10]。テンポラリとの相互コピー [PT-08]。不備・危険な形は行ごとのバッジで知らせる |
| フォルダ階層割り当て | 階層 → 単一グループ or フォーマット [AL-01][AL-02] |
| ファイル名フォーマット | 一覧（D&D 並べ替え [FF-04]、有効／無効 [FF-05]）+ 編集支援 [9.5 節] |
| 巻数フォーマット | 一覧（`EditableListChrome` の枠内。並べ替え [SE-05]、**正規表現を行内で編集**、種別ピッカー〈巻数／区切り〉）+ サンプルプレビュー [SE-22]。不備・危険な形は行ごとのバッジ |
| **埋め込みメタデータ** | 読み取りの有無 [EM-06]、`ComicInfo.xml` の巻数をどちらから取るか（確認する／`Number`／`Volume`）[EM-30]、判断待ちの件数と確認ダイアログへの導線 [EM-35] |
| 重複表示 | 有効／無効と判定キー [DU-01][DU-02] |
| 表示設定 | 既定のソート順・表示形式 |
| テンプレート | 適用・変更 [LT-03][LT-04]、**差分ビュー** [LT-13] |

| # | 要件 |
|---|---|
| LS2-01 | フォーマット設定は中央で選択し右で編集 [LS-01] |
| LS2-02 | 設定変更時に既存ファイルへの再適用を促す確認 [LS-02][AT-04] |
| LS2-03 | テンプレートの適用・変更もここ [LS-03] |

### 正規表現の不備をどこで見せるか [2026-08]

巻数フォーマットと保護文字列は正規表現で書く（05章 §5.1、04章 §4.6）。

| いつ | 何を見せるか | 実装 |
|---|---|---|
| 入力のたび | 行ごとのバッジ（赤＝エラー／橙＝警告）。読めない正規表現、キャプチャグループの過不足、量指定子の付いたグループ、全角の文字 | `RegexSafety.staticFindings` |
| 保存時 | 「入力によっては極端に遅くなります」の警告 | `LibrarySettingsDraft.measuredIssues(samples:)` |

**実測を入力のたびに走らせてはならない。** `validate()` は描画のたびに
（セクションの数だけ）呼ばれるので、実測を混ぜると**危険な正規表現を直している
最中にこそ画面が重くなる**。だから静的検査と実測を別の入口に分けてある。

**エラーで保存を止めるのは「読めない正規表現」と「キャプチャグループの過不足」
だけ。** 遅くなりうる形は警告にとどめる——実行時に時間の上限で必ず打ち切られる
ので、表現力を削る理由が無い（05章 §5.6）。

### 編集の経路と不変条件 [実装済み]

**テンプレートは登録時に一度写されるだけの雛形** [LT-03] なので、写された設定を
実際に調整できる経路がここになる。実装は次の 3 つの型に集約してある:

| 型 | 役割 |
|---|---|
| `LibrarySettingsDraft`（`QooKit`）| 編集用の設定。**ソース文字列のまま**持ち、無効にしてあるフォーマットも落とさない。`validate()` が純粋関数として不備を全件返す |
| `LibraryRepository.settingsDraft(libraryID:)` | 草案を読む |
| `LibraryRepository.updateSettings(_:libraryID:)` | 草案を書く |

**`LibrarySettingsSnapshot`（パーサ用）を編集に使ってはならない。** あちらは
コンパイル済みで `isEnabled = 0` の行を落とすため、編集に使うと ①ソース文字列が
失われて直せない ②無効にしていたフォーマットが保存で消える。

`updateSettings` が守る不変条件は 3 つ:

1. **検証を通らない草案は書かない。** `settingsSnapshot` は壊れたフォーマットを
   黙って落とす造りで、「保存時に検証済み」を前提にしている。
2. **`settingsRevision` を必ず上げる** [VT-02]。呼び出し側に任せず、この 1 箇所で
   行う——上げ忘れるとパーサが古いコンパイル結果を使い続ける。
3. **ラベルグループは作り直さず差分適用する。** ラベルは `labelGroup` へ連鎖削除で
   紐づくため、消して入れ直すと蓄積したラベルと紐づけが全部消える。
   （フォーマット・階層割り当て・保護文字列は付随データを持たないので入れ替えてよい。）

**ライブラリタイプ名の変更は型を分岐させる** [LT-05]。`libraryType` の行は同じ
プリセットから登録した複数のライブラリで共有されるので、そのまま書き換えると
他のライブラリの `@librarytype` の照合値まで変わる。プリセット由来か他から参照
されている場合は、このライブラリ専用の非プリセット型へ複製してから書き換える。

**プレビューは保存を経由しない** [FF-06][HP-05]。`LibrarySettingsDraft
.compiledSnapshot()` が編集中の値をそのままパーサへ渡せる形に組み立てる（壊れた
フォーマットは落とす——編集の途中で壊れているのは普通の状態で、そこで例外を投げると
プレビューが一切出せなくなる。「試せる」と「保存できる」は別の関門）。

**未実装**: 左ペインからの登録・削除、テンプレートの適用・変更 [LS-03]、
差分ビュー [LT-13]、ラベルグループの色編集 [LE-10]。

### フォーマット編集支援 [9.5 節]

右ペインの構成:

```
┌─────────────────────────────────────────────┐
│ フォーマット入力欄（エラー位置に下線）[HP-04]         │
├──────────────────┬──────────────────────────┤
│ 予約語パレット [HP-01]│ 区切り文字パレット [HP-03]    │
│ @labelgroup1（著者）│ [ ] ( ) 【 】               │
│ @title  @series … │ - （セパレータ）              │
├──────────────────┴──────────────────────────┤
│ サンプル入力 + フィールド分解プレビュー [HP-05][HP-06] │
├─────────────────────────────────────────────┤
│ 詳しい説明（README の該当セクションへ）[HP-07]        │
└─────────────────────────────────────────────┘
```

パレットの各語には 1 行説明を添え、ラベルグループに紐づく語には現在の名称を併記する [HP-02]。網羅的な文法説明はアプリ内に持たない [HP-08]。

## 15.2 ラベルグループの編集ウインドウ

3 ペイン。左＝ライブラリ一覧／中央＝ラベルグループ一覧（リネーム、セマンティック予約語の紐づけ）／右＝ラベル一覧。

| # | 要件 |
|---|---|
| LE2-01 | セマンティック予約語（`@series` / `@author`）の紐づけはドロップダウンで選ぶ [LE-01][RW-12] |
| LE2-02 | 1 予約語 → 複数グループ、1 グループ → 複数予約語をいずれも UI で禁止する [LE-02][RW-14] |
| LE2-03 | 各ラベル右に紐づけファイル数バッジ [LE-03] |
| LE2-04 | 0 件のラベルは赤字 [LE-04][RC-07] |
| LE2-05 | 保管庫へ移されたファイルもバッジに影響しない（紐づけ維持のため）[LE-05][FA-05] |
| LE2-06 | アーカイブ済みラベルも表示。グレー文字 + 保管庫バッジ [LE-06] |
| LE2-07 | 編集・追加・削除・保管庫へ移す／戻すが可能 [LE-07][LE-09] |
| LE2-08 | 削除時は紐づけも削除 [LE-08][LB-05] |
| LE2-09 | グループ色・ラベル色の設定 [LE-10][CO-04][CO-06] |
| LE2-10 | マージ機能 [LE-11][LB-07] |
| LE2-11 | 名前順・件数順のソートとインクリメンタル検索 [LE-12] |
| LE2-12 | グループ数が 10 を超えてもスクロールで対応 [MT-14] |

### 実装状況（2-6、2026-08）

| 項目 | 状態 |
|---|---|
| ウインドウ | 済（`LabelGroupEditorWindow`。`Window(id: "labelEditor")`）|
| LE-01 / LE-02（予約語の紐づけ）| 済。**ライブラリ設定ウインドウと同じ実装を共有する**（下記）|
| LE-03（件数バッジ）| 済。`LabelChip(count:)`。**保管庫のファイルも数える** [LE-05]（下記）|
| LE-04（0 件は赤字）| 済。`Row.isOrphaned` → `DangerText` |
| LE-05（保管庫はバッジに影響しない）| 済。`LabelSummary.fileCountIncludingArchived`（下記）|
| LE-06（保管庫のラベルも表示）| 済。グレー＋ `archivebox.fill` バッジ |
| LE-07（編集・追加・削除・保管庫へ／戻す）| 済。**削除 API を新設**（`deleteLabels`）|
| LE-08（削除で紐づけも削除）| 済。`fileLabel` の `ON DELETE CASCADE` が保証する |
| LE-09（アーカイブ属性の ON/OFF）| 済。複数選択でまとめて扱える |
| LE-10（グループ色・ラベル色）| 済。`LabelColorWell`。**設定ウインドウにも同時に足した**（従来どちらにも無かった）|
| LE-11（マージ）| 済。統合先はメニューから選ぶ。改名の衝突からも誘導する |
| LE-12（ソートと検索）| 済。名前順（自然順）／件数順、`NameFilter` による幅・大小文字非依存の検索 |
| Undo | 済。5 つのコマンド（下記）|
| 実機検証 | 済（2026-08）。使い捨てボリュームで全項目——3 ペインの描画・件数バッジ・件数順・色編集（ラベル 1 色／グループ 2 色）・削除と統合の確認ダイアログ・**⌘Z が同じ行 ID へ戻すこと**・保管庫の印・**オフラインでの操作**・改名の衝突 [LE-11]・検索（全角で半角に当たること）・新規作成と 0 件の赤字。実機で 3 件の欠陥を見つけて直した。詳細は CLAUDE.md「ラベル編集ウインドウ」節 |

### 中央ペインは設定ウインドウと同じ実装を共有する［ユーザー判断］

本節は 3 ペインを定めるが、中央（グループの改名・予約語紐づけ）の中身は
**2-12 のライブラリ設定ウインドウに既にある**。同じ編集を 2 箇所に実装すると
片方だけ直して取り残すので、`LibraryLabelGroupsSettingsView` に
`selection` と `showsHeader` を足して両方から使う。違うのはその 2 つだけ。

**保存の意味が 1 つのウインドウの中で 2 通りになる**（承知のうえ）。中央は
草案を編集して**保存**する形（`settingsRevision` が上がり再スキャンを促す）、
右は**即座に DB へ書いて ⌘Z で戻す**形。混ざると分かりにくいので、中央には
保存ボタンと未保存の印を必ず出す。

### 件数は 2 つある [LE-03][LE-05][FA-05]

**要件が意図的に食い違う唯一の点。** ファイル保管庫へ移したファイルは
ラベルフィルタの結果から外れる [FA-05] が、**このウインドウのバッジには
影響しない** [LE-05]——紐づけは維持されているので、保管庫へ入れただけで
ラベルが「0 件」＝赤字＝消してよさそう [LE-04][RC-07] に見えるのは誤り。

したがって `LabelSummary` は件数を 2 つ持つ:

| プロパティ | 誰が見るか | 保管庫のファイル |
|---|---|---|
| `fileCount`（非正規化列）| ラベルフィルタ [LF-11] | 数えない [FA-05] |
| `fileCountIncludingArchived`（問い合わせのたびに数える）| このウインドウ [LE-03] | 数える [LE-05] |

> **[CO-05 の警告は有効な色では発火しない]**［実測］。全 sRGB 色を走査した
> 最悪値は 4.583:1（`#5D60FF`）で、黒と白の良いほうを選ぶ限りどんな色でも
> 4.5:1 を上回る——「いずれも 4.5:1 未満なら警告」は数学的にほぼ到達しない。
> 実装は残してある: `contrast(of:)` は解釈できない hex に 0 を返すので、
> **DB に壊れた色が入ったときだけ**印が出る。

**並べ替え（件数順）もバッジ側の件数で決める**——一覧に出ている数字と並び順が
食い違うと、何を基準に並んでいるのか読めなくなる。

後者に専用の非正規化列を作らないのは、保管庫が稀な操作だから
——列を増やすと `adjustCount` の呼び出し全てに二重の更新が要り、ずれる箇所が倍になる。

**ファイル保管庫（2-11）が入るまで 2 つの値は必ず一致する**ので、この区別は
自動テストでしか観測できない（`managedFile.isArchived` を直に書いて確かめている）。

### Undo [LE-07][LE-11]

5 つのコマンド（`LabelEditCommands.swift`）。**2 つの形がある。**

| 形 | コマンド | 戻し方 |
|---|---|---|
| 列を書き換えるだけ | `RenameLabelCommand` / `SetLabelColorCommand` / `SetLabelArchivedCommand` / `SetLabelPinnedCommand` | 変更前の値を持ち、書き戻す。行 ID は変わらない |
| 行が消える | `DeleteLabelsCommand` / `MergeLabelsCommand` | `LabelSnapshot` を控え、**同じ ID で**作り直す |

**元の ID へ戻せるのは `label.id` が AUTOINCREMENT だから**［実測］。削除された
ID は二度と再利用されないので空いたまま残り、明示指定した `INSERT` で取り戻せる
（`sqlite_sequence` も巻き戻らない）。別 ID で作り直すと、ラベルフィルタで
チェック中だった選択やウインドウ状態復元 [ST-26] が黙って外れる——`label` にだけ
AUTOINCREMENT を付けた T-03 の決定③が、想定とは別の形でここでも効いている。

- **写しは `init` ではなく `execute()` の直前に取る。** 組み立てから実行までの間に
  他所（スキャン・右ペイン）が紐づけを変えうるので、作った時点の写しで戻すと
  ⌘Z が現在と違う状態へ戻す。
- **統合は統合元と統合先の両方を控える。** 統合先は `origin` が書き換わり、
  統合元にしか無かった紐づけも移ってくるので、統合元だけ戻しても統合先が
  統合後のまま残る。
- **`SetLabelArchivedCommand` は変更前の状態を 1 件ずつ持つ。** 複数選択では
  もともと保管庫にあったものと無かったものが混ざりうる（`SetRatingCommand` /
  `AssignLabelCommand` と同じ判断）。
- **ラベルの作成は Undo に載せない**［設計判断］。紐づけを持たない空のラベルが
  増えるだけで、消したければ削除（Undo 可）で片付く。載せると「作る → ⌘Z で
  消える → ⇧⌘Z で**別 ID で**復活」という、ID の安定性を崩す経路を新しく作る。

### 統合したときの `origin` [LB-07]［ユーザー判断］

同じファイルに両方が付いていたら **manual > auto > manuallyRemoved** で決める
（`LabelOrigin.merging`）。素朴に `UPDATE OR IGNORE` で移すと移動先の値が
無条件に残り、統合元が `manual`・統合先が `manuallyRemoved` のファイルで
**手動付与が黙って消える**——しかも件数を見ても気づけない。

逆に「統合したのだから一律 `manual`」にもしない。自動付与されたラベルを
まとめただけで再スキャン耐性が変わり [RC-04]、`manuallyRemoved` の印まで消えて
**外したはずのラベルが復活する**。

### 改名の衝突は理由を添えて断る [LE-11][LB-01]

同じグループに正規化後が同じ名前があれば `LabelEditError.nameAlreadyExists`
（衝突相手の ID 付き）。素の UNIQUE 制約違反を投げると
「UNIQUE constraint failed: label.labelGroupId, label.normalizedName」が
そのまま画面に出るうえ、呼び出し側が「代わりに統合しますか」を出せない。

**自分自身は衝突ではない。** 大小文字や全角英数だけを直す改名
（`studio abc` → `STUDIO ABC`）は正規化名が変わらないので、除外しないと
表記を整える操作が一切できなくなる。

### 下端の操作群は高さを増やさない

**失敗の理由が 1 行増えただけで、ウインドウの中身全体がはみ出す**［実機で発見］
——右ペインのフッターが伸びると一覧が譲らず、**両ペインの上端と下端が消えた**。
「固定サイズのウインドウでは、可変高さの領域を 2 つ持たない」（有効化ウインドウ
§15.1.1 で 3 度直したのと同じ形）。

理由は上限付きのスクロール領域（44pt）へ閉じ込め、フッターに
`.layoutPriority(1)`、一覧に `minHeight: 60` を与えて**一覧の側を縮む側**にする。

### 0 件の赤字はチップに渡す [LE-04][CO-05]

`LabelChip` は**背景から文字色を計算する** [CO-05] ので、外側から
`.foregroundStyle` を掛けても上書きされる［実機で発見］。赤字にするかどうかは
`isOrphaned` としてチップに渡す。

**既定は偽**——ラベルフィルタと右ペインで求められているのは「絞り込む／
付け外しする」ことで、「消してよさそう」の判断はこのウインドウの仕事 [LE-04]。

### 入口 [CP-02]

3 箇所すべてが `LabelEditorNavigation.open(libraryID:openWindow:)` を通る。

| 入口 | 備考 |
|---|---|
| フォルダツリーの登録ルート行 | **2 つある行型の両方**（通常の `FolderTreeContextMenu` と縮退した `DegradedRegisteredFolderRow`）。配線は別々なので項目は 2 か所に要る |
| ラベルフィルタのラベル行（右クリック）| 改名・統合・保管庫はフィルタの狭い行では扱えないので、ここへ送る |
| 右ペインのラベル行（右クリック）| 同上 |

逆向き——このウインドウから**保管庫の整理ウインドウ** [§15.3] へは、ツールバーの
「ラベルの保管庫…」で行く。保管庫へ送る手段がここにある以上、送った先を確かめる
手段もここに要る。

**オンラインを要らない** [LibraryMenuVisibility]。DB しか触らないので、外付けが
無い間に表記ゆれを片付けられる——無効化をオンライン条件で囲って「外付けを失うと
二度と片付けられない」欠陥を作った前例がある。

## 15.3 ラベル保管庫の整理ウインドウ

2 ペイン。左＝ライブラリ一覧（アーカイブ済みラベルがなければグレーアウト）／右＝アーカイブ済みラベル（グループごとに整理）。

| # | 要件 |
|---|---|
| LAW2-01 | 各ラベル右に「保管庫から戻す」ボタン [LAW-01] |
| LAW2-02 | 削除はコンテキストメニューからのみ [LAW-02] |
| LAW2-03 | 複数選択して一括で戻せる [LAW-03] |

### 実装状況（2-13 のラベル保管庫、2026-08）

| 実装 | 置き場所 |
|---|---|
| 判定（区画の組み立て・並べ替え・検索・既定のライブラリ） | `LabelVaultModel`（`QooApplication`）|
| ウインドウ | `LabelVaultWindow`（`qooLibraryApp/LabelEditor/`）|
| 件数の集計 | `LabelRepository.archivedLabelCounts()`（07章 §7.5）|
| 戻す／削除 | **既存の `SetLabelArchivedCommand` / `DeleteLabelsCommand`**。新規コマンドは書いていない |

**実機検証済み**（2026-08、使い捨てボリューム）。3 つの入口・区画ごとの整理・
区画内での並べ替え・全角での検索・2 つの空状態・LAW-01〜03・⌘Z が同じ行 ID へ
戻すこと・オフラインでの読み書きまで確認した。欠陥は出ていない。経緯と
限界は CLAUDE.md「ラベル保管庫の整理ウインドウ」節。

**ファイル保管庫（§15.4、FAW-01〜05）は 2-11 のまま。** `managedFile.isArchived`
の器はあるが、ファイルを保管庫へ送る導線がまだ無い。

### なぜラベル編集ウインドウとは別に要るのか

ラベル編集ウインドウ [§15.2] も保管庫の中身を見せる [LE-06][LA-06] が、あちらは
**グループを 1 つ選んでから**でないと見えない。「どこかへ送ったが、どのグループ
だったか思い出せない」を解けるのはこちらだけ——**横断して一覧すること**が
この画面の存在理由である。

### 触れるのは 3 つだけ［ユーザー判断］

戻す [LAW-01]・一括で戻す [LAW-03]・削除（コンテキストメニューのみ）[LAW-02]。
改名・統合・色・ピンは §15.2 の仕事で、**そちらへ飛ぶ導線だけを置く**
（行のコンテキストメニューと下端の「ラベルを編集…」）。LA-06 は「操作可能」と
広く書くが、LAW が挙げるのは 3 つだけ——**同じ編集を 2 箇所に実装しない。**

**削除にボタンを置かない**のが LAW-02 の趣旨。押しやすい場所に置かないという
意味なので、下端にも出さない。確認は §15.2 と**同じ `DeleteLabelsDialog` を共有**
する（同じ破壊力の操作に確認を 2 種類作らない）。

### 一覧の形

§15.3 が「グループごとに整理」と定めるので**セクション**にする。区画の順序は
ラベルフィルタと同じ `displayOrder`（ライブラリ単位の永続設定 [ST-23]）。

**並べ替え・検索を持つ**［ユーザー判断］。並べ替えは**区画の中へ効かせる**
——グループをまたいで混ぜると、この節が定める整理が消える。並べ替え・検索の
実装は `LabelGroupEditorModel.rows(from:sortedBy:matching:)` を共有する
（検索は `NameFilter` なので**全角で打っても半角のラベルに当たる**）。

| 見せ方 | 規則 |
|---|---|
| 区画 | **アーカイブ済みが 0 件のグループは出さない。** 検索で 0 件になった区画も消す——見出しだけ残ると何のための区画か読めない |
| 行 | `LabelRowView` を §15.2 と共有（保管庫のバッジ・0 件の赤字・グループ色の継承がそのまま効く）|
| 件数バッジ | `fileCountIncludingArchived`（保管庫のファイルも数える [LE-05]）。**保管庫へ入れても紐づけは維持される** [LA-04] ので、0 件＝赤字にはならない |
| 左ペイン | 保管庫が空のライブラリは**グレーアウトするが選べる**——「空だった」を確かめに来ることがあるため、押せない項目にしない |
| 空状態 | 「保管庫は空です」と「一致するラベルがありません」を**分ける**。次の一手が違う（前者は閉じる、後者は検索語を消す）|

### 既定で選ぶライブラリ

**入口が指定したものが最優先**——その登録の保管庫を見に来たのだから、空でも
そのライブラリを見せる（「空だった」という答えも答えである）。指定が無ければ
**アーカイブ済みラベルを持つ最初のライブラリ**［設計判断］——素直に先頭を選ぶと
保管庫が空のライブラリに着地して行き止まりになる（§15.2 が「ラベルを持つ最初の
グループ」を選ぶのと同じ理由）。どこにも無ければ先頭へ落とす。

### Undo

新しいコマンドは書かない。戻す＝`SetLabelArchivedCommand`（変更前の状態を
1 件ずつ持つ）、削除＝`DeleteLabelsCommand`（`LabelSnapshot` を控え、
AUTOINCREMENT のおかげで**同じ行 ID へ復元できる**［実測］）。

**一括の戻しは 1 つの Undo 単位** [UD-04]。1 件ずつのコマンドに割ると、
⌘Z を選択件数ぶん押すことになる（回帰テストで固定・変異検証済み）。

⌘Z / ⇧⌘Z は View を通らずに DB を変えるので、`CommandStack.operationHistory.count`
を `.onChange` の鍵に含める——含めないと取り消した結果が画面に出ない。

### 入口 [CP-02]

3 箇所すべてが `LabelVaultNavigation.open(libraryID:openWindow:)` を通る。

| 入口 | 備考 |
|---|---|
| フォルダツリーの登録ルート行 | **2 つある行型の両方**（通常の `FolderTreeContextMenu` と縮退した `DegradedRegisteredFolderRow`）。配線は別々なので項目は 2 か所に要る |
| ラベル編集ウインドウのツールバー | **保管庫へ送る手段がそこにある以上、送った先を見に行く手段もそこに要る**——ツリーへ戻らないと開けないのでは、送った直後に確かめられない |

**オンラインを要らない** [LibraryMenuVisibility]。DB のアーカイブ属性を書き換える
だけなので、外付けが無い間にこそ片付けたいことがある。

> **`Item` に case を足してもコンパイラは配線漏れを教えてくれない。**
> 消費側は `Set.contains` だけで、網羅的な `switch` がどこにも無いため
> ——項目を足すときは**必ず 2 つの行型の両方を目で確かめること。**
> CLAUDE.md が 3 度踏んだと記録している形は、いまも機械的には防げていない。

## 15.4 ファイル保管庫の整理ウインドウ

2 ペイン。左＝ライブラリ一覧／右＝アーカイブ済みファイル（元フォルダごとに整理）。

| # | 要件 |
|---|---|
| FAW2-01 | フォルダごと移した場合もファイル単位で扱う [FAW-01][FDA-05] |
| FAW2-02 | ファイル名の右に「保管庫から戻す」[FAW-02] |
| FAW2-03 | 削除はコンテキストメニューのみ [FAW-03] |
| FAW2-04 | 複数選択して一括で戻せる [FAW-04] |
| FAW2-05 | アーカイブ日時を表示、それによるソート可 [FAW-05] |

### 実装状況（2-11、2026-08）

| 何 | どこ |
|---|---|
| 判定（区画の組み立て・並べ替え・検索・既定のライブラリ・操作の可否） | `FileVaultModel`（`QooApplication`）|
| ウインドウ | `FileVaultWindow`（`qooLibraryApp/FileVault/`）|
| 削除の確認 | `DeleteVaultFilesDialog`（同上）|
| 件数の集計 | `ManagedFileRepository.archivedFileCounts()`（07章 §7.5）|
| 戻す | **既存の `SetFileArchivedCommand`**（08章 §8.8）——新しいコマンドを書いていない |
| 削除 | `TrashCommand` ＋ `DeleteOrphanedFilesCommand` を `CompositeCommand` で 1 単位に |

### ラベル保管庫（§15.3）との違い — **オンラインの要否**

| | ラベル保管庫 | ファイル保管庫 |
|---|---|---|
| 一覧 | オフラインでも出せる | **オフラインでも出せる**（`isArchived` は DB の属性）|
| 戻す・削除 | オフラインでも書ける | **ボリュームが要る**（実ファイルを動かす）|

一覧は出し続け、操作だけを無効にする（`FileVaultModel.canModify`）
——「何がしまってあるか」は DB だけで答えられるし、そこで行き止まりにする
理由が無い。左ペインには「オフライン」と出す。

### 「削除」は本当に捨てる［ユーザー判断］。**ゴミ箱が無ければ完全削除へ振り替える** [NV4-01]

§15.7（見つからないファイル）の削除は**記録だけ**を消す（実体がもう無いため）が、
**こちらは実ファイルをゴミ箱へ送り、記録も消す。** 記録だけ消すと、`.qooarchive` は
走査の対象 [SY-10] なので**次の走査で必ず復活する**——しかもラベルを失った状態で。
「保管庫から削除した」の意味は「本当に捨てる」であって、記録の付け替えではない。

ゴミ箱を経由するので Finder から取り戻せるし、⌘Z も効く。確認ダイアログでは
**外れるラベルの件数**と**ゴミ箱へ行くこと**の両方を見せる。

**ゴミ箱を持たない場所では完全削除へ振り替える** [NV4-01]［実機検証で発見］。
`TrashCommand` を直に呼ぶと `CompositeCommand` が最初の子で止まり**何も
起きない**——しかもアプリは「『すぐに削除』をお使いください」と案内するのに、
この画面にその項目は無い。判定は**確認ダイアログを出す前**に行い
（`FileVaultModel.planDelete()`、`FolderOperations.moveToTrash` と同じ形）、
文言も「完全に削除され…この操作は取り消せません」へ変える。

> **共有だけの話ではない。** `TrashAvailability` は `.trashDirectory` を
> `create: false` で尋ねるので、**まだ一度も何も捨てていない外付けボリューム
> でも「ゴミ箱なし」になる**（1-17 の実測、8章 §8.7.1）。

`CompositeCommand.isUndoable` は子の `allSatisfy` なので、完全削除 [PD-05] が
混ざれば自動的に取り消せなくなる。**その性質と確認の文言がずれると、いちばん
取り返しのつかない場面で嘘をつく**ので、`makeDeleteCommand` を切り出して
テストで固定してある。

**サイドカーのカバー画像も一緒に捨てる。** 保管庫へは連れて行く [FA-14] のに
捨てるときだけ残すと、一覧が「保管庫は空です」と言っているのに `.qooarchive`
には画像が残る——誰も参照せず、画面からは見えず、片付ける手段も無い。

**空になったフォルダは畳まない**［設計判断］。移動 [FA-06][FA-08] とは違う
——Finder もファイルを消してフォルダを消しはしないし、畳むと ⌘Z の戻し先が消える。

### 区画は元のフォルダ [FAW-01][FDA-05]

フォルダごと移した場合もファイル単位で扱う。`.qooarchive` の中は元の階層を
そのまま写している [FA-03] ので、「どのフォルダから来たか」はパスから読める
——フォルダという単位を別に持たない。記録（`archivedFromPath`）が無い
（外部で `.qooarchive` へ入れられた）ものは、現在のパスから導く [FA-03]。

**並べ替え [FAW-05] は区画の中へ効かせる**（§15.3 と同じ）——元フォルダを
またいで混ぜると「元フォルダごとに整理」が消える。**日時の記録が無い行は末尾へ**
——先頭に集めると「いつ保管したか分からないもの」が最初に目に入る。

### 入口

| 場所 | 備考 |
|---|---|
| フォルダツリーの登録ルート行 | **2 つある行型の両方**（通常の `FolderTreeContextMenu` と縮退した `DegradedRegisteredFolderRow`）。配線は別々なので項目は 2 か所に要る |

`LabelVaultNavigation` と同じく `FileVaultNavigation.open(libraryID:openWindow:)` が
唯一の入口 [CP-02]。

### 着脱に追随する配線が要る [SB-05]

`.onChange(of: LibraryServices.shared.libraries)` で読み直す。**`LabelVaultWindow`
にはこの配線が無く、それを写して抜けていた**［実機検証で発見］——あちらは
オフラインでも読み書きしてよいので要らないが、こちらは `canModify` が
`isOnline` を見るので必須で、**無いと取り出したあとも押せるボタンが残り、
押した瞬間に失敗する。** §15.7 が 2-14 で同じ穴を塞いでいる。

> **前例を写すときは、その前例が何によって守られているかまで写す。**
> 「同じ形にした」は「同じ性質を持つ」ではない。

### 2 行の行には縦の余白を明示する

行はファイル名と「保管した日時 ＋ ラベル件数」の 2 行。**`.padding(.vertical, 3)`
が無いと `List` の行の高さが中身より低くなり、選択の帯が 2 行目を切る**
［実機検証で発見］——一括で戻す [FAW-04] には選択が要るので、いちばん見たい
ときに日時 [FAW-05] が読めなくなる。**未選択では何も起きていないように見える。**

### 実機検証済み（2026-08、使い捨てボリューム）

3 つの入口・元フォルダごとの区画・区画内での並べ替え・全角での検索・2 つの
空状態・FAW-01〜05・オフラインでの出し分け・縮退した登録ルート行からの起動まで
確認した。見つけた 3 件（上記）を直してから再確認している。詳細は CLAUDE.md
「ファイル保管庫」節。

## 15.5 テンポラリの設定ウインドウ

3 ペイン。左＝テンポラリ一覧／中央＝設定項目グループ／右＝詳細。

| 中央ペインの項目 | 内容 |
|---|---|
| 自動リネーム設定 | ルール一覧（D&D 並べ替え [AR2-05]、有効／無効）+ 編集 + サンプルプレビュー [AR2-06] + 冪等性検証結果 [AR2-05a] + 例外設定 [AR2-20〜AR2-24] |
| 区切り文字 | ペア型・セパレータ型 [DL-13] |
| 保護文字列 | ライブラリ設定と相互コピー可 [PT-08] |
| 移動先の既定ライブラリ | [MV-01] |
| 変換リネーム | 導線のみ。編集は 15.9 節の専用ウインドウ |

自動リネーム履歴は操作履歴ウインドウから確認する [AR2-07][HS-03]。

## 15.6 未解決ファイルの整理ウインドウ

| # | 要件 |
|---|---|
| UR2-01 | マッチしなかったファイル一覧を表示 [UR-01] |
| UR2-02 | ライブラリごとにグループ化 [UR-02] |
| UR2-03 | 手動でラベル付与 [UR-03][AL-30①] |
| UR2-04 | 新規フォーマットをその場で登録し再マッチング [UR-04][AL-30②][AL-34] |
| UR2-05 | 「以後無視する」フラグ [UR-05][AL-33] |
| UR2-06 | 複数選択して一括適用 [UR-06][AL-32] |
| UR2-07 | 選択ファイル名をゴールデンテスト用サンプルとしてエクスポートする導線 [MT-26] |
| UR2-08 | 500 件超の場合、「フォーマットを追加して再マッチング」を最初の選択肢として提示 [OB-08] |

### 表示は「ファイル名フォーマットに一致しないファイル」と呼ぶ［ユーザー判断、2026-08］

**要件 ID・`unresolvedFile` テーブル・型名（`UnresolvedFile` 等）は「未解決」の
まま据え置く**——要件との対応が切れると追えなくなる（§15.7 で「孤立ファイル」を
「見つからないファイル」に改めたときと同じ扱い）。診断ログも内部用語のまま。

**「ラベルのないファイル」は採らなかった。** 直感的だが**約束する範囲が実装と
食い違う**——この一覧に出るのは「どのファイル名フォーマットにも一致しなかった」
ファイル [UR-01][AL-30] であって「ラベルが 0 件のファイル」ではない。
**プリセットを使っているだけでずれる**: 一般コミック(B) と成年コミック(B) は
`@title` だけのフォーマットを持ち、それに当たったファイルは**ラベル 0 件なのに
一覧に出ない**（フォーマットには一致しているため）。「ラベルのないファイル」と
名乗ると、ラベルの無いファイルが出てこない画面になる。

#### 語彙を揃えた範囲

窓の題・メニュー項目・件数・空状態に加えて、**有効化のプレビュー**
（「N 件が一致せず」）と**走査結果のシート**（「どのファイル名フォーマットにも
一致せず」）も同じ語彙にした。「解決」という内部用語は画面から消してある
（再マッチングの結果は「N 件のうち M 件が**一致するようになりました**」）。

**`unresolvedFiles.noMatches` は「検索に一致するファイルがありません」へ変えた**
——「一致」がフォーマット照合の意味を持つようになり、素の「一致するファイルが
ありません」だと 2 通りに読めるため。

#### 収まらない場所では短い形を使う［実測］

フッターの「フォーマットを追加…」は**長い形にすると切り詰められる**——
「ファイル名フォーマットを追加…」は 178pt 要るのに枠は 130pt しかない（実測）。
窓の題が「ファイル名フォーマットに一致しない…」、開くダイアログが
「ファイル名フォーマットを編集」なので、この画面では「フォーマット」だけで
巻数フォーマットと紛れない。左ペインの件数も同じ理由で「一致しない N 件」。

> **フォルダツリーの右クリックメニューは 254pt → 386pt に広がる**（実測）。
> 壊れてはいないが、他の項目より目立つ。気になるようなら項目名だけ短くする
> 余地がある——窓の題は長いままでよい。

### 実装状況（2-7 の記録側 ＋ 2-14 の未解決側、2026-08）

**UR2-01〜UR2-06 と UR2-08 を実装した。** `UnresolvedFileModel`（判定）＋
`UnresolvedFilesWindow`（描画）＋ `SetUnresolvedIgnoredCommand`（Undo）。

| # | 状態 |
|---|---|
| UR2-01 一覧 [UR-01] | 済。中央ペイン |
| UR2-02 ライブラリごと [UR-02] | 済。左ペインで切り替える |
| UR2-03 手動ラベル [UR-03] | 済。**右ペイン（インスペクタ）と同じ実装を共有**（下記）|
| UR2-04 再マッチング [UR-04] | 済。フォーマットの追加も**その場で**できる（下記）|
| UR2-05 無視 [UR-05] | 済。行のメニューと下端の一括操作 |
| UR2-06 一括適用 [UR-06] | 済。ラベルは三状態のチェックボックス [RP-02]、無視は下端のボタン |
| UR2-07 サンプル書き出し [MT-26] | **実装しない**［ユーザー判断、2026-08］。実ファイル名がディスクへ落ちる経路を新設することになり、`MT-32` の層 0 と正面から衝突する。既に `Scripts/extract-golden-corpus.swift` が同じ役割を果たしており、そちらは `private/` へしか出さない形で守られている |
| UR2-08 500 件超の案内 | 済。`UnresolvedFileModel.shouldOfferFormatFirst`（境目は `AppLimits.Library.unresolvedBulkThreshold`）|
| 「最も近いフォーマット」の推定 [§4.11 UR2-05] | **未**。列（`nearestFormatSource` / `nearestFormatReach`）は v1 からあるが誰も書かない——パーサに「照合が最も進んだ入力位置」を保持させる必要があり、ゴールデンテストで固めてある中核を触ることになるため今回は見送った［ユーザー判断］|

### 空状態は 2 通りある［実機検証で見つけた］

「未解決が 1 件も無い」には**性質の違う 2 つ**がある。混ぜると文言が必ず
どちらかで嘘になる:

| 状態 | 出す文言 |
|---|---|
| 本当にすべて一致した | このライブラリのファイルはすべて、いずれかのファイル名フォーマットに一致しています。 |
| 無視 [AL-33] して空になった | 無視した N 件を隠しています。「無視したものも表示」で戻せます。 |

そのため `ManagedFileRepository.unresolvedFileCounts()` は **`UnresolvedCounts`
（`pending` / `ignored`）** を返す——1 つの数字に畳むと、後者のときに
「すべて一致しています」と**事実でないこと**を言い、しかも戻す手段へ誘導しない
（`LabelSummary` が件数を 2 つ持つ [LE-05][FA-05] のと同じ形）。

### 手でラベルを付けたら「以後無視する」も立てる [AL-30]①③［ユーザー判断、2026-08］

`isUnresolved` は**パース結果**だけを見る [EM-03] ので、手で付けたラベルは判定を
動かさない。そのままだと [AL-30]① で片付けたつもりのファイルが一覧に残り続け、
③（無視）を続けて使うしか無くなる——**①だけで一覧から消えるようにする。**

| 決めごと | 理由 |
|---|---|
| **同じ Undo 単位**にまとめる（`CompositeCommand`）[UD-04] | 別々に積むと ⌘Z 2 回を要し、その途中で「ラベルは戻ったが一覧に出てこない」半端な状態を経由する |
| Undo メニューの文言は**ラベル側**を使う | 付随する操作まで書くと、利用者が意図した操作（ラベルを付けた）が読み取りにくくなる |
| **付けたときだけ**（外したときは立てない）| 外した結果として一覧から消えるのは意味が逆 |
| **いま一覧に載っているものだけ**が対象 | 一覧の外（既に無視した・解決した）のファイルに人の判断を新しく立てない |

**実装は `LabelEditorModel.onAssign`**（付けたときに一緒に走らせる操作を返す
フック）と `UnresolvedFileModel.ignoreCommandForAssigned(_:)`。

> **右ペイン（インスペクタ）ではこのフックを設定しない。** 蔵書のどのファイルに
> ラベルを付けても未解決の判断が動いてはならない——`InspectorPane` は自分の
> `LabelEditorModel` を持ち、`onAssign` は既定の `nil` のまま。実機でも
> 「右ペインから付けても `isIgnored` が立たない」ことを確かめてある。

### オフラインでも開けて書ける（§15.7 との違い）

形は似ているが、**実体を見るかどうかが逆**である。孤立は「実体が見つからない」
という実体についての判断なのでオフラインでは一覧できない [OR2-06] が、未解決は
**ファイル名とフォーマットの照合結果**なので実体を 1 度も見ない——ラベル保管庫
（§15.3）と同じく、オフラインでも開けて読めて書ける。外付けが無い間に
フォーマットを直しておく、はむしろ普通の場面である。

**この違いは `LibraryMenuVisibility` にも `UnresolvedFileModel.defaultLibrary` にも
現れる**（後者は孤立側と違い `isOnline` を見ない）。§15.7 の造りを写すときは、
着脱に追随して一覧を伏せる配線（`onChange(of: libraries)`）を**持ち込まないこと**
——あちらが何を守っているかまで写すと、こちらでは意味の無い制限になる。

### 右ペインは右ペイン（インスペクタ）と同じ実装を共有する

ラベルの付け外し [UR-03][UR-06] は `LabelEditorModel` ＋ `InspectorLabelSection`
がそのまま使える——**三状態のチェックボックス** [RP-02] が「複数選択して同じ
ラベルを一括で付ける」[AL-32] そのものなので、ここに別の UI を作る理由が無い。

そのために `LabelEditorModel` へ **`load(rows:library:services:)`** を足した。
既存の `load(urls:)` は `fileRows(at:in:)` が inode を読むため**実体が要る**ので、
そのまま使うと**オフラインのときだけラベルを付けられない**という、画面からは
説明の付かない差が出る。行が揃ってからの処理は `finishLoading` に集約してある
——同じ「ラベル欄の組み立て」を 2 箇所に書かない。

### フォーマットはその場で足せる [UR-04]

編集ダイアログは設定ウインドウと同じ `FilenameFormatEditorDialog` を使う——
予約語パレットもサンプル試行も検証も同じものが要るので、ここに別の編集 UI を
作らない [CP-01]。確定すると `LibrarySettingsDraft.filenameFormats` へ 1 本足して
保存し、**続けて再マッチングまで走る**。

**保存の直前に草案を引き直す。** ダイアログを開いている間に設定ウインドウで
別の変更が保存されているかもしれず、開いた時点の草案をそのまま書き戻すと
**その変更を黙って巻き戻す**（`updateSettings` は設定を丸ごと置き換える）。
評価の全巻適用 [RA-04] で踏んだのと同じ形。

### Undo

| 操作 | Undo | 理由 |
|---|---|---|
| 無視の切り替え [UR-05] | 可（`SetUnresolvedIgnoredCommand`）| 変更前の値を 1 件ずつ持つ。混在した選択への一括操作を戻したときに「元から無視だったもの」まで裏返らないようにする |
| ラベルの付け外し [UR-03] | 可（`AssignLabelCommand`）| 右ペインと同じコマンド |
| 再マッチング [AL-34] | **不可**［設計判断］| 走査と同じ収束型の処理で、⌘Z で「解決したことを取り消す」のは意味が薄い——戻したいのは普通「足したフォーマット」のほうで、それは設定の編集という別の経路にある |
| フォーマットの追加 [UR-04] | 不可 | 設定の編集は元から Undo の対象外（草案 ＋ 保存） |

### 入口 [CP-02]

`UnresolvedFilesNavigation.open(libraryID:openWindow:)` の 1 つに集める。
呼ぶのは 3 か所:

1. フォルダツリーの登録ルート行（通常）
2. **同じく縮退した行**（`DegradedRegisteredFolderRow`）——配線は別々なので
   両方に要る。このリポジトリが片方だけ配線して取り残した形は、これで 6 度目
3. **走査結果のシートの「整理する…」** [UR2-02][AL-30]。孤立（件数を知らせる
   だけ）と扱いを変えているのは、未解決は放置すると**ラベルが 1 つも付かない
   まま蔵書に埋もれる**ため——ラベルフィルタからは永久に辿り着けない。
   そのために `LibraryEnableAction` へ `openWindow` を持ち回っている
   （`@Environment(\.openWindow)` は View からしか取れない。受け皿へ預ける形に
   すると、預ける前に走査が終わった場合に黙って開かなくなる）

## 15.7 見つからないファイルの整理ウインドウ

| # | 要件 |
|---|---|
| OR2-01 | `state == .orphaned` の一覧 [OR-01] |
| OR2-02 | 再照合候補があればワンクリック再紐づけ [OR-02][ID-05] |
| OR2-03 | 手動で任意ファイルを選んで再紐づけ [OR-03] |
| OR2-04 | 不要レコードの削除（一括可）[OR-04] |
| OR2-05 | 発生時に通知履歴へ記録。個別バッジは持たず通知バッジに統合 [OR-05][NT-06] |
| OR2-06 | オフラインのライブラリのレコードはここに出さない [ID-08][SB-05] |

### 表示は「見つからないファイル」と呼ぶ［ユーザー判断、2026-08］

要件と内部の状態名は `orphaned`（孤立）だが、**画面ではそう呼ばない**
——「何から孤立しているのか」が読み取れないため［ユーザー指摘］。孤立して
いるのはファイルではなく**そのファイルについて DB が覚えている記録**で、
しかも §15.6 の「**未解決**ファイル」（実体はあるがフォーマットに一致しない）と
並ぶと、どちらも「〜ファイル」で区別が付かない。

| 場所 | 表示 |
|---|---|
| ウインドウ名・メニュー項目 | 「見つからないファイル」/ Missing Files |
| 左ペインの件数 | 「見つからない N 件」 |
| 削除 | 「記録を削除」——消えるのは**覚えていること**であって実体ではない |
| 再紐づけ [OR-02][OR-03] | 「**このファイルの記録として紐づける**」［ユーザー指定］|

**「再紐づけ」も直感的ではなかった**［ユーザー指摘: 「そもそも、何と何の
紐づけなのか？」］。紐づけの両端は **①記録**（`managedFile` の 1 行が覚えて
いるラベル・評価・手動タイトル・カバー指定）と **②実体**（`(volumeUUID,
inode)` が指すディスク上のファイル）で、この操作は①が指す②を付け替える。
**語に「何の記録か」を入れる**ことで、両端が画面から読めるようにした
——「再」を落としたのは、一度結ばれていたという履歴が利用者に見えないため。

**走査結果のシート [ID-06] は最初から正しかった**——「N 件がライブラリに
記録されていますが、実体が見つかりませんでした」。ずれていたのはこの
ウインドウだけで、そちらへ揃えた形になる。

**要件 ID（OR-01〜05）・`state = .orphaned`・型名（`OrphanedFile` /
`OrphanCleanupModel`）は据え置く**——要件との対応が切れると、どの要件を
実装したものか追えなくなる。

### 実装状況（2-14 の孤立側、2026-08）

2 ペイン。左＝ライブラリ一覧（孤立件数／オフラインはグレーアウト）、
右＝孤立レコードの一覧。`LabelVaultWindow` と同じ `NavigationSplitView` で
見た目を揃えている [CP-01]。

| # | 状態 |
|---|---|
| OR2-01 | **実装済み。**`orphanedFiles(libraryID:)`。相対パス順 |
| ~~OR2-02~~ | **撤回**［ユーザー判断、2026-08］。候補の提示と結び直しは §15.7.1 の一括確認が担う |
| ~~OR2-03~~ | **撤回**［同上］。手動でファイルを選ぶ経路は設けない |
| OR2-04 | **実装済み。**行のコンテキストメニューと、フッターの一括削除。確認あり |
| OR2-05 | **未。**通知履歴 [NW-01〜08] もステータスバーのバッジも未実装で、NT-06 が「通知履歴のバッジに統合する」と定めている以上、先に作るのはそちら。現状は走査結果のシートが件数だけ知らせる |
| OR2-06 | **実装済み。**一覧を出さず、理由を書く（下記）|

### 未解決側（§15.6）とは前提が違うので分けて着手した

CLAUDE.md は長く「2-14 は `unresolvedFile` に誰も書いていないので 2-7 から
要る・見た目より大きい」と書いていたが、それが当てはまるのは**未解決側だけ**。
孤立側は `managedFile.state == .orphaned` を読むだけで、データは走査が既に
書いている——**2-2 で孤立が日常的に発生するのに件数しか分からない**という
実害を、2-7 を待たずに埋められる。

### オフラインは一覧しないが、ウインドウは開ける [OR2-06][ID-08][SB-05]

**ラベル保管庫（§15.3）とは扱うものの性質が逆である。** ラベルは実体に依らない
ので保管庫はオフラインでも読み書きできるが、孤立は「実体が見つからない」という
**実体についての判断**なので、見えない状態で確定させてはならない——一覧に出すと、
外付けを挿し忘れただけのものを削除できてしまう [R-01 が防ごうとしている事故]。

- 一覧の代わりに**理由を書く**［ユーザー判断］。左ペインからライブラリごと
  消すと「孤立が無い」のか「見られない」のか区別が付かない。
- **メニュー項目は縮退状態でも出す**（`LibraryMenuVisibility` の offline セット）。
  ウインドウは全ライブラリを持つので他へ切り替えられ、行き止まりにならない。
  加えて、縮退している登録こそ「孤立していないか」を確かめたくなる——開いて
  「オフラインの間は孤立と判定していない」と読めること自体に意味がある。

### 結び直しはここに置かない［ユーザー判断、2026-08］

**触れるのは一覧と削除だけ。** 実体を結び直す手段はこの画面から外した——
ユーザー指摘:「見つからなかったファイルをわざわざ解決する UI は不要。DB 上
一度は見失ったファイルが同じ inode で復活したらそのまま戻せばよいし、
ファイル名が同一で inode が異なるなら**リストを表示してユーザーにまかせれば
いい**。あとは、ファイル実体を失った DB 上のエントリをユーザーが任意に削除
する手段があれば十分」。

| 状況 | 誰が引き受けるか |
|---|---|
| 同じ inode で復活した | **走査が自動で戻す** [ID-02]（`updateInPlace` の `CASE`）|
| 名前が同じで inode が違う | **走査後の一括確認** [ID-05]（§15.7.1）|
| 実体が本当に無い | **この画面**（一覧と削除）|

3 つに分かれていて、どれもこの画面が担うべきものではなかった。**同じ操作を
2 箇所に置かない**——候補の提示を両方に持つと、片方だけ直して取り残す。

### 候補は保存されていない [ID-05]

走査は `.nameOnly` でしか一致しなかったものを数えるだけで、その実ファイルは
新規レコードとして別に入る。だから候補は**引き直す**（07章 §7.5）——
`identityMatchesAwaitingDecision` が §15.7.1 の一覧を組み立てるのに使う。

### Undo［ユーザー判断: 削除も戻せる］

`managedFile.id` も `AUTOINCREMENT` なので**同じ行 ID へ復元できる**
［実測、07章 §7.3 の訂正］。孤立レコードの削除で失うのは実体ではなく
「その本について覚えていたこと」だけなので、戻せるほうが安全側に倒れる
——実ファイルを消す `DeletePermanentlyCommand`（`isUndoable = false`）とは
性質が違う。

- 削除の確認は**何件のラベルが外れるか**を見せる（`DeleteLabelsDialog` と
  同じ考え方）。あわせて**実ファイルには触れない**ことを明示する——「削除」
  という語だけだと、まだ残っている実体まで消すと読める。

### 一覧に出すもの

名前・元の相対パス・ラベル件数・評価。**孤立した日時は出さない**——列を
持っておらず（`archivedAt` は保管庫用）、OR にも日時の要件が無い。
ファイル保管庫（§15.4）が FAW-05 で日時を要求しているのとは対照的である。

**絞り込みを付けた**［実装判断、要件には無い］。ボリュームの中身が入れ替わると
全件が孤立しうるので、数千件から目当ての 1 件を探せないと [OR-04] が実行
できない。名前だけでなく**相対パスも見る**——孤立の一覧で目印になるのは
「どのフォルダにあったか」であることが多い。並べ替えは付けていない（列が
少なく、パス順で足りる）。

### 開いたまま着脱に追随する [VD-03][VD-05]

**実機検証で見つけた欠陥。** 外付けを取り出しても、開いたままのこの
ウインドウは古い一覧を出し続け、**そこから記録を削除できてしまった**
——OR2-06 が防ごうとしている事故が、ウインドウを開きっぱなしにするだけで
起きる。

真因はこのウインドウではなく**その手前**にあった。`LibrarySyncCoordinator` は
`setOnline` で DB を書き換えるが、`LibraryServices.libraries`（問い合わせた
時点の写し）を更新していなかったため、**DB を直しても上位に届いていなかった**。
`onOnlineStateChanged`（`onScanFinished` と同じ形）を足して `refreshLibraries()`
へ繋ぎ、このウインドウは `libraries` の変化に乗る。

**`LabelVaultWindow` に同じ配線は無い**——あちらはオフラインでも読み書きして
よい（ラベルは実体に依らない）。形が同じでも前提が逆なので、写すときは
何を守っているかまで写すこと。

### 入口 [CP-02]

フォルダツリーの登録ルート行の「見つからないファイルを整理…」。**行型は 2 つある**
（通常の `FolderTreeContextMenu` と縮退した `DegradedRegisteredFolderRow`）
ので配線も 2 か所に要る——このリポジトリが 4 度取り残した形。開く経路は
`OrphanCleanupNavigation.open(libraryID:openWindow:)` の 1 つ。

**走査結果のシートには導線を足していない**［ユーザー判断で見送り］。巻数の
判断待ち [EM-30〜35] が同じ `RecoveryAction` の形を持っているので、必要に
なれば同じ形で足せる。

### 15.7.1 差し替えの確認ダイアログ [ID-05][ID-09〜ID-12]

**名前は同じだが inode が違う**ファイルについて、引き続き同じものとして扱って
よいかをまとめて聞く。承認されたものは inode を更新し、ラベル・評価・編集した
タイトル・選んだカバーをそのまま引き継ぐ。

**走査は止めない** [ID-05]。走り切ってから通知（強度 2）で件数だけ伝え、
押すとこのダイアログが開く——巻数の確認 [EM-30〜35] とまったく同じ形。

#### なぜ確認するのか、なぜ自動にしないのか

ユーザー指摘:「**同じパス＋同じ名前だが、中身が異なるファイルへの差し替えと
いうのは頻繁に起きる。スキャンした本から電子版への差し替えなど。過度な
同一性のチェックは逆に使いにくい**」。

改訂前の ID-05 では、この差し替えのたびに「見つからないファイル」が生まれ、
手で付けたラベル・評価・タイトルを失っていた——[ID-03]①② はどちらも
**サイズの一致**を条件にしており、差し替えは必ずサイズが変わるため。

一方で「名前が同じなら黙って同じもの」とするのも危うい。`第01巻.cbz` は複数の
シリーズに存在しうる。**走り切ってからまとめて聞く**ことで、日常的な差し替えの
手数を増やさずに、危険な取り違えだけを目に触れさせる。

| # | 決めたこと | 理由 |
|---|---|---|
| ID2-01 | **同じ場所**と**別の場所**を区画で分ける [ID-09] | 確信度がまったく違う。同じ場所の差し替えはほぼ確実だが、別の場所の同名ファイルは別作品かもしれない |
| ID2-02 | 同じ場所の区画を先に置く | 迷いなく承認できるものを上に集める |
| ID2-03 | **既定ですべてにチェック** [ID-10]［ユーザー判断］ | 差し替えは日常的に起こるので、通常は「適用」を押すだけで済ませる。危険なものは区画で分けて見せることで補う |
| ID2-04 | 候補が複数あっても**最有力の 1 件だけ**を出す | この画面は二択（同じものか別物か）に絞る。選択肢を増やすと毎回考えさせることになる |
| ID2-05 | 行には**大きさの変化**を出す | 「中身が違う」ことの唯一の手がかり。引き継がれるラベル件数も添える |
| ID2-06 | **チェックを外したものは却下として記録**し、以後聞かない [ID-11] | `第01巻.cbz` のような名前では走査のたびに同じ組が挙がる。一度答えたものを聞き直しては使い物にならない |
| ID2-07 | 「今は決めない」は**キャンセル**で表す | 何も起きず、次回また聞かれる。ダイアログ本文にそう書く——外したら二度と聞かれない、という副作用を先に伝える |
| ID2-08 | 1 回の「適用」が **1 つの Undo 単位** [UD-04] | 利用者から見て 1 度の操作。**却下も戻せる**——一度「別物」と答えると以後聞かれないので、間違えたときに取り消せないと行き止まりになる |
| ID2-09 | **後回しにできる** [ID-12] | 通知を閉じてしまっても、ライブラリ設定の「基本」からいつでも開ける（件数が 0 なら導線を出さない）|
| ID2-10 | **そもそも確認するかどうかを選べる** [ID-13]［ユーザー要望、2026-08］ | 「上書きや移動でダイアログが出るのを煩わしく感じる人がいる」——**既定は確認しない**ので、このダイアログを見るのは厳しい設定を選んだ人だけになる。それでも実装を残すのは、**選べるようにするには受け皿が要る**ため |

#### 既定では、この画面は出ない [ID-13]（2026-08 追加）

**同じウインドウの中で語を混ぜない。** 区画の見出しは当初「ほぼ確実に同じ本」
「同じファイル名の別の作品」と書いていたが、説明文と選択肢は「ファイル」で
統一されており、**1 つの画面に「ファイル」「本」「作品」の 3 語が混ざっていた**
［ユーザー指摘］。この画面が扱うのは「ディスク上のファイル」と「qooLibrary が
覚えている記録」の対応なので、**「ファイル」に揃えてある**。

ライブラリ設定「基本」の 3 択（[ID-13]、10章 §10.4.1 の表）でこの画面が
出る条件が変わる。**既定は「名前が同じなら確認しない」**なので、何も設定
しなければこのダイアログは現れない。

#### 自動走査でも知らせる［ユーザー判断、2026-08］

「**自動走査はダイアログを出さない**」（10章 §10.6）という既存の方針を、
**判断が要るものに限って緩めてある**。孤立・未解決の件数は「知らせるだけ」
なので黙ってよいが、差し替えの確認は**放置すると記録が失われたままになる**。

**この配線が無いと通知は実質出ない**——FSEvents の自動追随が先に差し替えを
拾ってしまうので、あとから手動で再スキャンしても `.nameOnly` の経路を通らない
（既に新しい行が同一性で引ける）。実機検証でそれを確認してから足した。
受け口は `LibraryServices.onAutomaticScanFinished`。

**同じ確認待ちが何度も出ることにはならない。** `candidatesForReview` は走査が
`.nameOnly` を**新しく検出したとき**にしか数えられない。

#### 却下の記録は組の片方が消えれば消える

`identityRejection`（v5 で追加）は `(orphanFileId, candidateFileId)` を主キーに
持ち、**どちらの行にも `ON DELETE CASCADE`** を張ってある——組の片方が無く
なれば、その判断はもう意味を持たない。ラベルの `manuallyRemoved` [RC-04] と
同じ考え方で、「一致しなかった」ではなく「**一致させないと決めた**」を
明示的に持つ。

## 15.8 ライブラリ移動の結果・ペンディング一覧ウインドウ

2 セクション（またはタブ）: 「移動完了」と「ペンディング」。

### 一覧の列

| 列 | 内容 |
|---|---|
| ファイル名 | インライン編集可 [PW-01] |
| 理由 | フォーマット不一致 / ライブラリタイプ不一致 / 階層値の欠落 [MV-19] |
| 解析結果 | 抽出できたフィールドの内訳。できなかった項目は空欄 |
| 移動先候補 | 判定できた場合のパス。できなければ「未定」 |
| 状態 | ペンディング / 解決済み（実行待ち）|

### アクション（個別・複数選択のいずれでも）

| ID | アクション | 実装メモ |
|---|---|---|
| PW-01 | その場でリネーム | インライン編集。入力のたびに再判定し、適合したら「解決済み」へ遷移。実ファイルもリネームする [PW-15] |
| PW-02 | リネーム候補の提示 | 最も近いフォーマット（§4.11 UR2-05 の最遠到達点）に当てはめた候補名。クリックで採用 |
| PW-03 | ライブラリ直下へ移動 | 解析結果を問わずまとめて |
| PW-04 | 移動をキャンセル | テンポラリに残し一覧から取り除く |
| PW-05 | 移動先ライブラリを変更 | 再判定。ライブラリタイプ不一致 [MV-14b] の主要解決手段 |
| PW-06 | 新しいフォーマットを登録 | 移動先ライブラリへ追加し全件再判定 [AL-34] |
| PW-07 | 手動でラベル割り当てて移動 | 各グループの値を手入力し移動先を決定 |
| PW-08 | タイプ不一致を承知で移動 | 名前を変えずに移動。ラベル付与は通常どおり。不一致を操作履歴に記録 |
| PW-09 | CSV エクスポート | 外部検討用 |
| PW-17 | **変換リネームを実行** | 選択行を対象に 15.9 をスコープ ③ で開く [MV-22] |

| # | 要件 |
|---|---|
| PW2-10 | 「解決済み」をまとめて移動実行するボタン。実行前にプレビュー [PW-10] |
| PW2-11 | 選択行のカバー画像・サイズ・更新日を右側に表示 [PW-11] |
| PW2-12 | 一括操作は 1 Undo 単位 [PW-12] |
| PW2-13 | ウインドウを閉じても保持し「ペンディング一覧を開く」で再開 [PW-13][MV-20] |
| PW2-14 | 残件数をステータスバーに表示。バッジは通知履歴に統合 [PW-14][NT-06] |
| PW2-15 | この経路のリネームは自動リネームの対象外 [PW-15][CR-63] |
| PW2-16 | 他ウインドウと同じリスト・ボタン配置 [PW-16][UI-04][UI-05] |
| PW2-17 | PW-17 で開いたウインドウには移動先ライブラリのフォーマットを出力候補として渡す [PW-18][CW-41] |
| PW2-18 | 変換リネーム実行後、対象を自動再判定し適合したものを「解決済み」へ [PW-19][CW-43] |
| PW2-19 | 解決手順として ① インラインリネーム（少数）② 変換リネーム一括（多数）を並べて提示 [PW-20] |
| PW2-20 | 外部で削除・移動されたペンディングは次回表示時に取り除き、その旨を示す [MV-21] |
| PW2-21 | 選択ファイル名をゴールデンテスト用サンプルとしてエクスポートする導線 [MT-26] |

## 15.9 フォーマット変換リネームウインドウ

```
┌─────────────┬──────────────┬───────────────────────┐
│ テンポラリ    │ ルール一覧     │ 選択ルールの編集         │
│ フォルダ一覧  │ （優先順）     │ 入力／出力フォーマット等  │
├─────────────┴──────────────┴───────────────────────┤
│ プレビュー（実ファイルに対する 変更前 → 変更後 の一覧）      │
└──────────────────────────────────────────────────┘
```

| # | 要件 |
|---|---|
| CW2-01 | 専用ウインドウ。**モーダルにしない** [CW-01] |
| CW2-02 | 左＝テンポラリ一覧。選択がプレビュー対象 [CW-02] |
| CW2-03 | 中央＝ルール一覧。追加・複製・削除・D&D 並べ替え・有効／無効 [CW-03] |
| CW2-04 | 右＝ルール編集（入力／出力フォーマット、巻数出力書式、対象拡張子、サブフォルダ、禁止文字の置換規則）[CW-04] |
| CW2-05 | 右ペインにサンプル入力欄。実ファイルと独立に即時確認 [CW-05][CR-07] |
| CW2-06 | 区切り文字・保護文字列はテンポラリ単位設定のため、設定画面へのリンクを置く [CW-06] |

### 実行スコープ [CW-07〜CW-09]

```swift
public enum ConvertScope: Hashable, Sendable {
    case wholeFolder(temporaryFolderID: UUID, includeSubfolders: Bool)  // ① 既定
    case selectedFiles([URL])                                           // ②
    case pendingList(sessionID: UUID, entries: [UUID])                  // ③
}
```

現在のスコープと対象件数を上部に常時表示 [CW-08]。①ではサブフォルダ含有を切替可、②③では固定・無効化 [CW-09]。

### プレビューエリア [CW-10〜CW-20]

| 列 | 内容 |
|---|---|
| チェックボックス | 実行対象の選択 [CW-14] |
| 変更前 | |
| 変更後 | インライン手入力で上書き可。上書き行に「手動」バッジ [CW-17] |
| 適用ルール | |
| 状態 | 変換可 / 変更なし / 未変換 / 衝突 / 警告 [CW-12] |
| （スコープ③のみ）判定 | 解決見込み / 未解決 [CW-42] |

| # | 要件 |
|---|---|
| CW2-10 | 選択テンポラリ配下の実ファイルに全ルールを適用した結果を一覧表示 [CW-10] |
| CW2-11 | 衝突行は赤字、実行対象から自動除外 [CW-13] |
| CW2-12 | 全選択・全解除ボタン [CW-14] |
| CW2-13 | 状態フィルタ（未変換のみ / 変換可のみ / 問題のある行のみ）[CW-15] |
| CW2-14 | 行選択で**フィールド分解表示**（どのルールのどのフィールドにどう分解されたか）。必須機能 [CW-16][T-11] |
| CW2-15 | 手動上書き行は再計算で上書きされない [CW-17][PV-05] |
| CW2-16 | 設定変更でプレビューを自動再計算。件数が多ければ非同期・進捗・キャンセル [CW-18] |
| CW2-17 | 未変換行から「この名前からルールを作成」。入力フォーマットの雛形を提示 [CW-19] |
| CW2-18 | プレビューを CSV エクスポート [CW-20] |

### 一括実行 [CW-21〜CW-25]

| # | 要件 |
|---|---|
| CW2-20 | 数千件規模に対応。プレビュー計算は非同期で UI をブロックしない [CW-21][PF-08] |
| CW2-21 | 一覧は Lazy。可視範囲のみ描画 [CW-22][PF-10] |
| CW2-22 | 適用ルールごとのグループ表示切替。ルール単位で件数把握・一括チェック [CW-23] |
| CW2-23 | 部分文字列による絞り込みと「表示中をすべて選択」[CW-24] |
| CW2-24 | 「変換可のみ」を一括選択して実行する運用を標準手順とする [CW-25] |

### 実行 [CW-30〜CW-38]

| # | 要件 |
|---|---|
| CW2-30 | チェック済み行のみリネーム [CW-30] |
| CW2-31 | 実行前に対象件数の確認ダイアログ [CW-31] |
| CW2-32 | 実行後に結果サマリ（成功／失敗／スキップ + 理由別内訳 + 再試行）[CW-32][ER-14][ER-15] |
| CW2-33 | 1 実行 = 1 Undo 単位 [CW-33][CR-11] |
| CW2-34 | 実行後にプレビューを自動再計算し、残った未変換を確認できる状態にする [CW-34] |
| CW2-35 | 実行中キャンセル可。処理済み分はそのまま残す [CW-35][LK-26] |
| CW2-36 | 進捗を件数と割合で表示 [CW-37][UI-09] |
| CW2-37 | 1,000 件超は確認ダイアログを出し、Undo 可能である旨を明示 [CW-38] |
| CW2-38 | 他ウインドウと同じペイン構成・リスト・ボタン配置 [CW-36] |

### ペンディング一覧との連携 [CW-40〜CW-45]

| # | 要件 |
|---|---|
| CW2-40 | スコープ ③ では対象を渡されたペンディング群に限定 [CW-40] |
| CW2-41 | 移動先ライブラリのファイル名フォーマットを出力フォーマットの候補として提示 [CW-41] |
| CW2-42 | 「変換後にライブラリのフォーマットへ適合するか」の判定列を追加 [CW-42] |
| CW2-43 | 実行後、変換されたファイルをペンディング一覧へ返して再判定。適合分は「解決済み」へ [CW-43] |
| CW2-44 | 実行後にペンディング一覧を前面に戻す [CW-44] |
| CW2-45 | スコープ ③ でもルールの追加・編集ができ、テンポラリ設定として保存される [CW-45] |

## 15.10 環境設定ウインドウ

| タブ | 内容 |
|---|---|
| 一般 | 起動時の動作、フォルダを上にまとめる [LV-03]、確認ダイアログの表示設定 |
| 表示 | 既定の表示モード、アイコンサイズ、日付書式 |
| キーボード | キーバインド割り当て [7.4 節][KB-01〜KB-05] |
| 関連付け | 拡張子とアプリ [16 章][AS-01][AS-06] |
| スキャン | フルスキャン間隔 [SY-05]、FSEvents レイテンシ [SY-07]、UI 反映バッチ閾値 [ST-11]、ゴミ箱レコード保持期間 [TR-06] |
| キャッシュ | サムネイルキャッシュ上限・クリア [IV-09] |
| データ | JSON インポート／エクスポート [17 章]、バックアップ世代数 [BK-01]、任意フォルダへの書き出し [BK-04]、復元 [BK-03] |
| 通知 | システム通知の有効／無効 [ER-33]、通知履歴の保持期間・上限 [NT-07] |
| 詳細 | ログレベル [LG2-03]、診断ログ書き出し [LG2-05]（匿名化オプション [LG2-06]）、整合性チェック [RB-02]、復旧手順の実行 [IE-16][MG-24]、展開上限 [EX-22]、Undo スタック深さ [UD-05]、操作履歴保持期間 [HS-04] |

### 実装状況（フェーズ 1 完了時点）

**一次資料は `PreferencesCategory`（`Sources/qooLibraryApp/Preferences/PreferencesView.swift`）**。
実装は **9 タブ**で、上表の 9 行とは一致しない——3 行が未実装、3 タブが表に無い。

| 実装されているタブ | 上表との対応 | 備考 |
|---|---|---|
| 一般 | 一般 | 起動時に開くフォルダ、フォルダを上にまとめる [LV-03]、表示言語、2 本指スワイプの用途、スワイプ方向の反転、すべてのウインドウが閉じたら終了 |
| 表示 | 表示 | 既定の表示モード・アイコンサイズ、表示するカラム、フォルダツリーの自動展開 |
| キーボード | キーボード | [KB-01〜KB-05]。変更可／標準（読み取り専用）の 2 セクション（§13.6）|
| **ビューア** | 関連付け | **表示名を「関連付け」から改めた**［ユーザー判断: このタブはダブルクリック／Enter で開く既定アプリを指定するだけのもので、「関連付け」は誤解を招く］。単一の拡張子一覧（組み込み／カスタムの区別は廃止）[AS-01]（12章 §12.9）|
| **圧縮／展開** | — | 上表に無い。圧縮形式・レベル・コーデック・暗号化方式、展開の安全上限 [EX-22]（9章 §9.4）|
| **スキャン** | スキャン | 定期フルスキャンの有効／無効と間隔 [SY-05]、「ボリュームを再検証」[VD-09]（2-2）。**FSEvents レイテンシ [SY-07]・UI 反映バッチ閾値 [ST-11]・ゴミ箱レコード保持期間 [TR-06] は未実装** |
| **アクセス権** | — | 上表に無い。ボリューム／フォルダへのアクセス許可の一覧と取り消し。**フルディスクアクセスが App Sandbox の制限を回避しないと実測で判明したことへの対処**（`VolumeAccessStore`）|
| キャッシュ | キャッシュ | サムネイルキャッシュ上限・クリア [IV-09]、バックグラウンド動画サムネイル生成の有効／無効（§9.6）|
| 詳細 | 詳細 | ログレベル [LG2-03]、ログサイズ、ログフォルダを Finder で表示、パス匿名化 [LG2-06]、診断情報の書き出し [LG2-05]（1-15）|
| **リセット** | 「データ」の一部 | 上表に無い。ライブラリ単位の削除 [RG-06]、JSON の書き出しと取り込み [IE-01〜IE-14]、サムネイルの一括削除 [IV-09]。詳細は下記 §15.10.1 |

**未実装のタブ**: 「通知」——`NotificationHistoryStore` に依存するため、
フェーズ 2 以降で実装する。「スキャン」は 2-2 で実装した（下記 §15.10.2）。
「データ」は独立したタブを作らず、**「リセット」タブへ統合した**（下記 §15.10.1）
——書き出し・取り込み・削除は同じ画面で順に並んでいるほうが、
「消す前に書き出す」という順序そのものが案内になる［設計判断］。

**実装済みタブの中でも未実装の項目**: 「一般」の確認ダイアログの表示設定、
「表示」の日付書式、「ビューア」の副次アプリ固定リスト [AS2-04]、「詳細」の
整合性チェック [RB-02]・復旧手順 [IE-16][MG-24]・Undo スタック深さ [UD-05]・
操作履歴保持期間 [HS-04]（いずれも永続化層または未実装の基盤に依存）。

展開上限 [EX-22] は上表では「詳細」だが、**実装では「圧縮／展開」タブに置いた**
——形式ごとのオプションと同じ場所にある方が探しやすいため、移す予定は無い。

**スライダー等の調整系設定があるタブには必ず「既定に戻す」を付ける**
［ユーザー指摘の一般原則。既定値が分からなくなるため］。

### 15.1.1 ライブラリ有効化ウインドウ [LT-01〜LT-03][LS-01][HP-05、ユーザー要望]

**実装は `LibraryEnableWindow`／`LibraryEnableModel`／`LibraryPreview`。**

ユーザー指摘:「登録済みフォルダに対してライブラリを有効化する際に、ユーザーは
その選択肢で何がどう変化するのかわからない」。テンプレート名を選ぶだけの
ダイアログを、**設定を確認・調整し、実ファイル名に当てた結果まで見てから
決める画面**へ置き換えた。

構成は**設定ウインドウと同じ 3 ペイン**［ユーザー判断］——左＝起点
（プリセット 8 種 ＋「カスタム（空）」）、中央＝設定項目 8 セクション、
右＝エディタ ＋ プレビュー。有効化前と後で同じ UI になるので覚え直しが要らず、
**エディタ 8 種をそのまま再利用できる**（同じ編集 UI を 2 つ作らない）。

| # | 決めたこと | 理由 |
|---|---|---|
| LE2-01 | **既定値の出どころは `TemplateInstantiation.draft(from:)` の 1 箇所** | この画面が見せる草案が、そのまま `register(_:draft:template:)` へ渡って DB になる。既定値を 2 箇所に書くと「見たものと登録されたものが違う」という、最も気づきにくい壊れ方をする |
| LE2-02 | 起点を変えたら草案を作り直す（編集内容は捨てる）| 別のテンプレートへ移るのは「やり直す」という意思表示。混ぜると何を基にしているのか分からなくなる |
| LE2-03 | **プレビューは走査と同じ条件で絞る** [AL-11][IF-01] | 絞らないと対象外のファイル（`メモ.txt` 等）まで未解決に数え、有効化後の走査結果と数が合わない。実機でそうなった（12 件中 4 件未解決 → 実際は 11 件中 3 件）。除いた件数も出す |
| LE2-04 | 未解決を先頭に集める［ユーザー判断］| 調整が要るのはそこなので、探させない |
| LE2-05 | ラベルは**番号ではなくグループ名**で見せる | `@labelgroup3` と言われても何のことか分からない |
| LE2-06 | 壊れたフォーマットがあってもプレビューを出す [HP-05] | 編集の途中で壊れているのは普通の状態。そこで打ち切ると直している最中に結果が見られない |
| LE2-16 | **プレビューは埋め込みメタデータを読まない** [EM-09] | プレビューは設定を変えるたびに走る。ここで最大 500 件のアーカイブを開くと、テンプレートを切り替えるだけで数秒〜数十秒待たされる（§9.9 の実測: ネットワークなら 1 件 12ms、RAR なら 51.6ms）。**そのぶん「プレビューの結果」と「有効化後の実際の値」は、メタデータを持つファイルでは食い違う**——ファイル名だけを見た結果が出る。LE2-03 が「走査と同じ条件で絞る」と定めているのと矛盾するように見えるが、あちらは**費用ゼロの拡張子フィルタ**の話で、こちらは 1 件ずつ開く話である |
| LE2-07 | 収集は上限 `AppLimits.Library.previewSampleLimit`（500）で打ち切る | 知りたいのは傾向であって全件の集計ではない。実蔵書は 1 ライブラリ 1〜5 万件になり得る [C-07] |
| LE2-13 | **フォーマットの編集は別ダイアログ**［ユーザー指摘］| 一覧・不備・プレビューと縦に積む限り、どれかの高さが変わるたびに編集欄が隠れる（3 度直して分かった）。独立ウインドウなら入力欄・予約語パレット・サンプル試行が一度に見える |
| LE2-14 | ダイアログは**入れ子で開ける**必要がある | 有効化ウインドウ自体がモーダル。`DialogWindowPresenter` はスタックで持ち、「生成待ちへ重ねない」防止だけを残す |
| LE2-15 | `VSplitView` の先頭ペインは**中身を実測して縮める** | 余った高さは先頭へ配られる。固定の上限では、セクションごとに中身の量が違うぶんどれかが必ず余る——`GeometryReader` を `background` に置いて測り、`min(実測, 上限)` を渡す |
| LE2-09 | **入力欄には必ず `.editableFieldChrome()`**［ユーザー指摘、2 度目］| `Form` の中の素の `TextField` は、値が右端に寄ったただのテキストにしか見えず、そこが入力できる場所だと気づけない |
| LE2-10 | 一覧の増減ボタンは**枠内・下端のバー**（`EditableListChrome`）| 一覧の外に並べると、スクロールでボタンが一覧から離れて流れる。macOS のシステム設定と同じ形にする |
| LE2-11 | 一覧の高さは**内容にあわせて伸ばさない** | 伸びると下の編集欄がスクロールの外へ出て「編集する手段が見当たらない」状態になる |
| LE2-12 | **可変高さの領域を 2 つ持たない** | 固定サイズのウインドウで、下部の不備メッセージが増えると上の編集ペインが黙って潰れた。不備一覧は高さを固定して中でスクロールさせる |
| LE2-08 | 型名を編集したら**専用の非プリセット型へ分岐する** [LT-05] | `libraryType` の行は同じプリセットから登録した複数のライブラリで共有される。そのまま書き換えると他のライブラリの `@librarytype` の照合値まで変わる |

**白紙（カスタム）は不備を明示して止める** [LT-02]。フォーマットを 1 本も
持たないので全件が未解決になり、「ライブラリタイプ名を入力してください」
「有効なファイル名フォーマットが 1 つもありません」が下部に出て、
「有効にする」が押せない——**何を埋めればよいかが画面から分かる**。

### 15.1.2 巻数の確認ダイアログ [EM-30〜EM-35]

`ComicInfo.xml` の `Number` と `Volume` が**両方あり、かつ違う数を指す**ファイルに
ついて、どちらを巻数として採るかを聞く（05章 §5.7）。

**スキャンは止めない** [EM-31]。衝突は `hasVolumeConflict` に記録して走り切り、
完了後に通知（強度 2）で件数だけ伝える。押すとこのダイアログが開く。
未解決ファイルの整理 [UR2-01][UR2-02] と同じ形で、**日常的に走る再スキャンが
毎回ダイアログで止まらない**ようにする。

| # | 決めたこと | 理由 |
|---|---|---|
| VC-01 | ファイル名・`Number` の値・`Volume` の値を**横に並べて**見せる | どちらを選ぶかはファイル名との突き合わせで決まる。`第3巻` というファイル名なら `3` を指す側が正しい |
| VC-02 | 1 件ずつ選ばせる。**「以降すべてに適用」を選べる** [EM-33] | 同じツールで付けたファイルは同じ流儀になるので、1 回答えれば残りも決まることが多い |
| VC-03 | 「以降すべてに適用」を選んだら**ライブラリ設定を書き換える** [EM-30] | 次回のスキャンでも同じ判断が効く。設定として見えるので、後から変えられる |
| VC-04 | **後回しにできる。**ライブラリの設定ウインドウからいつでも開ける [EM-35] | 取り込み直後にすべてを決めさせない。件数が多いときは日を分けてよい |
| VC-05 | 判断が済むまで、そのファイルの巻数は**ファイル名から抽出した値**を使う [EM-34] | どちらか分からない値を採るより、下の層の値のほうが確からしい |
| VC-06 | ダイアログを閉じても、答えなかったぶんは記録に残る | 「全部答えないと閉じられない」形にしない |

**通知の強度は 2（シート）** [ER-02]。強度 4（一時通知）は提示先がまだ無く、
ログだけになって届かない——走査結果の通知（孤立・未解決）と同じ判断。

### 15.10.2 「スキャン」タブ [SY-05][SY-06][VD-09]

**取りこぼしへの備えを、利用者が調整できる場所。** 変更の検知は完全ではない:

- 他のマシンが加えた変更は FSEvents に届かない（§8.11.5 の実測）
- 履歴を持たないボリューム（**ネットワーク共有はすべて**、§10.1.0 の実測）では
  非起動中の変更を再生できない

そこで定期フルスキャンを最終安全網に置く [SY-05]。要件が「間隔は変更可能、
無効化も可能」と明記しているのは、**大きな蔵書やネットワーク越しでは走査
そのものが重い**ため——止める手段が無いと、起動のたびに数分待たされる人が出る。

| 項目 | 要件 | 備考 |
|---|---|---|
| 定期フルスキャンの有効／無効 | SY-05 | 既定は有効 |
| 間隔（1〜90 日）| SY-05 | 既定 7 日。無効にすると `nil` として扱う |
| ボリュームを再検証 | VD-09 | 着脱の検知に失敗しても人手で回復できる経路。`MountTable` から始まるので、相手が落ちていても固まらない |
| 既定に戻す | — | ［ユーザー指摘: 調整系の設定には必ず付けること］|

### 15.10.1 「リセット」タブ [ユーザー要望、要件定義書には無い]

**実装は `ResetPreferencesTab`／`LibraryBackupAction`（`Sources/qooLibraryApp/`）。
JSON の中身と取り込みの規則は 07章 §7.5 が正。**

構成は上から **書き出し／取り込み → ライブラリの削除 → サムネイルの一括削除**。
**この順序自体が仕様である**［ユーザーからの制約: 一括削除より先に
エクスポート／インポートを実装すること］——「消す前に書き出す」導線を
画面の並びで示す。

| # | 決めたこと | 理由 |
|---|---|---|
| RS-01 | **DB の側から一覧する。** フォルダツリーの登録行ではなく `library` テーブルを直接列挙する | ツリー経由の「ライブラリ機能を無効にする」は登録行から辿るので、登録が先に消えた場合や、登録と対応しない行が DB に残った場合に届かない。**このタブが唯一の片付け手段になる** |
| RS-02 | 縮退状態（オフライン・ゴミ箱・消失 [1-17]）でも削除できる | 削除は DB の行を消すだけでボリュームにも実ファイルにも触れない。**縮退状態こそ片付けたい場面**で手段が消えてはならない——無効化をオンライン条件で囲って「外付けを失うと二度と片付けられない」欠陥を作った前例がある [`LibraryMenuVisibility`] |
| RS-03 | 登録フォルダに対応が無い行には印を出す | ツリーからは一切辿れない行なので、何を消しているのか分からなくなる |
| RS-04 | 削除の確認で**失うもの**を明示する [RG-06] | `unregister` は `keepLabels` をまだ見ずに連鎖削除する。選ばせられない以上、せめて何が消えるかを言う（ラベル保管庫 2-11 が入ったら選択に変える）|
| RS-05 | 取り込める対象が 1 件も無いときは**確認ダイアログを出さない** | 押せない決定ボタンを見せるのは、承認を求める形をしているだけで何も選ばせていない（実機で実際にそうなり、直した）。理由を通知で言って終わる |
| RS-06 | サムネイルは `covers/` と Quick Look 用の書き出しの**両方**を消す | どちらも「表示のために作った派生物」で、片方だけ残ると「消したのに古い絵が出る」 |
| RS-07 | **DB 全体を一度に消すボタンは作らない** | 世代管理と復元 UI [BK-01〜BK-04] が揃うまでは、取り返しのつかない操作を 1 つの画面に置かない |

## 15.11 通知履歴ウインドウ

2 ペイン。左＝区分（すべて／エラー／警告／情報）と対象ライブラリ、右＝一覧。

| # | 要件 |
|---|---|
| NW2-01 | 列: 日時 / 区分 / 対象 / 本文 [NW-02] |
| NW2-02 | 未読は太字。開いた時点で既読。「すべて既読にする」[NW-03] |
| NW2-03 | 行選択で詳細と関連画面への遷移ボタン [NW-04][NT-05] |
| NW2-04 | 期間・キーワードで絞り込み [NW-05] |
| NW2-05 | 選択削除・全削除 [NW-06] |
| NW2-06 | CSV エクスポート [NW-07] |
| NW2-07 | UI 共通要件に従う [NW-08] |

## 15.12 初回セットアップウィザード

| ステップ | 画面 |
|---|---|
| 1 ようこそ | 役割と、ライブラリ／テンポラリの考え方を 1 画面で |
| 2 フルディスクアクセス | 必要性の説明 + システム設定を開く導線 + 付与状態の検出 [SB-03][B-20][B-21] |
| 3 アプリの関連付け | コミック拡張子の既定アプリ。`qooViewer` があれば既定 [AS-03][AS-05] |
| 4 ライブラリ登録 | フォルダ選択 + テンプレート選択 + FS 適合検証 [RG-01][LT-03][FS-01〜FS-05] |
| 5 設定のプレビュー | 実フォルダのファイル名を解析した結果を `FieldBreakdownView` で提示 [OB-04][HP-09] |
| 6 初回スキャン | バックグラウンド実行。ここで完了できる [OB-06][ST-15] |
| 7 未解決ファイル | 0 件なら表示しない [OB-07]。500 件超なら「フォーマット追加して再マッチング」を最初に提示 [OB-08] |

要件 OB-01〜OB-10 の実装詳細は 12 章 §12.8 を参照。

## 15.13 操作履歴ウインドウ [18.1 節]

| # | 要件 |
|---|---|
| OH-01 | すべての破壊的操作を日時・対象・内容付きで表示 [HS-01] |
| OH-02 | 閲覧・検索・CSV エクスポート [HS-02] |
| OH-03 | 自動リネーム・自動ラベル付与の結果も表示 [HS-03][AR2-07] |
| OH-04 | 変換リネームは変換前後の名前を確認できる [CR-53] |
| OH-05 | 保持期間は既定 90 日、環境設定で変更可 [HS-04] |
| OH-06 | 通知履歴からのリンク先として機能する [NT-04] |
