# 17. 実装ロードマップ

対応要件: 20 章（20.1〜20.4）/ DP-01〜DP-09 / R-01〜R-16

---

## 17.1 全体像

```
フェーズ0  基盤検証         （T-13 / T-12 / ゴールデンサンプル収集開始）
   ↓
フェーズ1  ファイルマネージャー （20.1 節）→ Finder の代替として日常使用できる
   ↓
フェーズ2  ライブラリマネージャー（20.2 節）→ ライブラリ管理が実用レベル
   ↓
フェーズ3  テンポラリフォルダ   （20.3 節）→ 取り込み〜投入のワークフロー完結
```

## 17.2 フェーズ 0: 基盤検証（実装前）

| # | 作業 | 完了条件 |
|---|---|---|
| 0-1 | libarchive / UnRAR の組み込み検証 [T-13] | サンドボックス下のダミーアプリで zip / rar を一覧・展開できる。ビルドスクリプトが再現可能 |
| 0-2 | RAR カバレッジ差分の測定 [T-12] | 手持ちの `.cbr` で UnRAR と libarchive の成功率を比較した表が残る |
| 0-3 | **ゴールデンサンプルの収集開始** [MT-27][DP-09] | 手持ちのファイル名 500 件以上を `Tests/GoldenDataset/private` に蓄積 |
| 0-4 | プロジェクト骨格の作成 | SwiftPM ターゲット構成、CI（build / static-checks）が緑 |

> フェーズ 0 の 0-3 は他の作業と並行して継続する。パーサ実装（2-3）の着手時点で検証付きの開発ができる状態にすることが目的 [DP-09]。

## 17.3 フェーズ 1: ファイルマネージャー [20.1 節]

> **完了（2026-08-18、v0.10）。1-1〜1-17 すべて実装済み。** 各項目の実装内容・
> 設計判断・実機検証の記録は CLAUDE.md §0 が持つ（このロードマップは計画であり
> 進捗表ではない）。残っている未検証項目は §17.6 にまとめてある。

| # | 内容 | 主な要件 | 依存 |
|---|---|---|---|
| 1-1 | プロジェクト基盤（レイヤ構成、デザイントークン、共通コンポーネント）| A-01〜A-04 / UI-01〜UI-06 | 0-4 |
| 1-2 | サンドボックス + Security-Scoped Bookmark 基盤、FS 適合検証 | SB-01〜SB-05 / FS-01〜FS-09 | 1-1 |
| 1-3 | メインウインドウ 3 ペイン、タブ・複数ウインドウ。**状態の 3 分類を最初から分離** | MW-01〜MW-04 / ST-20〜ST-27 / UI-02 / UI-08 | 1-1 |
| 1-4 | フォルダツリー（ボリューム／テンポラリ／ライブラリの 3 グループ）| LP-01〜LP-08 | 1-2, 1-3 |
| 1-5 | 基本ファイル操作・衝突処理。**`FileOperationService` への集約** | FM-01〜FM-13 / FO-01〜FO-03 | 1-2 |
| 1-6 | ドラッグ＆ドロップ | DD-01〜DD-07 | 1-4, 1-5 |
| 1-7 | 圧縮・展開、文字化け対策、**展開時のセキュリティ**、ライセンス表記一式 | AR-01〜AR-23 / EX-01〜EX-34 / LC-20〜LC-27 | 0-1, 1-5 |
| 1-8 | キーボードショートカット | KB-01〜KB-05 | 1-3 |
| 1-9 | リスト表示・アイコン表示、サムネイル生成 | LV-01〜LV-03 / IV-01 / IV-08 / IV-09 / PF-10 / PF-11 | 1-3, 1-7 |
| 1-10 | 右ペイン（詳細情報表示）| DT-01〜DT-07 / DT-10 | 1-3 |
| 1-11 | **Undo 基盤（ファイル操作のみ）** | UD-01〜UD-11 / HS-01〜HS-04 | 1-5 |
| 1-12 | 環境設定（一般・表示・キーボード・関連付け・通知）| 15.10 節 / AS-01〜AS-07 | 1-8 |
| 1-12b | **エラー処理と通知の共通基盤**、通知履歴ウインドウ | ER-01〜ER-34 / NT-01〜NT-08 / NW-01〜NW-08 | 1-1 |

> **通知履歴（NT-01〜08 / NW-01〜08）は 2026-08 に完了した**（フェーズ 2 の
> 途中で。DB が要るためフェーズ 1 では作れなかった）。ステータスバーの
> 未読バッジ [NT-02]、2 ペインの履歴ウインドウ [15章 §15.11]、環境設定
> 「通知」タブ [NT-07]、走査結果の記録 [OR2-05][UR2-02] まで。
> **これで「自動走査で孤立が起きても誰も知らない」状態が解けた**——
> 強度 4 の提示先が無かったのがフェーズ 1 での制約で、バッジができて初めて
> 成立する。**残るのは ER-10〜16（`BatchNotificationSession`）と
> ER-30〜34（システム通知）**［ユーザー判断で今回の範囲外］。
| 1-13 | ライブラリ／テンポラリフォルダの登録・削除（この時点ではエイリアス相当）| RG-01〜RG-08 | 1-2, 1-4 |
| 1-14 | Quick Look 連携、完全削除、サムネイル表示制御 | QL-01〜QL-10 / FM-14〜FM-18 / DS-01〜DS-07 | 1-9 |
| 1-15 | 診断ログ（ファイル出力・ローテーション・書き出し・匿名化・環境設定「詳細」タブ・ヘルプメニュー）| LG2-01〜LG2-08 | 1-1 |
| 1-16 | Finder 対比監査で「技術的には実装できるが未着手」と分類した項目の実装 | —（ユーザー要望、要件定義書には無い）| 1-14 |
| 1-16b | **ネットワークボリューム・File Provider の扱い**（ゴミ箱・同一性・変更検知・実行モデル・性能縮退）。**完了。§8.11 が実測結果と決定事項を、§8.11.14 が実装状況を、§8.11.15 が実 NAS で見つけた破損 3 件（NV-102〜104）を持つ。**残るのは NV-101（スリープ復帰、低優先）と第三者クラウドでの実測のみ | FM-04 / TR2-01 / UD-08 / SY-01 / FS-06 相当 | 1-16 |
| 1-17 | **登録フォルダの縮退状態**（オフライン／ゴミ箱／消失の区別、§8.7.1）| SB-05 / FS-08 / LP-04 / VD-02 / VD-11 / RG-03 / RG-04 | 1-13 |

### フェーズ 1 の完了条件（DoD）

- Finder の代替として日常的に使える。ラベル機能は未実装 [20.1 完了条件]
- `FileOperationService` 以外から `FileManager` の変更系 API を呼ぶ箇所が **0**（静的検査 B-10 が緑）[FO-02][DP-05]
- Undo / Redo がすべてのファイル操作で動作する [DP-01][R-08]
- （DP-07 はフェーズ 2 へ移動。フェーズ 1 には DB 自体が存在しないため）
- エラー・通知が `NotificationRouter` に一本化されている [DP-08]
- 状態の 3 分類が実装上分離されている [ST-20]
- 登録フォルダの状態がオフライン／ゴミ箱／消失を区別し、**いずれの場合も登録レコードを自動削除しない** [SB-05][R-01]（§8.7.1）
- 展開セキュリティのテストが全件パス [EX-10〜EX-24]
- `LICENSE` / `README` / `THIRD-PARTY-NOTICES.md` が LC-20〜LC-27 を満たす

### この段階で必ず守る設計制約

| # | 制約 | 破ったときの被害 |
|---|---|---|
| DP-01 | Undo 基盤を Command パターンで設計する | 後付けは困難。第 2 段階で全面改修 [R-08] |
| DP-05 | `FileOperationService` への集約を徹底する | 第 2 段階の自己変更識別が成立しない [R-13] |
| ~~DP-07~~ | フェーズ 2 の 2-1 へ移動。DB が無い段階では確定できない | — |
| DP-08 | エラー・通知の共通基盤を作る | 機能ごとの個別実装は後から統一できない |
| DP-02 | フォルダ登録に Security-Scoped Bookmark を使い、DB スキーマは第 2 段階を見越す | 再設計 |
| — | 登録フォルダの縮退状態（§8.7.1）を DB 導入前に確定する [設計判断] | DB とラベルが乗った後は、オフラインと消失の誤判定がそのままラベル紐づけの喪失になる [SB-05][R-01] |

## 17.4 フェーズ 2: ライブラリマネージャー [20.2 節]

| # | 内容 | 主な要件 | 依存 |
|---|---|---|---|
| 2-1 | データモデル、ファイル同一性判定、**スキーマ移行の器の導入**、接続構成の確定 [DP-07] | 6 章 / ID-01〜ID-08 / MG-01〜MG-04 / ST-01〜ST-07 | 1-13 |

> **同一性の判定を 2026-08 に改訂した [ID-05][ID-09〜ID-12]。** ユーザー指摘
> 「**過度な同一性のチェックは逆に使いにくい**——同じパス＋同じ名前だが中身が
> 異なるファイルへの差し替え（スキャンした本から電子版へ、など）は頻繁に
> 起きる」を受けたもの。改訂前の ID-05 は、その差し替えのたびに孤立を作り、
> 手で付けたラベル・評価・タイトルを失っていた——[ID-03]①② はどちらも
> **サイズの一致**を条件にしており、差し替えは必ずサイズが変わるため。
> **要件が守ろうとしたケース（「Finder での上書き保存」「再ダウンロード」）
> そのものを弾いていた。**
>
> かといって「名前が同じなら黙って同じもの」も危うい（`第01巻.cbz` は複数の
> シリーズに存在しうる）ので、**走り切ってから一括で確認する**形にした
> ——巻数の確認 [EM-30〜35] と同じ。**保留のための列もテーブルも要らない**
> ことが分かったのが実装上の勘所で、当初は「孤立していて同じ名前の生きている
> 行がある」という状態がそのまま確認待ちを表す、として `identityRejection`
> （v5、「別物だと決めた」を覚える）だけを新設した。
> **［訂正、2026-08］その導出は誤りだった** ——無関係な同名ファイルと
> 区別が付かず、片方を消しただけでもう片方の評価・ラベルごと行が消える
> [ID3-08]。確認待ちは状態ではなく**出来事**なので `identityPending`（v7）へ
> 明示的に記録する。詳細は 10章 §10.4.0。**あわせて OR-02 / OR-03 を撤回した**
> ——結び直しの手段を「見つからないファイル」の画面から外し、そちらは
> 一覧と削除だけにした。詳細は 15章 §15.7.1、経緯は CLAUDE.md の同名節
>
> **さらに [ID-13〜ID-15] を足した（2026-08）。** 「上書きや移動でダイアログが
> 出るのを煩わしく感じる人がいる」というユーザー要望を受け、**どこまでを
> 黙って同じファイルとみなすかをライブラリ単位で選べる**ようにした。
> ③を **③a 同じ場所での差し替え**と **③b 別の場所**に割り、3 段階
> （必ず確認／同じ場所なら確認しない／名前が同じなら確認しない）から選ぶ。
> **既定は「名前が同じなら確認しない」**なので、既定のままでは上の確認
> ダイアログは現れない——それでも実装を残すのは、選べるようにするには
> 受け皿が要るため。判断は `IdentityMatchPolicy.acceptsAutomatically(_:)`
> の 1 箇所だけが持つ。表は 10章 §10.4.1。
| 2-2 | FSEvents 監視・差分／フルスキャン、**自己変更の識別**、ボリューム着脱検知（`VolumeMonitor` 本実装）、**一括操作の部分成功の保持** | SY-01〜SY-12 / VD-01〜VD-11 / FO-10〜FO-24 / RB-04 | 2-1, 1-17 |

> **2-2 は完了（2026-08）。** `LibraryWatcher` / `VolumeMonitor` をアプリへ
> 結線し、調整役 `LibrarySyncCoordinator`（10章 §10.6）を置いた。着手前に
> 実測で 2 つの前提を確定させている——**`NSWorkspace` の着脱通知は
> ネットワークボリュームでも届く**（NV-43 の懸念は当たらなかった、§10.2.0）、
> **FSEvents の履歴はネットワークでは 1 件も再生されないのにエラーも
> フラグも出ない**（§10.1.0）。後者のせいで、差分の起点は
> `(eventID, デバイス UUID)` の組でしか保存できない形にしてある [WA-10]。
> **一括操作の部分成功の保持 [RB-04][ER-13] は既に済んでいる**——フェーズ1
> 完了後の総点検で `PartialTransferFailure` として実装済み（`trash` /
> `setLocked` も同様）。着手時に確かめたので、下記の注意書きは履歴として残す。
| 2-3 | **ファイル名フォーマットのパーサ**（Domain 層として先行実装）| 9 章 / MT-20〜MT-28 | 0-3 |
| 2-4 | シリーズ抽出・巻数 | 10 章 | 2-3 |
| 2-5 | ライブラリタイプ・テンプレート | 11 章 / LT-01〜LT-06 | 2-3, 2-4 |
| 2-6 | ラベルグループ・ラベルの管理、配色 | LG-01〜LG-07 / LB-01〜LB-07 / CO-01〜CO-07 / MT-10〜MT-16 | 2-1 |
| 2-7 | 自動ラベル付与、再計算ポリシー | AL-01〜AL-34 / AT-01〜AT-04 / RC-01〜RC-16 | 2-3, 2-6 |

> **2-7 は `AL-30〜AL-34`（未解決ファイルの救済フロー）まで完了（2026-08）。**
> 付与そのもの [AL-01〜29] と `RC-01〜05`（`replaceAutoLabels` が `manual` /
> `manuallyRemoved` を守る）は以前から実装済み。**残るは `RC-06`（手動編集を
> 破棄して完全再計算）と `RC-10〜16`（リネーム起点の再計算ダイアログ）だけ**
> ——どちらも未解決ファイルとは独立した「リネームしたときの見せ方」の話。
| 2-8 | ラベルフィルタ、ピン留め、評価フィルタ | LF-01〜LF-14 / PN-01〜PN-06 / RT-01〜RT-03 / PF-12 | 2-6, 2-7 |
| 2-9 | フォルダ表示モード／ライブラリ表示モード、ブックフォルダ | VM-01〜VM-23 / IF-01〜IF-18 / LV-04 / IV-02〜IV-07 | 2-8 |
| 2-10 | 右ペインのラベル設定・評価・カバー画像 | RL-01〜RL-07 / RA-01〜RA-08 / CV-01〜CV-08 / RP-10〜RP-12 | 2-9 |
| 2-11 | ラベル／ファイル／フォルダアーカイブ | LA-01〜LA-09 / FA-01〜FA-17 / FDA-01〜FDA-05 | 2-9 |

> **2-11 は完了（2026-08）、§15.4 の整理ウインドウ（FAW-01〜05）も同時に閉じた。**
> ラベル側 [LA-01〜09] は 2-6 / 2-13 で済んでおり、今回はファイル・フォルダ側。
> **着手前の調査で走査の穴を 1 件掘り当てた**——`LibraryEnumerator` が隠し名の
> 項目を一律で飛ばしており `.qooarchive` へ降りていなかった（**コメントだけが
> 「対象に含める [SY-10]」と、存在しない保護を主張していた**）。塞がないまま
> 保管庫を作ると**保管庫へ移しただけで孤立レコードになる**。
> 新設は ①`VaultPath`（`QooKit`、保管庫のパスの唯一の実装）②`FileVault`
> （実ファイル・サイドカー [FA-14]・空フォルダの後始末 [FA-06][FA-10]）
> ③リポジトリの `archivedFiles` / `archivedFileCounts` / `setArchived` /
> `filesUnder` ④`SetFileArchivedCommand`（**入れると出すを 1 つのコマンドに**）と
> `ArchiveFolderCommand` ⑤`VaultEditorModel`（右ペイン [DT-11]）
> ⑥`FileVaultModel` / `FileVaultWindow` / `LibraryMenuVisibility.Item.fileVault`。
> **`isArchived` は観測した位置が決める**——`FileSnapshot` の計算プロパティに
> したので、外部で `.qooarchive` へ出し入れされても次の走査で追随する。
> **削除は実ファイルをゴミ箱へ送り記録も消す**［ユーザー判断］——記録だけ消すと
> `.qooarchive` は走査の対象 [SY-10] なので次の走査でラベルを失った状態で
> 復活する。詳細は 08章 §8.8・07章 §7.5・15章 §15.4、経緯は CLAUDE.md
> 「ファイル保管庫」節。**実機検証済み**——検証中に 3 件見つけて直した
> （選択すると行の 2 行目が読めない／**ゴミ箱を持たない場所で削除が丸ごと
> 失敗する** [NV4-01]——共有だけでなく**一度も何も捨てていない外付け**でも
> 起きる／削除でサイドカーが置き去りになる）。加えて `LabelVaultWindow` を
> 写した際に**着脱へ追随する配線を写しきれていなかった**（§15.7 が 2-14 で
> 塞いだのと同じ穴）。
| 2-12 | ライブラリの設定ウインドウ、ラベルグループ編集ウインドウ、**フォーマット編集支援** | LS-01〜LS-03 / LE-01〜LE-12 / HP-01〜HP-09 | 2-5, 2-6 |
| 2-13 | ~~ラベル保管庫~~／ファイル保管庫の整理ウインドウ | ~~LAW-01〜LAW-03~~（撤回）/ FAW-01〜FAW-05 | 2-11 |

> **2-13 は完了（2026-08）。** ラベル側 [LAW-01〜03] は 2-11 より先に、
> ファイル側 [FAW-01〜05] は 2-11 と同時に閉じた（15章 §15.3 / §15.4）。
>
> **［2026-09-03、v3 ステージ 11］ラベル側は概念ごと撤回した** [§19.12]
> ——「保管庫」をやめ、生きている実体を失ったラベルが**自動的に非表示**に
> なる導出モデルへ置き換えた。ウインドウ・`LabelVaultModel`・
> `archivedLabelCounts()` は削除済み。**ファイル側 [FAW-01〜05] は存続する。**
> 以下はラベル側の当時の記録で、同じ形の 2 ペイン整理ウインドウを作るときの
> 前例として残す。

> **2-13 のラベル保管庫は完了（2026-08）。** `LAW-01〜03` の 2 ペイン
> （左＝ライブラリ一覧・保管庫が空ならグレーアウト／右＝アーカイブ済みラベルを
> グループごとに整理）。**依存として挙げた 2-11 より先に着手した**——2-6 で
> 保管庫へ送る手段ができたのに、送った先を見て戻す画面が無かったため。
> **新しいコマンドは 1 つも書いていない**（戻す＝`SetLabelArchivedCommand`、
> 削除＝`DeleteLabelsCommand`）。触れるのは LAW の 3 つだけで、改名・統合・色・
> ピンは 2-12 のラベル編集ウインドウへ飛ばす［ユーザー判断］。新設は
> ①`LabelRepository.archivedLabelCounts()`（左ペインのグレーアウト用、1 問い合わせ）
> ②`LabelVaultModel` ③`LabelVaultWindow` ④`LibraryMenuVisibility.Item.labelVault`。
> ~~**ファイル保管庫（FAW-01〜05）は 2-11 のまま**~~ → **完了**（2026-08、2-11 と同時）。
> 詳細は 15章 §15.4。詳細は 15章 §15.3、経緯は CLAUDE.md
> 「ラベル保管庫の整理ウインドウ」節。**実機検証済み**——3 つの入口・区画ごとの整理・
> 区画内での並べ替え・全角での検索・LAW-01〜03・⌘Z が同じ行 ID へ戻すこと・
> オフラインでの読み書きまで確認し、欠陥は出ていない
| 2-14 | 未解決ファイル／孤立ファイルの整理ウインドウ | UR-01〜UR-06 / OR-01〜OR-05 | 2-7, 2-2 |

> **2-14 の孤立側は完了（2026-08）。** `OR-01〜04` の 2 ペイン（左＝ライブラリ
> 一覧・オフラインはグレーアウト／右＝孤立レコード）。**依存として挙げた 2-7 は
> 未解決側（§15.6）の前提であって、孤立側には要らない**——`managedFile.state ==
> .orphaned` は走査が既に書いており、2-2 で孤立が日常的に発生するのに件数しか
> 分からない状態を、2-7 を待たずに解消できる。新設は ①リポジトリの
> `orphanedFiles` / `orphanedFileCounts` / `identity(of:)` / `reattachOrphan` /
> `deleteFiles` / `fileSnapshots` / `restoreFiles` ②`ManagedFileSnapshot`
> （全列＋ラベル紐づけ。往復テストで写し漏れを捕まえる）③`OrphanCommands` の
> 2 コマンド ④`OrphanCleanupModel` ⑤ウインドウと `LibraryMenuVisibility.Item
> .orphanCleanup`。**再紐づけは候補側のレコードを消して孤立側を生かす**
> ［ユーザー判断］——A のラベル・評価・手動タイトルを引き継ぐため [ID-04]。
> **記録と実装の食い違いを 1 件訂正した**——`managedFile` も `AUTOINCREMENT`
> だった［実測］ので、削除しても同じ行 ID へ復元できる（07章 §7.3）。
> **既存の欠陥も 1 件直した**——`state` が変わったときにラベルの非正規化件数
> [DB-02] を数え直しておらず、走査が孤立にした後もゴミ箱へ入れた後も件数が
> 古いまま残っていた。**残るは OR2-05（通知履歴のバッジ [NT-06]）と未解決側
> （§15.6、2-7 の記録側が前提）。** 詳細は 15章 §15.7、経緯は CLAUDE.md
> 「孤立ファイルの整理ウインドウ」節
> **2-14 の未解決側は完了（2026-08）。** `UR-01〜UR-06` の 3 ペイン（左＝
> ライブラリ一覧／中央＝未解決ファイル／右＝選択したファイルのラベル）。
> **前提だった 2-7 の記録側も同時に実装した**——`unresolvedFile` テーブルは
> v1 から完成していて（`isIgnored` 込み）、欠けていたのは書き手と UI だけ
> だった（2-12 の設定 API・2-10 のタイトル列に続いて 3 度目。**「無い」と
> 判断する前にスキーマを読むこと**）。新設は ①リポジトリの `syncUnresolved` /
> `unresolvedFiles` / `unresolvedFileCounts` / `setUnresolvedIgnored`
> ②`ScanEngine.rematchUnresolved`（[AL-34]。**実ファイルを列挙し直さない**）
> ③`UnresolvedFileModel` ④`SetUnresolvedIgnoredCommand` ⑤ウインドウと
> `LibraryMenuVisibility.Item.unresolvedFiles`。**ラベルの付け外し [UR-03][UR-06]
> は右ペイン（インスペクタ）と同じ実装を共有する**——三状態のチェックボックス
> [RP-02] が「複数選択して同じラベルを一括で付ける」[AL-32] そのものなので、
> そのために `LabelEditorModel.load(rows:)`（実体を stat しない経路）を足した。
> **無視フラグは名前が変わったら解く**［ユーザー判断］——「この名前は当たらない」
> という判断なので、名前が変われば前提が消える。`isIgnored` は再生成不可能
> [MG-22] なので JSON バックアップにも含めた。**残るは UR2-05（最も近い
> フォーマットの推定）と UR2-07（ゴールデンテストへの書き出し、[MT-32] の
> 層 0 と衝突するため実装しない）。** 詳細は 15章 §15.6 と 07章 §7.5、
> 経緯は CLAUDE.md「未解決ファイルの整理ウインドウ」節
| 2-15 | Undo の DB 操作対応 | UD-03 の DB 系 | 1-11, 2-7 |
| 2-16 | JSON インポート／エクスポート、自動バックアップ | IE-01〜IE-16 / BK-01〜BK-05 / MG-20〜MG-25 | 2-1 |
| 2-19 | **埋め込みメタデータの読み取り**（`ComicInfo.xml` / EPUB / PDF）| EM-01〜EM-63 | 2-2, 2-3 |
| 2-17 | 初回セットアップウィザード | OB-01〜OB-10 | 2-5, 2-7, 2-14 |
| 2-18 | 重複ファイルの比較・削除、プリセット差分適用 | DU-01〜DU-29 / LT-10〜LT-17 | 2-9, 2-5 |

> **2-18 の重複ファイル側（DU-01〜DU-29）は完了（2026-08）。**
> **プリセット差分適用（LT-10〜LT-17）は未着手**——同じ番号に入っているが
> 独立した話で、2-5 の残り（ユーザー定義テンプレート）とまとめるほうが自然。
>
> **5 度目の「スキーマはあるが書き手と UI が無い」**だった——`duplicateGrouping`
> 列・`isDuplicateRepresentativePinned`・`pageCount`・`firstImageWidth/Height` は
> v1 から存在し、`FileQuery.duplicatesOnly` も宣言済みで、`register` が
> `"off"` をハードコードしていた。**新しい移行は `v6_duplicateTitleKey` の 1 つ**
> （正規化済みタイトル、再生成可能）。
>
> 実装の勘所: ①グループ化は**SQL の窓関数**で、代表の決定順は
> `DuplicateSelection.precedes` と 1 対 1 に対応させる（自然順は専用の照合
> `QooDatabase.naturalOrder` が Swift と同じ `localizedStandardCompare` を呼ぶ）
> ②**タイトルの無い行は組を作らない**——`PARTITION BY` の NULL は同じ区画に
> なるので、問い合わせを 2 枝に割って構造で守る ③総数は `COUNT(*) OVER ()` で
> 同じ走査に畳む（別に数えると窓関数がページごとに 2 回走り、実測 199 ms が
> 298 ms になっていた）④比較・削除は**新しいコマンド型を作らず**既存の
> 5 つを `CompositeCommand` で束ねる。
>
> **実測（5 万件・重複 1 万組）**: 素の一覧 6.6 ms / 畳む 199 ms。
> 30 倍だが**既定で無効** [DU-01] なので、費用を払うのは有効にした利用者だけ。
> 窓関数は区画全体を見るため `LIMIT` を先に効かせられず、これは構造的な費用。
>
> 詳細は 12章 §12.6・14章 §14.3・15章 §15.14、経緯は CLAUDE.md「重複ファイル」節。
> **実機検証済み**——規則ごとに答えが変わる標本（12 頁/1600×2400 対 40 頁/400×600）で
> 「ページ数が最多」が測った値を使うことまで確かめ、**欠陥は出ていない**。

### フェーズ 2 の順序に関する注意

| # | 注意点 |
|---|---|
| DP-06 | **2-1 の最初にスキーマ移行の器（`DatabaseMigrator`）を導入する**。データが溜まってからの後付けは移行そのものがリスク [MG-01][MG-02][R-14] |
| DP-03 | 2-3 のパーサは UI から独立した純粋関数として早期に実装し、ゴールデンテストで固めてから UI に接続する |
| — | 2-3 / 2-4 は 2-1 / 2-2 と並行できる（`QooKit` は永続化に依存しないため）。並行実装を推奨する |
| — | 2-16 は 2-1 完了後すぐ着手してよい。MG-23 の網羅性検証を早く回すほど後戻りが小さい |
| — | **1-16b は 1-17 より先に行う。** ネットワークボリューム上では「ゴミ箱に入れる」が成立しない可能性があり、成立しない場合に何が起きるかを確定させないまま縮退状態（1-17）を設計すると、`.offline` の扱いと取り違えるおそれがある |
| — | **1-16b は実測済み。§8.11 の [実測]／[普遍]／[依存]／[文献] の札を必ず確認してから設計すること。** 最重要の結論: ①**ファイル同一性はサーバ実装で成否が分かれ、`volumeUUIDString` は SMB 3 系統すべてで nil**（§8.11.3）→ 2-1 のファイル同一性 [ID-01〜ID-08] は**ネットワーク上では成立しない前提**で設計する ②**能力フラグは偽陽性・偽陰性の両方を出す**（Windows は `supportsPersistentIDs=×` なのに実際は完全に安定）→ フラグで分岐しない ③**協調スレッドプールでブロッキング I/O をすると全 async 処理が停止する**（実測）。NFS では永久に |
| — | **1-16b は 2-2 の設計にも直結する。** 変更検知は実測で確定した（§8.11.5）——FSEvents は**この Mac のカーネルを通った変更**なら SMB 上でも届き、**別マシンの変更**は届かないがディレクトリ mtime のポーリングが 1 秒以内に拾う。`NSWorkspace` のボリューム通知がネットワークを対象外にするか（NV-43）は**未検証のまま**で、VD-01〜VD-06 の着脱検知の手段は 2-2 着手時に確かめること |
| — | **2-2 は 1-17 で確定した縮退状態（§8.7.1）を土台にする。** 1-17 は着脱検知を持たないため「ツリー更新時にマウント一覧と突き合わせる」受動的な判定に留めており、2-2 で `NSWorkspace` のボリューム通知（VD-01〜VD-06）に置き換える。状態モデルと判定順序は変えず、遷移の駆動方法だけを差し替えること |
| — | **2-12 は第 1 段を先行実装済み（2026-08）。** 依存として挙げた 2-5 / 2-6 の UI より先に着手した——テンプレートは登録時に一度写されるだけの雛形 [LT-03] なのに、**写された設定を触る経路がまったく無く、選び直すにはラベルと評価を捨てるしかない**状態だったため。実装済みは設定の読み書き API（`LibraryRepository.settingsDraft` / `updateSettings`）と 8 セクションの設定ウインドウ。詳細と不変条件は §15.1「編集の経路と不変条件」、経緯は CLAUDE.md「ライブラリ設定ウインドウ」節 |
| — | **2-5 の残りはユーザー定義テンプレート**（複製・保存・JSON 入出力 [LT-02][LT-06]）と改訂の差分適用 [LT-04][LT-10〜17]。ライブラリ側の設定編集は 2-12 の第 1 段で済んでいるので、テンプレートの編集 UI は**設定ウインドウの草案（`LibrarySettingsDraft`）を再利用できる**——同じ編集 UI を 2 つ作らないこと |
| — | ~~**2-2 に `transfer()` の部分成功の保持を含める。**~~ **済**（フェーズ1完了後の総点検で `PartialTransferFailure` として実装。2-2 着手時に確認した）。 一括操作の途中で失敗すると、それまでに成功した `OpReceipt` を丸ごと捨てるため、実際にはファイルが移動しているのに Undo にも操作履歴にも残らない。イジェクトがこれを日常的に踏ませるため、着脱検知と同時に直す [RB-04][ER-13] |

| — | **2-8 は完了（2026-08）。** 左ペイン下半分のラベルフィルタ・ピン留め・評価フィルタ。**依存として挙げた 2-6 / 2-7 の UI より先に着手した**——リポジトリ（`ensureLabel`/`setPinned`/`setArchived`）と自動付与はどちらも済んでおり、欠けていたのは「入っているラベルを使う手段」だけだったため。中央ペインへは**実体の一覧を絞る**形で効かせる（14章 §14.2）。**評価フィルタは動くが、評価を付ける手段（RA-01〜08、2-10）がまだ無いので実データでは常に 0 件**になる。詳細は CLAUDE.md「ラベルフィルタ」節 |
| — | ~~**2-13 のラベル保管庫（LAW-01〜03）の必要度が 2-6 で上がった。**~~ **完了**（2026-08、上記）。~~ファイル保管庫（FAW-01〜05）のほうは 2-11 のまま~~ → **こちらも完了**（2026-08、2-11 と同時。15章 §15.4）|
| — | **2-6 は完了（2026-08）。** ラベルグループ・ラベルの編集ウインドウ [LE-01〜LE-12]。**§15.2 の 3 ペインのうち中央（グループの改名・予約語紐づけ）は 2-12 の設定ウインドウと同じ実装を共有する**［ユーザー判断］——同じ編集を 2 箇所に作らない。新設は ①リポジトリの `deleteLabels` / `setColor` / `snapshot` / `restore` ②`LabelEditCommands` の 5 コマンド（**削除と統合は行が消えるので `LabelSnapshot` を控え、AUTOINCREMENT のおかげで同じ ID へ復元できる**［実測］）③`LabelGroupEditorModel` ④ウインドウと `LabelColorWell`。**`merge` の `origin` 決定を manual > auto > manuallyRemoved に直した**［ユーザー判断］——旧実装は統合先を無条件に残し、手動付与が黙って消えていた。**`LabelSummary` が件数を 2 つ持つようになった**——フィルタ用 [FA-05] とバッジ用 [LE-05] で要件が意図的に食い違うため。詳細は 15章 §15.2、経緯は CLAUDE.md「ラベル編集ウインドウ」節 |
| — | **2-10 は第 2 段（ラベル設定 RL-01〜RL-07 ＋ 複数選択の一括付与／削除 RP-02）まで実装済み（2026-08）。** 残りはカバー画像 [CV-02〜08] とタイトル編集 [RP-10〜12]。ラベル設定では **`AssignLabelCommand`（DB 操作の Undo の 2 つ目）**を作り、一覧の並べ方を `PinnedLabelListing`（`QooKit`）へ切り出してラベルフィルタと共有した [RL-04]。詳細は 14章 §14.4「ラベル設定」、経緯は CLAUDE.md「右ペインのラベル設定」節 |
| — | **2-10 は第 1 段（評価 RA-01〜RA-08）を実装済み（2026-08）。** 依存として挙げた 2-9 より先に着手した——2-8 で評価フィルタ [RT-01〜03] が動くようになったのに**星を付ける手段が無く、実データでは常に 0 件**という状態だったため。残りはラベル設定 [RL-01〜07]・カバー画像の差し替え [CV-02〜08]・タイトル編集 [RP-10〜12]。**あわせて「DB 操作の Undo」（2-15）の最初の 1 つを作った**——`RA-06` が「シリーズ全巻への適用は 1 つの Undo 単位」と名指しで要求しており、後から被せる形にできないため。詳細は 14章 §14.4「評価」、経緯は CLAUDE.md「右ペインの評価」節 |
| — | **2-19 は完了（2026-08）。** ファイル自身のメタデータを読み、ファイル名・フォルダ名由来の値より優先する [EM-03]。**読み取りはスキャンの一部**［ユーザー判断］で、印（更新日時＋サイズ）が変わらなければ開き直さない [EM-07] ——これが無いと再スキャンのたびに 5 万件を開く（§9.9 の実測で 7 秒〜10 分）。`ComicInfo.xml` の `Number` と `Volume` は**実装によって真逆**なので、食い違えば走査後にまとめて聞く [EM-26][EM-31]。仕様は 09章 §9.9（読み取り）・04章 §4.10（優先解決）・05章 §5.7（巻数の決着）・15章 §15.1.2（確認ダイアログ）|

### フェーズ 2 の完了条件（DoD）

- ライブラリ管理が実用レベルで動作する [20.2 完了条件]
- ゴールデンテストがプリセット 8 種 × 正例／負例 20 件以上で全件パス [MT-23][MT-24]
- 性能目標 PF-01〜PF-07 を 5 万件のデータセットで満たす
- 「新規ストア → JSON インポート → 再スキャン」で再生成不可能データが完全復元される [MG-24][IE-16]
- 外部ボリュームを抜き差ししてもラベル紐づけが失われない [R-01][SB-05][ID-08]
- T-02 / T-03 / T-04 / T-05 / T-10 の検証結果が反映されている

## 17.5 フェーズ 3: テンポラリフォルダ [20.3 節]

| # | 内容 | 主な要件 | 依存 |
|---|---|---|---|
| 3-1 | 自動リネームルールのエンジンと設定 UI | AR2-01〜AR2-24 / FO-21〜FO-24 | 2-2 |
| 3-1b | **フォーマット変換リネーム**のエンジンと専用ウインドウ | CR-01〜CR-64 / CW-01〜CW-45 | 2-3（入力パーサを流用）|
| 3-2 | 一括リネーム | BR-01〜BR-11 | 1-5, 1-11 |
| 3-3 | ライブラリフォルダへの移動、ペンディング機構 | MV-01〜MV-22 / PW-01〜PW-20 | 2-3, 2-7 |
| 3-4 | テンポラリの設定ウインドウ | 15.5 節 | 3-1, 3-1b |

| # | 注意点 |
|---|---|
| DP-04 | 3-3 は 2-3 のパーサとフォルダ階層割り当てに依存するため、順序を入れ替えられない |
| — | 3-1b は 3-3 のペンディング解決手段でもあるため、3-3 より先に完成させる（PW-17 / CW-40〜CW-45 の連携）|

### フェーズ 3 の完了条件（DoD）

- 取り込みから整理・ライブラリ投入までの一連のワークフローが完結する [20.3 完了条件]
- 変換リネームの実データ誤変換率を測定済み。CW-16 で原因を特定できることを確認 [T-11]
- 自動リネームの非冪等ルールが保存できない [FO-22]
- CR-60 の処理順序が UI 上でも表現されている

## 17.6 リスクと対応の実装対応表

| リスク | 実装での担保 | 検証 |
|---|---|---|
| R-01 外部ボリューム未接続でラベル一括喪失 | 8.7.1 節の `RegisteredFolderStatus`（1-17 で DB 導入前に確定）、`SB-05` の `.offline`、`ID-08` の孤立判定停止、`ID-06` の `.orphaned` 遷移 | 統合テスト「オフライン」 |
| R-02 誤設定による大量ファイル名破壊 | `UD-04` 複合コマンド、`BR-08`/`MV-07`/`CW-10` の実ファイルプレビュー、`HS-01` 全操作ログ | 統合テスト + 手動確認 |
| R-03 NFC/NFD 差異によるラベル重複 | `TextNormalizer` を照合の必須経路に、`LB-07` マージ | 単体テスト |
| R-04 フォーマット設定の難しさ | テンプレート 8 種、`HP-01〜HP-09` の編集支援、`AL-30〜AL-34` の救済フロー、ウィザード ステップ 5 | ユーザビリティ確認 |
| ~~R-05~~ 永続化基盤の性能不足 | **解決**: 着手前の実測（`Spikes/README.md`）で SQLite(GRDB) を採用。全 PF 目標を大きな余裕で満たす。`A-02` の抽象化と `DB-04` の閾値警告は維持 | 実測済（T-03 / T-04）|
| R-06 サムネイルがストレージ・メモリを圧迫 | `IV-09` キャッシュ上限、`PF-11` 同時実行制限、`DS-05` 非表示時は生成しない | 性能テスト |
| R-07 公証なしで実行に手間取る | README に Gatekeeper 回避手順 | — |
| R-08 Undo 基盤の後付け | フェーズ 1 の DoD に含める [DP-01] | フェーズ 1 レビュー |
| R-09 手持ちの外部 HDD が exFAT | `FS-04` の理由明示、`FS-07` でファイルマネージャーとしては使える | 実機確認 |
| R-10 セパレータ型の誤爆 | `DL-11` 既定無効、`TY-01` 型付き照合、`DL-13` の運用案内、`CR-08` プレビュー | ゴールデンテスト（負例）|
| R-11 変換リネームの設定ミス | `CR-61` 手動専用、`CW-10〜CW-17` プレビュー、`CW-16` 分解表示、`CR-11` 1 Undo、`CR-53` 履歴 | T-11 |
| R-12 UnRAR 同梱によるライセンス誤解 | `LC-22`/`LC-23` を必須化、`LC-20` の隔離、`LC-24` 全文収録 | CI（license ジョブ）|
| R-13 自己変更の誤認による無限リネーム | `FO-01` 集約、`FO-11` 台帳、`FO-21` 冪等性（最終防衛線）| 統合テスト「無限リネームの防止」 |
| R-14 スキーマ移行の失敗 | `MG-10` 移行前バックアップ、`MG-20〜MG-24` の分割と JSON 完全保全 | 統合テスト「移行」 |
| R-15 悪意あるアーカイブ | `EX-01` ステージング、`EX-10〜EX-15` パス検証、`EX-20〜EX-24` 展開爆弾対策 | 統合テスト「展開セキュリティ」 |
| R-16 `PERMISSIVE_ONLY_BUILD` で展開できない RAR | 既定ビルドは UnRAR、`T-12` で差分把握、`LC-13`/`AR-07` で明示 | T-12 |

## 17.7 推奨する実装の進め方

1. **フェーズ 0 の 0-3（サンプル収集）を今日から始める。** パーサの品質はサンプル量に比例する [MT-27]。
2. フェーズ 1 では**制約 4 つ（DP-01 / DP-05 / DP-07 / DP-08）を最優先**で入れる。機能を増やす前に骨格を正しくする。
3. フェーズ 2 は `QooKit`（2-3 / 2-4）と永続化（2-1 / 2-2）を並行させる。前者は UI なしでテストできるため、着手が早いほど検証が進む。
4. 各フェーズの DoD をチェックリストとして PR テンプレートに埋め込み、部分的な達成で次へ進まない。
5. 技術検証（T-xx）は該当フェーズの**開始時**に片付ける。設計判断が覆ると手戻りが大きい項目（T-03 / T-05 / T-13）は特に前倒しする。
