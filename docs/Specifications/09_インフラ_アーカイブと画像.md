# 09. インフラ — アーカイブと画像処理

対応要件: AR-01〜AR-23 / EX-01〜EX-34 / LC-10〜LC-17 / IV-01〜IV-09 / CV-01〜CV-08 / QL-01〜QL-10 / DS-01〜DS-07 / DT-05 / DU-21〜DU-23

---

## 9.1 バックエンドの抽象化 [AR-01a][MT-04][LC-16]

```swift
public protocol ArchiveReading: Sendable {
    var supportedFormats: Set<ArchiveFormat> { get }
    func canRead(_ url: URL) async -> Bool
    /// エントリ一覧のみを読む（展開しない）。エンコーディング候補も返す。
    func listEntries(_ url: URL, encoding: StringEncoding?) async throws -> ArchiveListing
    /// 1 エントリだけをメモリへ読む（カバー画像用）。上限あり [EX-32]
    func readEntry(_ url: URL, entry: ArchiveEntry, maxBytes: Int64) async throws -> Data
    /// ステージングディレクトリへ展開する [EX-01]
    func extract(_ url: URL, to staging: URL, options: ExtractOptions,
                 progress: ProgressReporter?) async throws -> ExtractResult
}

public protocol ArchiveWriting: Sendable {
    var supportedFormats: Set<ArchiveFormat> { get }   // rar を含まない [OS-06][LC-17]
    func compress(_ items: [URL], to: URL, format: ArchiveFormat,
                  progress: ProgressReporter?) async throws
}

public enum ArchiveFormat: String, Sendable, CaseIterable {
    case zip, sevenZip, rar, tarGz
    /// cbz→zip, cb7→7z, cbr→rar のマッピングを持つ
    public static func from(fileExtension: String) -> ArchiveFormat?
}
```

### バックエンド構成

| 形式 | 圧縮 | 展開 | バックエンド |
|---|---|---|---|
| zip / cbz | ○ | ○ | libarchive（常時）[AR-01][LC-14] |
| 7z / cb7 | ○ | ○ | libarchive（常時）[LC-14] |
| tar.gz | ○ | ○ | libarchive（常時）[LC-14] |
| rar / cbr | × [OS-06][AR-05] | ○ | 既定 UnRAR [AR-01b][LC-11] / `PERMISSIVE_ONLY_BUILD` 時 libarchive [LC-12] |

```swift
public struct ArchiveBackendRegistry: Sendable {
    public static func reader(for format: ArchiveFormat) -> any ArchiveReading {
        switch format {
        case .rar:
            #if PERMISSIVE_ONLY_BUILD
            return LibarchiveBackend.shared            // [LC-12][LC-13]
            #else
            return UnrarBackend.shared                 // [LC-11]
            #endif
        default:
            return LibarchiveBackend.shared            // [LC-14]
        }
    }
    public static var rarBackendName: String { … }     // アバウト画面・診断情報 [B-01]
}
```

| # | 要件 |
|---|---|
| AB-01 | ビルドフラグによる差し替えが呼び出し側に波及しない構造とする [LC-16][MT-04] |
| AB-02 | 扱えないアーカイブは理由を明示して中止する。無言で失敗しない [AR-06][RB-01] |
| AB-03 | `PERMISSIVE_ONLY_BUILD` で展開できなかった RAR は、既定ビルドなら展開できる可能性がある旨をエラーに含める [AR-07] |
| AB-04 | パスワード付きアーカイブは非対応。遭遇時に明示して中止する [AR-08][OS-09] |
| AB-05 | UnRAR ラッパーの先頭コメントに RAR 互換アーカイバ開発への使用禁止を記す [LC-26] |

## 9.2 文字化け対策 [AR-02〜AR-04]

対象は zip / 7z のみ。RAR はアーカイブ側がエンコーディング情報を持つため対象外 [AR-03][C-06]。

```swift
public enum ArchiveNameEncodingHeuristic {
    public struct Candidate: Sendable {
        public let encoding: String.Encoding      // .utf8 / .shiftJIS (CP932)
        public let score: Double                  // 0.0〜1.0
        public let decodedSamples: [String]
    }
    /// UTF-8 フラグ未設定時の推定 [AR-02]
    public static func evaluate(rawNames: [Data]) -> [Candidate]
}
```

```pseudo
evaluate(rawNames):
    # ① UTF-8 として妥当にデコードできるか
    utf8Score = 妥当にデコードできた件数 / 全件
              - (U+FFFD や制御文字の出現率 × ペナルティ)
    # ② CP932 としてデコードした結果の妥当性
    cp932Score = デコード成功率
               + (日本語文字クラス〔ひらがな・カタカナ・CJK〕の出現率 × ボーナス)
               - (機種依存の未定義領域の出現率 × ペナルティ)
    # ③ よくある化けパターン（"ﾁ" "�" "?" の連続、Latin-1 由来のモジバケ）を減点
    return [utf8, cp932].sorted(byScoreDesc)
```

| # | 要件 |
|---|---|
| EN-01 | Keka の実装を参考にする [AR-02]。判定結果は展開前にプレビューし、ユーザーが手動でエンコーディングを変更できる [AR-04] |
| EN-02 | プレビューは先頭 20 エントリを両エンコーディングで並べて表示する |
| EN-03 | ユーザーが選んだエンコーディングは、そのアーカイブの展開・一覧取得の全経路で使う |
| EN-04 | UTF-8 フラグが立っている場合はヒューリスティックを走らせず UTF-8 として扱う |

## 9.3 展開のセキュリティ [7.3.1 節]

### ステージング [EX-01〜EX-04]

```swift
public struct ExtractOptions: Sendable {
    public var destination: URL
    public var encoding: String.Encoding?
    public var followSymlinks: Bool = false          // [EX-12]
    public var limits: ExtractLimits = .default      // [EX-21]
}

public struct ExtractResult: Sendable {
    public let extractedCount: Int
    public let rejected: [(entry: String, reason: RejectionReason)]  // [EX-14]
    public let renamedForCaseCollision: [(from: String, to: String)] // [EX-15]
    public let totalBytesWritten: Int64
}
```

```pseudo
extract(archive, options):
    staging = appContainer/staging/<uuid>/          # [EX-01][EX-03]
    defer { removeRecursively(staging) }

    # 事前チェック [EX-23]
    declared = 宣言された非圧縮サイズの合計
    if freeSpace(options.destination) < declared + AppLimits.freeSpaceMargin:
        throw .insufficientFreeSpace

    written = 0; entryCount = 0
    for entry in archive.entries:
        entryCount += 1
        if entryCount > limits.maxEntries: throw .tooManyEntries        # [EX-21]

        # ── パストラバーサル対策 ──
        name = normalize(entry.pathname)
        if name.isEmpty                       : reject(.emptyName)      # [EX-10]
        if name.hasPrefix("/")                : reject(.absolutePath)
        if name.components.contains("..")     : reject(.parentTraversal)
        if name.containsNULOrControlChars     : reject(.invalidCharacters)
        if entry.isSymlink and !followSymlinks: reject(.symlinkSkipped)  # [EX-12]
        if entry.isHardlink || isDevice || isFIFO: reject(.specialEntry) # [EX-13]

        target = (staging / name).standardizedFileURL.resolvingSymlinksInPath()
        if !target.path.hasPrefix(staging.resolved.path + "/"):
            reject(.escapesDestination)                                  # [EX-11]

        # 大文字小文字のみ異なるエントリの衝突 [EX-15]
        if caseInsensitiveSeen.contains(target.lowercased):
            target = nextAvailableName(target); record(renamed)

        # ── 展開爆弾対策 ──
        stream = entry.dataStream
        while chunk = stream.next():
            written += chunk.count
            if written > limits.maxUncompressedBytes: throw .expansionLimitExceeded  # [EX-20][EX-21]
            ratio = Double(written) / Double(archive.compressedSize)
            if ratio > limits.ratioAbort:  throw .compressionRatioExceeded
            if ratio > limits.ratioWarn:   warnOnce()
            write(chunk, to: target)
        if Task.isCancelled: throw CancellationError()                   # [EX-24]

    # 最終位置へ移送。FileOperationService 経由 + 台帳登録 [EX-04]
    try await fileOps.promoteFromStaging(staging, to: options.destination)
```

| # | 要件 |
|---|---|
| EXI-01 | 宣言された非圧縮サイズを信用しない。**書き出し中の実バイト数**で判定する [EX-20] |
| EXI-02 | 上限は環境設定で変更できる。中断時は理由と該当する上限を明示する [EX-22] |
| EXI-03 | 拒否・スキップしたエントリは件数と理由を展開結果に表示する [EX-14][RB-01] |
| EXI-04 | キャンセル時はステージングごと破棄する [EX-24] |
| EXI-05 | 異常終了後の次回起動時に残存ステージングを削除する [EX-03][RB-07] |

## 9.4 圧縮・展開メニュー [7.3 節]

| # | メニュー | 挙動 |
|---|---|---|
| AR-10 | ここに圧縮 | カレントフォルダに zip。単一選択時は項目名、複数選択時はカレントフォルダ名 |
| AR-11 | 圧縮… | ファイル名・形式・保存先を指定するダイアログ。既定値は AR-10 と同じ |
| AR-20 | ここに展開 | カレントフォルダへ展開。複数選択時も同じ |
| AR-21 | （名前）に展開 | 圧縮ファイル名のフォルダを作って展開。複数選択時はファイルごと |
| AR-22 | 展開… | 展開先を選ぶダイアログ。複数選択時はまとめて |
| AR-23 | — | 複数選択時の AR-21 の表記は「それぞれのフォルダに展開」 |

**実装状況（1-12 環境設定「圧縮／展開」タブ）**: 圧縮形式（zip/7z）・zip の圧縮レベル・7z のコーデック（PPMd/bzip2/deflate/copy）・zip の暗号化方式（なし/Traditional/AES-128/AES-256）は環境設定の既定値として設定可能にした。AR-11「圧縮…」ダイアログは環境設定の既定形式に従うのみで、**操作のたびに形式を選び直す UI は未実装**（この表の「ファイル名・形式・保存先を指定する」の「形式」部分は環境設定側の既定を使う設計に簡略化している）。7z は libarchive の書き込み側に暗号化オプションが存在しないため、暗号化は zip 選択時のみ有効。パスワード文字列自体は環境設定に保存せず、暗号化が有効なときは圧縮のたびに `ArchivePasswordSheet` で入力させる。展開の安全上限（`ExtractLimits`）も同タブで調整可能。詳細は CLAUDE.md 参照。

### 圧縮エントリ名の Unicode 正規化 [設計判断][AR-10][AR-11]

macOS の Foundation API（`FileManager`/`URL`）はファイル名を NFD（濁点・半濁点等が
結合文字に分解された形）で返すことが多い。これをそのまま zip のエントリ名として
書き込むと、NFC を前提とする Windows 側の展開ツールで文字化けしたり、見た目は同じ
なのにバイト列が異なる別名として扱われたりする（Mac 製 zip アーカイバでよく知られた
問題）。**圧縮時は各エントリ名を NFC 正規化してから書き込み、UTF-8 フラグも明示的に
立てる**（Keka 等と同じ方式、EN-01 参照）。N-01〜N-03 のラベル照合用正規化
（`TextNormalizer`、3章）とは目的も適用箇所も別物で、両者を混同しない。

## 9.5 画像読み込みの防御 [EX-30〜EX-34]

```swift
public protocol ImageLoading: Sendable {
    /// ヘッダのみを読んで寸法を取得する（デコードしない）[EX-31]
    func imageSize(from data: Data) -> CGSize?
    /// ImageIO のサムネイル生成 API を使い、全画素を展開しない [EX-33]
    func makeThumbnail(from data: Data, maxPixelSize: Int) throws -> CGImage
}
```

| # | 要件 |
|---|---|
| IM-01 | ピクセル数上限 1 億。デコード前にヘッダから寸法を取得して判定し、超過は生成をスキップ [EX-31] |
| IM-02 | 1 エントリの読み込み上限 512MB [EX-32] |
| IM-03 | `CGImageSourceCreateThumbnailAtIndex` を `kCGImageSourceCreateThumbnailFromImageAlways` + `kCGImageSourceThumbnailMaxPixelSize` で使う [EX-33] |
| IM-04 | 生成失敗時は既定アイコンにフォールバックし、アプリ全体を巻き込まない [EX-34] |
| IM-05 | Quick Look 用の読み込みにも同じ上限を適用する [QL-10] |

## 9.6 サムネイル・カバー画像

### 解決順序 [IV-02][IV-03][QL-05]

```pseudo
resolveCover(file, library):
    if file.coverImageSource == .userSpecified and cacheExists: return cached  # ①
    if let sidecar = sidecarCover(of: file): return sidecar                    # ②
    return firstImageInArchiveOrFolder(file)                                   # ③
```

- サイドカーの探索: ファイルと同じフォルダの `covers/` に、拡張子のみ異なる同名画像 [IV-02②][CL-04]
- `.qooarchive` 配下でも探索する [FA-17]
- ブックフォルダは配下画像の自然順先頭 [IF-14]

```swift
public actor ThumbnailService {
    /// 同時実行数を制限したタスクキュー [PF-11]
    public init(maxConcurrent: Int = 4, cache: CoverImageCache)

    public func thumbnail(for file: FileSnapshot, size: CGSize) async -> CGImage?
    public func invalidate(_ fileIDs: [UUID]) async
    public func regenerate(_ fileIDs: [UUID]) async         // 手動コマンド [MX 一覧]
    public func setGloballyDisabled(_ disabled: Bool) async // [DS-01][DS-05]
}

public protocol CoverImageCache: Sendable {
    /// ~/Library/…/qooLibrary/covers/<libraryUUID>/<fileUUID>.<ext> [CL-05]
    func url(for fileID: UUID, libraryID: UUID) -> URL
    func store(_ data: Data, for fileID: UUID, libraryID: UUID) throws -> String
    func totalSize() async -> Int64
    func prune(toMaxSize: Int64) async                      // [IV-09]
    func clear() async                                      // [IV-09]
}
```

| # | 要件 |
|---|---|
| TH-01 | 生成はバックグラウンド非同期。生成中はプレースホルダを表示 [IV-08] |
| TH-02 | スクロールを妨げない [PF-06]。可視範囲を優先するキュー（LIFO + 可視判定）とする |
| TH-03 | キャッシュ上限と手動クリアを環境設定に設ける [IV-09] |
| TH-04 | ユーザー指定カバーは App Support 配下に**複製**して保存する。元画像が消えても表示は維持される [CV-06][CV-08] |
| TH-05 | 「既定に戻す」で自動抽出へ戻せる [CV-07] |
| TH-06 | アーカイブ内の別ファイル選択、アーカイブ外のファイル選択の双方をサポート [CV-05] |
| TH-07 | 差し替えはカバー画像上への D&D とダイアログの両方から [CV-03][CV-04] |

### サムネイル表示の一括制御 [DS-01〜DS-07]

| # | 要件 |
|---|---|
| DS2-01 | 一括トグル。無効時は汎用アイコンに切り替わる [DS-01] |
| DS2-02 | メニューバーとキーバインド（既定 `⌃⌘I`）から即時切替 [DS-02] |
| DS2-03 | 全ウインドウ・全タブに即座に反映（アプリ全体の状態）[DS-03] |
| DS2-04 | ライブラリごとに既定値を持つ（`Library.thumbnailsHiddenByDefault`）[DS-04] |
| DS2-05 | **非表示中は生成・キャッシュを行わない**。再表示時に生成する [DS-05] |
| DS2-06 | 右ペインのカバー表示 [CV-01] と Quick Look [QL-03] も同じトグルに従う [DS-06] |
| DS2-07 | 現在の状態をツールバーまたはステータスバーに示す [DS-07] |

## 9.7 Quick Look [7.7 節]

```swift
@MainActor
public final class QuickLookCoordinator: NSObject, QLPreviewPanelDataSource, QLPreviewPanelDelegate {
    public func toggle()                       // Space キー [QL-01]
    public func updateSelection(_ items: [FileSnapshot])   // 矢印キー追従 [QL-07]
}
```

| # | 要件 |
|---|---|
| QL2-01 | pdf / epub / 画像は標準 Quick Look に委譲する [QL-02] |
| QL2-02 | `.cbz` / `.cbr` / `.cb7` およびブックフォルダは**独自プレビュー**に差し替える [QL-03][QL-08] |
| QL2-03 | 独自プレビューはカバー画像 1 枚のみ。ページ送りを持たない [QL-04] |
| QL2-04 | カバー選択順は IV-02 / IV-03 に従う [QL-05] |
| QL2-05 | タイトル・シリーズ名・巻数・評価・付与ラベル・ファイルサイズを併記 [QL-06] |
| QL2-06 | 矢印キーで選択移動、プレビューが追従 [QL-07] |
| QL2-07 | カバー未生成なら待たずプレースホルダを出し、非同期に差し替える [QL-09] |
| QL2-08 | 実装は `QLPreviewingController` ではなく、`QLPreviewPanel` の `previewItem` に独自ビューを持つ `NSViewController` を返す方式とする [設計判断: 拡張機能（App Extension）を追加せずに済み、公証なしの配布形態と相性が良い] |

## 9.8 遅延メタデータ [DT-05][DT-06][DU-21][DU-22]

```swift
public actor ArchiveMetadataService {
    /// ページ数（画像ファイル数）・サブフォルダ数・先頭画像の解像度。
    /// アーカイブを開くため遅延取得し、結果を DB にキャッシュする。
    public func metadata(for file: FileSnapshot) async throws -> ArchiveMetadata
}

public struct ArchiveMetadata: Sendable {
    public let entryCount: Int
    public let imageCount: Int          // ページ数 [DU-21]
    public let subfolderCount: Int      // [DT-06]
    public let firstImageSize: CGSize?  // [DU-21]
}
```

| # | 要件 |
|---|---|
| MD-01 | 算出中はプレースホルダを表示する [DT-05 注][DU-22] |
| MD-02 | 結果は `ManagedFile.pageCount` / `subfolderCount` / `firstImageWidth` / `firstImageHeight` にキャッシュする（`@Regenerable`）[DU-22] |
| MD-03 | 比較ビュー [DU-20] を開いたときにまとめて非同期取得する。同時実行数を制限する |
| MD-04 | EPUB のタイトルメタデータもここで取得する [DT-08][MG-21] |
