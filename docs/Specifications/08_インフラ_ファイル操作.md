# 08. インフラ — ファイル操作

対応要件: FO-01〜FO-24 / FM-01〜FM-18 / DD-01〜DD-07 / SL-01〜SL-07 / TR-01〜TR-09 / FA-01〜FA-17 / FDA-01〜FDA-05 / RG-01〜RG-08 / SB-05 / FS-08 / LP-04 / VD-11（8.7.1 節）

---

## 8.1 FileOperationService [FO-01]

**ファイルシステムへの変更はすべてこのサービスを経由する。** 他のレイヤから `FileManager` の変更系 API を呼んではならない。CI の静的検査 B-10 で強制する [FO-02]。

```swift
public actor FileOperationService {
    public init(ledger: ExpectedChangeLedger,
                history: OperationHistoryStore,
                bookmarks: BookmarkResolving,
                logger: DiagnosticLogging)

    // MARK: - 基本操作 [7.1 節]
    public func createDirectory(at: URL, options: OpOptions) async throws -> OpReceipt   // [FM-01]
    public func copy(_ items: [URL], to: URL, options: OpOptions) async throws -> [OpReceipt] // [FM-02]
    public func move(_ items: [URL], to: URL, options: OpOptions) async throws -> [OpReceipt] // [FM-03]
    public func rename(_ item: URL, to newName: String, options: OpOptions) async throws -> OpReceipt // [FM-05]
    public func trash(_ items: [URL], options: OpOptions) async throws -> [TrashReceipt] // [FM-04]
    public func deletePermanently(_ items: [URL], options: OpOptions) async throws -> [OpReceipt] // [FM-14]
    public func restoreFromTrash(_ receipts: [TrashReceipt]) async throws                // [UD-08]

    // MARK: - 一括・2 パス
    /// 循環リネームに対応した 2 パス実行 [BR-10][CR-52]
    public func renameBatchTwoPass(_ plan: [(from: URL, toName: String)],
                                   options: OpOptions) async throws -> BatchSummary

    // MARK: - ステージングからの移送 [EX-04]
    public func promoteFromStaging(_ staging: URL, to destination: URL,
                                   options: OpOptions) async throws -> [OpReceipt]
}

public struct OpOptions: Sendable {
    public var conflictPolicy: ConflictPolicy = .ask       // [FM-11]
    public var suppressAutoRename: Bool = false            // [CR-63][PW-15]
    public var suppressFSEvents: Bool = false              // 一括中の監視停止 [FO-14]
    public var commandContext: CommandContext?             // Undo/履歴の起点 [FO-03]
    public var progress: ProgressReporter?
}

public enum ConflictPolicy: Sendable {
    case ask                     // ダイアログ [FM-11]
    case replace                 // [FM-13]
    case keepBoth                // 連番付与
    case skip
}

public struct OpReceipt: Sendable {
    public let before: FileIdentity?
    public let after: FileIdentity?
    public let fromURL: URL
    public let toURL: URL
    public let kind: OperationKind
}
```

| # | 要件 |
|---|---|
| FS2-01 | `FileOperationService` は Undo コマンド生成と操作履歴記録の起点を兼ねる [FO-03]。`OpReceipt` を返し、呼び出し側の Command がそれを保持して `undo()` を実装する |
| FS2-02 | xattr 書き込み API を持たない [CL-03] |
| FS2-03 | サイドカーファイルを作る API を持たない [CL-01] |
| FS2-04 | すべての操作は実行前に対象ボリュームのオンライン状態を確認する。オフラインなら `.volumeOffline` で失敗する [VD-11] |
| FS2-05 | シンボリックリンク・エイリアスは追跡しない。リンク自体を 1 項目として扱う [SL-01] |

## 8.2 期待変更台帳 [FO-11〜FO-13]

自己変更を FSEvents から確実に除外するための主たる手段。

```swift
public actor ExpectedChangeLedger {
    public struct Entry: Sendable, Hashable {
        public let volumeUUID: String
        public let inode: UInt64?          // 作成時は nil（まだ存在しない）
        public let path: String            // 正規化済み絶対パス
        public let kind: OperationKind
        public let expiresAt: Date
        public let suppressAutoRename: Bool
    }

    /// 操作の【実行前】に登録する [FO-11]
    public func register(_ entries: [Entry])
    /// イベント受信時に照合し、一致したエントリを消費する [FO-12]
    public func consume(volumeUUID: String, inode: UInt64?, path: String,
                        kind: OperationKind) -> Entry?
    /// 期限切れを破棄する（10 秒ごとのタイマー、および consume のたび）[FO-13]
    public func purgeExpired(now: Date)
}
```

```pseudo
FileOperationService.move(items, dest, options):
    entries = []
    for src in items:
        entries.append(Entry(vol, inode(src), src.path, .moveFrom, now + 10s))
        entries.append(Entry(vol, nil,        dest/src.name, .moveTo, now + 10s))
    await ledger.register(entries)          # 【実行前】に登録 [FO-11]
    for src in items:
        try FileManager.default.moveItem(...)
    …
```

### 4 層の自己変更識別 [5.4 節]

| 層 | 実装 |
|---|---|
| 1 | `FSEventStreamCreate` に `kFSEventStreamCreateFlagIgnoreSelf` を指定。**一次フィルタとしてのみ位置づける** [FO-10] |
| 2 | 期待変更台帳。主たる識別手段 [FO-11〜FO-13] |
| 3 | 一括操作中の監視停止。完了後に差分照合を 1 回 [FO-14][FO-15] |
| 4 | 処理の冪等化。漏れたイベントを無害化 [FO-20〜FO-24] |

| # | 要件 |
|---|---|
| LG3-01 | 台帳の照合キーは `(volumeUUID, inode, path)` の**いずれか一致**で緩めに判定する。移動先ではまだ inode が確定していないためパスで照合する |
| LG3-02 | 期限切れエントリは以後「外部変更」として扱う [FO-13] |
| LG3-03 | 一括操作（一括リネーム・変換リネーム・一括移動・一括アーカイブ）中は対象フォルダの監視を停止する。この集合は 5.6 節の排他処理と同一 [FO-14] |
| LG3-04 | 監視停止前の `FSEventStreamEventId` を保持し、再開時に差分を取得する [FO-15][SY-03] |

## 8.3 衝突処理 [FM-11〜FM-13]

```swift
public struct ConflictDecision: Sendable {
    public let policy: ConflictPolicy
    public let applyToAll: Bool          // [FM-12]
    public let inheritLabels: Bool?      // .replace のとき [FM-13]
}
```

```pseudo
resolveConflict(src, dest, options, session):
    if !exists(dest): return .proceed
    switch options.conflictPolicy:
      case .ask:
          d = await session.ask(.nameCollision(src, dest))    # 強度 2 シート [ER-11]
          if d.applyToAll: options.conflictPolicy = d.policy
          return d
      case .replace:
          # 置換されるファイルの DB レコードを削除し、
          # 置き換えたファイルへラベルを引き継ぐか確認する [FM-13]
      case .keepBoth: dest = nextAvailableName(dest)   # "name 2.cbz"
      case .skip:     return .skip
```

| # | 要件 |
|---|---|
| CF-01 | 連番付与の書式は Finder に倣い `name 2.ext` `name 3.ext` とする |
| CF-02 | 一括処理中は「以降すべてに適用」を必ず提供する [FM-12][ER-11][FM-18] |
| CF-03 | 保管庫への移動時は例外的に、確認せず連番を付与して両方残す [FA-13] |

## 8.4 ゴミ箱 [7.8 節]

```swift
public struct TrashReceipt: Sendable {
    public let originalURL: URL
    public let trashURL: URL?            // NSWorkspace.recycle の結果
    public let identity: FileIdentity
}
```

| # | 要件 |
|---|---|
| TR2-01 | `NSWorkspace.shared.recycle(_:completionHandler:)` を使い、Finder の「元に戻す」と互換にする [FM-04] |
| TR2-02 | DB レコードは削除せず `state = .trashed`、`trashedAt` を記録。ラベル・評価・カバーはすべて保持 [TR-01][TR-03] |
| TR2-03 | `.trashed` はラベルフィルタ・検索・ライブラリ表示モード・件数バッジから除外 [TR-02] |
| TR2-04 | 元の場所へ戻された場合は inode 一致で `.active` へ復帰 [TR-04]。別の場所なら ID-02〜ID-04 の複合判定で再照合 [TR-05] |
| TR2-05 | 既定 30 日で自動削除。環境設定で変更可（0 = 即削除、-1 = 無期限）[TR-06] |
| TR2-06 | ゴミ箱が空にされたことを検知しない。期限まで保持する [TR-07] |
| TR2-07 | 一覧の確認と手動即時削除を提供する [TR-08] |
| TR2-08 | Undo は `TrashReceipt.trashURL` から復元。失敗時はゴミ箱を開いて手動復元を促す [TR-09][UD-08] |

## 8.5 完全削除 [FM-14〜FM-18]

| # | 要件 |
|---|---|
| PD-01 | メニューおよびコンテキストメニューから実行できる [FM-14] |
| PD-02 | 実行前に確認ダイアログを必ず表示し、対象件数と合計サイズを明示する [FM-15][UD-10] |
| PD-03 | キーバインドを既定で割り当てない [FM-16] |
| PD-04 | DB レコードは即座に削除する。`.trashed` の対象外 [FM-17] |
| PD-05 | Undo スタックに積まない。積まないこと自体を確認ダイアログで明示する [UD-10] |
| PD-06 | 一括処理中の確認は「以降すべてに適用」を提供する [FM-18][ER-11] |

### 実装上の確定事項（1-14 で実装）

| # | 内容 |
|---|---|
| PD-10 | `deletePermanently` は**最初の失敗で中断しない** [ER-13]。成功・失敗・スキップを `DeletionOutcome` に個別に集めて返し、完了後に結果サマリを提示する [ER-12][ER-14]。他の一括操作（`transfer` 等）が「最初の失敗で例外を投げ、それまでの `OpReceipt` を捨てる」形なのに対し、完全削除だけこの形にする理由は、**「実際にはファイルが消えているのに操作は失敗扱いで記録も残らない」状態が復元不能な事故に直結する**ため |
| PD-11 | PD-06 の「都度尋ねる対象」は**ロック済み項目**とする。ER-11 が定める「ユーザーの選択によって結果が変わるもの」に完全削除で該当するのはこれだけで、Finder も同じくロック項目のみを個別に確認する。「以降すべてに適用」の状態は UI 側が `DeletePermanentlyOptions.lockedItemResolver` に閉じ込めて保持する（`BatchNotificationSession` を待たない）[設計判断] |
| PD-12 | 確認は**操作対象 1 件ごと**に行い、フォルダ配下の個々のロック項目ごとには行わない。フォルダ自身がロックされていなくても**配下にロック項目を含めば尋ねる** — `FileManager.removeItem` はロック済みの子に当たった時点で失敗し、そこまでに消した子だけが失われた中途半端な状態を残すため [設計判断] |
| PD-13 | ロック解除は削除の直前にのみ行い、**削除に失敗したら元に戻す**。シンボリックリンクの先へは入らない（`removeItem` はリンク自体しか消さないため、リンク先のロックを外してはならない）|
| PD-14 | 完全削除で実体を失うライブラリ／テンポラリ登録は**強制的に登録解除する** [ユーザー要望]。対象の照合は削除の**前**（ブックマークが解決できるうち）に行い、解除は削除の**後**に行う（先に解除するとアクセススコープが閉じて削除自体が権限エラーになる）。確認ダイアログにも登録が解除される旨を明示する |
| PD-15 | メニュー配置は Finder と同じく**「ゴミ箱に入れる」[FM-04] の ⌥ 代替**とする（`modifierKeyAlternate(.option)`、13章 §13.7.1）。対にすることで「ゴミ箱を出せない場面で完全削除だけが現れる」経路が構造的に無くなる。**[訂正]** 当初は「SwiftUI では ⌥ による入れ替えを実装できない」との誤った判断から「その他 ▸」サブメニューへ退避していたが、`View.modifierKeyAlternate(_:_:)`（macOS 15.0+）で実装可能と判明したため、サブメニューは廃止した |

## 8.6 ドラッグ＆ドロップ [7.2 節]

| # | 要件 |
|---|---|
| DD2-01 | アプリ内 D&D はコピー（Option 併用）と移動をサポート [DD-01] |
| DD2-02 | 他アプリ・Dock へは `NSFilePromiseProvider` ではなく**実 URL を提供**する [DD-02] |
| DD2-03 | 他アプリ・Finder からのドロップ取り込みをサポート [DD-03] |
| DD2-04 | 左ペインの「ライブラリフォルダ」「テンポラリフォルダ」見出しへのドロップで登録 [DD-04]。FS 適合検証を通す [FS-05] |
| DD2-05 | 中央ペインの選択項目を左ペインのフォルダへドロップして移動 [DD-05] |
| DD2-06 | ドロップ先がライブラリなら移動完了後に自動ラベル付与 [DD-06] |
| DD2-07 | ライブラリ配下をテンポラリとして登録（およびその逆）は拒否し理由を表示 [DD-07][RG-03] |
| DD2-08 | ロック中は変更を伴うドロップを拒否し、理由を表示 [LK-24] |

## 8.7 フォルダ登録 [7.5 節]

```swift
public protocol FolderRegistering: Sendable {
    func registerLibrary(url: URL, typeID: UUID, displayName: String?) async throws -> UUID
    func registerTemporary(url: URL, displayName: String?) async throws -> UUID
    func unregister(_ id: UUID, kind: RegisteredFolderKind, keepLabels: Bool) async throws
}
```

```pseudo
registerLibrary(url, typeID, name):
    # ① シンボリックリンク/エイリアスは実パスに解決 [SL-07]
    url = url.resolvingSymlinksInPath()

    # ② 入れ子の禁止 [RG-03][RG-04]
    if url が既存ライブラリ/テンポラリの配下、または祖先: throw .nestedRegistration

    # ③ FS 適合検証 [RG-08][FS-01〜FS-05]
    switch await volumeChecker.evaluate(url):
      case .rejected(r): throw .unsupportedFileSystem(r)
      case .eligible(warnings): 警告を提示（ネットワークボリューム等）[FS-06]

    # ④ Security-Scoped Bookmark 生成 [RG-07]
    bookmark = try bookmarks.makeBookmark(for: url)

    # ⑤ テンプレート適用 → 初回フルスキャン [LT-03][AT-02]
```

| # | 要件 |
|---|---|
| RG2-01 | 実フォルダ名とは別の表示名を設定可 [RG-05] |
| RG2-02 | 登録解除時、ラベル情報を削除するか保持するかを選択できる（再登録時に復元可能）[RG-06] |
| RG2-03 | 保持を選んだ場合、`Library` レコードを `isUnregistered = true` で残し、再登録時に相対パスで突き合わせる |

### 8.7.1 登録フォルダの縮退状態 [SB-05][FS-08][LP-04][VD-11]

登録フォルダには、本アプリの制御外で操作が加わる——ボリュームのイジェクト、削除、移動、リネーム。
**「ブックマークを解決できたか否か」の二値では、性質のまったく異なる事象が同じ扱いに潰れる。**
ボリューム未接続は「待てば必ず戻る」ので登録を絶対に消してはならず、完全削除は「戻らない」ので
登録の整理をユーザーに促すべきである。この区別を状態として持つ。

#### 実測にもとづく前提

以下は使い捨てのプローブと `hdiutil` のスパースイメージで実測して確定させた事実であり、
設計はこれらを前提にしている。**推測で置き換えないこと。**

| # | 実測結果 |
|---|---|
| BM-1 | 登録フォルダのリネーム・移動、祖先のリネームでは、ブックマークは**追従して解決に成功する**（`isStale = true`）|
| BM-2 | **ゴミ箱へ移動しても解決は失敗しない。**`~/.Trash/…` を指したまま成功する（inode を追跡するため）|
| BM-3 | 完全削除は `NSCocoaErrorDomain` code = 4 |
| BM-4 | **ボリュームのアンマウントも code = 4。**エラーコードだけでは BM-3 と区別できない |
| BM-5 | **`.withoutMounting` を付けない解決は、未マウントのボリュームを実際にマウントする。**ネットワークボリューム（FS-06）ではタイムアウトぶんブロックし得る |
| BM-6 | 未マウント時でも `startAccessingSecurityScopedResource()` は `true` を返す。生存確認には使えない |
| BM-7 | 削除後に同名フォルダを作り直すと、inode が違っても**パスで解決してしまう** |
| BM-8 | セキュリティスコープの保持はアンマウントを妨げなかった（非サンドボックスでの実測。サンドボックス下は要実機検証）|

#### 状態

```swift
public enum RegisteredFolderStatus: Sendable, Equatable {
    case online(url: URL)
    case offline(volumeUUID: String)      // [SB-05][VD-05]
    case inTrash(url: URL)                // [設計判断] 要件定義書には無い。BM-2 を受けて新設
    case missing                          // 削除された可能性。自動解除はしない [ID-06 の精神]
    case unsupportedFileSystem            // [FS-08] オフラインとは区別する
}
```

| 状態 | 表示 | 許可する操作 | 復帰 |
|---|---|---|---|
| `.online` | 通常 | 制限なし | — |
| `.offline` | グレーアウト＋「ボリュームが接続されていません。接続すると自動的に復帰します」[LP-04][VD-11] | 「登録解除」のみ。**展開しても子を読みに行かない** | マウント検知で自動 [VD-03] |
| `.inTrash` | 警告色＋「この登録フォルダはゴミ箱にあります」 | **書き込み系を全面禁止。**「Finder で表示」「登録解除」のみ | ユーザーがゴミ箱から戻せば自動 |
| `.missing` | 「見つかりません」 | 「登録解除」「場所を選び直す…」 | — |
| `.unsupportedFileSystem` | 警告つき | 閲覧のみ。登録解除または移行を促す [FS-08] | — |

#### 判定順序 [設計判断]

**ボリュームのマウント状態を、ブックマークを解決する「前」に見る。**
順序を逆にすると BM-5 を踏む——未接続を判定するはずの処理が、その判定の過程で
ボリュームをマウントしてしまう。

```pseudo
resolveStatuses(folders):
    # ① マウント済みボリュームの UUID → URL マップを 1 回だけ作る。
    #    登録ごとに問い合わせない（性能）
    mounted = mountedVolumeURLs() をUUIDで索引化

    for folder in folders:
        # ② 未マウントならここで打ち切る。解決を一切試みない [BM-5 の回避]
        if folder.volumeUUID != nil && mounted[folder.volumeUUID] == nil:
            yield .offline(folder.volumeUUID); continue

        # ③ マウントは抑止して解決する
        switch bookmarks.resolve(folder.bookmarkData)   # 既定で .withoutMounting
          case .offline: yield .missing; continue       # マウント済みなので削除と断定してよい
          case .resolved(url): break

        # ④ ゴミ箱配下か [BM-2]
        if url.pathComponents contains ".Trash" または ".Trashes":
            yield .inTrash(url); continue

        # ⑤ 実在とディレクトリ種別の確認
        if !isDirectory(url): yield .missing; continue

        yield .online(url)
```

| # | 要件 |
|---|---|
| RG3-01 | `BookmarkResolving.resolve` は既定で `.withoutMounting` を付ける。マウントを許す解決は、ユーザーの明示的な操作からのみ呼ぶ別 API に分ける [BM-5] |
| RG3-02 | `RegisteredFolder` に `volumeUUID: String?` を持つ [VD-02]。`Optional` なので既存 JSON はそのままデコードでき、旧レコードは初回のオンライン解決時に埋める（値が `nil` であること自体が「未移行」を表すため、専用の移行フラグは不要）|
| RG3-03 | ゴミ箱判定は `pathComponents` の照合で行う。**`FileManager.url(for: .trashDirectory, appropriateFor:)` は使わない** — ボリュームによって `code 3328`（未サポート）で失敗することを実測で確認済み（同一マシンで `/` は成功、`/Volumes/T7` は失敗）|
| RG3-04 | いずれの状態でも**登録レコードを自動削除しない** [SB-05][R-01]。登録解除は必ずユーザーの明示的な操作で行う |
| RG3-05 | 入れ子の禁止 [RG-03][RG-04] は外部での移動によって事後に破れる（BM-1）。起動時とオンライン復帰時に再検証し、破れていたら警告する。**自動解除はしない** |
| RG3-06 | 表示中のタブが読み込みに失敗したとき、原因がボリューム未接続なら**祖先へ退避させない**。オフラインのプレースホルダを出し、履歴も破棄しない（破棄すると挿し直しても戻れない）。退避するのは削除・改名の場合のみで、退避先は登録ルートで止める（`NavigationRoot` の境界と揃える）|
| RG3-07 | フェーズ 1 では着脱検知を持たないため、判定はツリー更新時にマウント一覧と突き合わせる受動的なものに留める。2-2 で `NSWorkspace` のボリューム通知に置き換える（10.2 節）。**状態モデルと判定順序は変えず、遷移の駆動方法だけを差し替える** |
| RG3-08 | ゴミ箱を監視する必要はない。`.inTrash` は解決先を見るだけの受動的な判定であり、[TR-07] の「システムのゴミ箱を監視しなくて済むようにする」方針と整合する |

## 8.8 ファイル／フォルダアーカイブ [8.8, 8.9 節]

```swift
public protocol FileArchiving: Sendable {
    func archive(files: [PersistentIdentifier], libraryID: UUID) async throws -> BatchSummary
    func unarchive(files: [PersistentIdentifier]) async throws -> BatchSummary
    func archiveFolder(_ url: URL, libraryID: UUID) async throws -> BatchSummary   // [FDA-01]
}
```

```pseudo
archive(file):
    src  = library.root / file.relativePath
    dst  = library.root / ".qooarchive" / file.relativePath      # [FA-02][FA-03]
    createIntermediateDirectories(dst.parent)
    if exists(dst): dst = nextAvailableName(dst)                 # [FA-13]
    receipt = fileOps.move([src], to: dst, options: .init(conflictPolicy: .keepBoth))

    # サイドカーカバー画像の連動 [FA-14]
    if let cover = sidecarCover(of: src):        # src.parent/covers/<basename>.<imgExt>
        coverDst = dst.parent / "covers" / cover.lastPathComponent
        fileOps.move([cover], to: coverDst)

    file.isArchived = true                                        # [FA-05]
    file.archivedFromPath = file.relativePath                     # [FA-04]
    file.archivedAt = now
    removeEmptyFoldersUpward(src.parent, stopAt: library.root)    # [FA-06][FA-10][FA-16]
```

| # | 要件 |
|---|---|
| FA2-01 | `.qooarchive` はライブラリフォルダ直下。同一ボリューム内移動のため inode が維持される [FA-02][T-07] |
| FA2-02 | 保管庫内はライブラリルートからの相対パスを維持する。`.qooarchive` を取り除くと元パスと一致する [FA-03] |
| FA2-03 | ラベル紐づけは維持し、`isArchived = true` でフィルタ結果から除外 [FA-05][LF-13] |
| FA2-04 | 空になったフォルダは自動削除。隠しファイルのみが残る場合も空とみなす [FA-06][FA-08][FA-10] |
| FA2-05 | 戻す先が削除済みなら作成して戻す [FA-09] |
| FA2-06 | サイドカー画像の探索は `.qooarchive` 配下でも有効 [FA-17] |
| FA2-07 | フォルダアーカイブは丸ごと移動。配下の全ファイルが `isArchived = true` [FDA-01][FDA-02] |
| FA2-08 | 「フォルダを保管庫から戻す」は提供しない [FDA-04]。整理ウインドウではファイル単位で扱う [FDA-05] |
| FA2-09 | `.qooarchive` はフォルダ表示モードかつラベルフィルタ全 OFF のときのみアクセス可能 [FA-11][VM-04] |
| FA2-10 | ライブラリ表示モードでは `.qooarchive` 配下を表示しない [FA-12][VM-14] |
| FA2-11 | `.qooarchive` は左ペインのツリーに表示しない [LP-08] |
| FA2-12 | `.qooarchive` 配下も通常スキャンの対象に含める [SY-10] |

## 8.9 ブックフォルダ判定 [8.10 節]

```swift
public enum BookFolderDetector {
    public static func isBookFolder(_ url: URL,
                                    targetExtensions: Set<String>,
                                    imageExtensions: Set<String>,
                                    entries: [DirectoryEntry]) -> Bool
}
```

```pseudo
isBookFolder(url, target, image, entries):
    name = url.lastPathComponent
    if name == "covers" or name == ".qooarchive" or name.hasPrefix("."): return false  # [IF-03]
    if entries.contains(where: isDirectory):        return false   # ① [IF-01]
    if entries.contains(where: ext in target):      return false   # ②
    if !entries.contains(where: ext in image):      return false   # ③
    return true
```

| # | 要件 |
|---|---|
| BF-01 | 判定はスキャン時に行い `isBookFolder` に保持。内容変化で再判定 [IF-04] |
| BF-02 | 1 冊扱いが解除された場合、`.orphaned` にせず通常フォルダに戻したうえで通知する [IF-05] |
| BF-03 | `ManagedFile` を 1 レコード登録。同一性キーはファイルと同じ `(volumeUUID, inode)` [IF-10] |
| BF-04 | 配下の個々の画像は `ManagedFile` として登録しない [IF-12] |
| BF-05 | 階層としてカウントしない。親フォルダまでが階層割り当ての対象 [IF-13] |
| BF-06 | カバー既定は配下画像をファイル名の**自然順**（数値部分を数値比較）で並べた先頭 1 枚 [IF-14] |
| BF-07 | アーカイブの対象単位としても 1 件。フォルダごと `.qooarchive` へ移動 [IF-15] |
| BF-08 | フォルダ表示モードでは通常フォルダとして操作でき、アイコンにインジケータを表示 [IF-17] |
| BF-09 | 「開く」の既定はフォルダを開く。関連付けアプリで開く設定も選択可 [IF-18][AS-06] |

## 8.10 シンボリックリンク [7.6 節]

| # | 要件 |
|---|---|
| SL2-01 | 追跡しない。リンク自体を 1 項目として表示 [SL-01] |
| SL2-02 | フォルダ表示モードでは Finder 同様に表示し、ダブルクリックでリンク先へ移動（表示上の追従のみ）[SL-02] |
| SL2-03 | スキャン・自動ラベル付与の対象外。`ManagedFile` として登録しない [SL-03] |
| SL2-04 | ライブラリ表示モードには表示しない [SL-04] |
| SL2-05 | フォルダへのリンクはツリー展開の対象としない（無限ループ防止）[SL-05] |
| SL2-06 | アイコン上のバッジで視覚的に区別 [SL-06] |
| SL2-07 | 登録時はリンク先の実パスに解決 [SL-07] |
